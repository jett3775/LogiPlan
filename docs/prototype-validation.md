# LogiPlan AI 方案 B 桌面原型验收记录

状态：人工验收通过  
日期：2026-08-17  
依据：D-123、`docs/prototype-spec.md`

## 1. 验收环境

| 项目 | 1440px 基准 | 1280px 最低支持宽度 |
|---|---|---|
| 浏览器与版本 | 待填写 | 待填写 |
| 操作系统 | Windows | Windows |
| 页面缩放 | 100% | 100% |
| 实际视口 | 待填写 | 待填写 |

## 2. 核心路径与交互

| 编号 | 操作 | 预期结果 | 1440px | 1280px | 问题编号 |
|---|---|---|---|---|---|
| V01 | 打开全年工作区 | 固定显示方案 B；左侧范围为“2026 全年 · 公司”；无 A/C 入口或方案切换器 | 通过（用户确认） | 通过（用户确认） | — |
| V02 | 查看指标带和月度主图 | 六项指标可读；Budget、Actual、Forecast 和 8—9 月结账分界清楚 | 通过（用户确认） | 通过（用户确认） | — |
| V03 | 展开运营固定成本 | 显示四类成本及发货仓或共享层归属；展开后无溢出或遮挡 | 通过（用户确认） | 通过（用户确认） | — |
| V04 | 点击 8 月英国异常入口 | 进入“2026 年 8 月｜英国｜Actual vs Budget”归因工作区 | 通过（用户确认） | 通过（用户确认） | — |
| V05 | 展开德国仓后选择并清除五因素 | 选择“量”时公路排在空运前；选择“结构”时空运排在公路前；按钮、瀑布图和末列同步；清除后恢复五因素合计 | 通过（复验） | 通过（用户确认） | P01 |
| V06 | 展开发货仓到成本类别 | 可沿“德国仓 → 空运 → Carrier C → 成本类别”逐层展开；父子关系可辨认 | 通过（用户确认） | 通过（用户确认） | — |
| V07 | 打开并关闭数字证据 | 证据侧栏显示指标、期间、比较、筛选、分组、版本、算法和来源；关闭有效 | 通过（用户确认） | 通过（用户确认） | — |
| V08 | 从证据侧栏定位表格 | 自动切换到归因页、展开对应路径并定位目标行 | 通过（用户确认） | 通过（用户确认） | — |
| V09 | 查看 AI 管理分析 | “示例回答”、证据、建议状态和限制持续可见；不伪装为实时模型结果 | 通过（复验） | 通过（用户确认） | P02、P03 |
| V10 | 运行四步演示主线 | 可启动、前进、跨页、完成和退出；不锁定其他操作 | 通过（用户确认） | 通过（用户确认） | — |
| V11 | 使用返回入口及浏览器前进后退 | 工作区状态与 URL 中的 `view` 状态一致，无空白页或错误状态 | 通过（用户确认） | 通过（用户确认） | — |
| V12 | 全页视觉检查 | 公司与英国口径不混淆；关键文本、数字、按钮、图表和侧栏无截断、遮挡或不可操作 | 通过（用户确认） | 通过（用户确认） | — |

## 3. 问题记录

| 问题编号 | 等级 | 环境 | 步骤 | 实际结果 | 预期结果 | 修复状态 | 复验结论 |
|---|---|---|---|---|---|---|---|
| P01 | 轻微 | 1440px | V05 | 原验收步骤只观察发货仓根层；德国仓在所有因素下均高于法国仓，无法据此确认自动排序 | 指定存在顺序反转的第二层节点进行验证 | 已修正 V05 步骤 | 通过 |
| P02 | 主要 | 1440px | V09 | 订单粒度保护只显示英文内部代码；限制部分未显式显示未经证据支持的因果边界 | 显示中文订单粒度说明及“相关性不等于因果”标签 | 已修正生成源、契约与页面渲染 | 通过 |
| P03 | 主要 | 1440px | V09 | 页面刷新后数据已更新，但浏览器继续复用旧版 `app.js`，导致限制区域仍按旧逻辑渲染 | 静态资源引用包含版本号，修复后必须加载对应新版脚本 | 已为 CSS 与 JavaScript 增加版本号 | 通过 |

等级定义：

- 阻断：核心路径中断、业务口径错误、关键数字或证据不可核对；
- 主要：功能可继续，但关键交互、可读性或键盘操作明显受损；
- 轻微：不影响业务理解和核心操作的装饰性问题。

## 4. 验收结论

- 1440px：通过（V01—V12；P01—P03 均已修复并复验）；
- 1280px：通过（V01—V12）；
- 阻断问题：0；
- 主要问题：0 项未关闭，2 项已修复；
- 轻微问题：0 项未关闭，1 项已修复；
- 最终结论：通过；
- 查询与证据契约：已按 D-125 冻结为 V1.0。
