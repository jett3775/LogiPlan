# LogiPlan AI 决策记录

本文档记录业务模型冻结阶段已经明确确认的产品决策。历史基线保留在 `docs/product-baseline.md`，后续决定不直接覆盖基线中的原始假设。

## D-001：第一项目目标

- 状态：已确认
- 日期：2026-08-12
- 决策：LogiPlan AI 第一阶段以“求职作品集”为第一目标，不以可持续运营的通用产品为 MVP 验收目标。
- 理由：第一阶段重点证明业务建模、差异归因、滚动预测、AI 可靠性，以及产品设计、开发、测试、部署和展示的完整交付能力。
- 后续约束：MVP 范围、演示故事、数据设计、技术架构和公开部署均优先服务于岗位能力展示；登录、权限、多租户、企业系统接口和商业化运营能力不因“未来可能产品化”而提前进入第一版。
- 保留选项：完成作品集目标后，可以根据真实用户反馈评估是否继续产品化。

## D-002：首要展示岗位

- 状态：已确认
- 日期：2026-08-12
- 决策：以“物流财务 BP”为首要展示岗位，以“经营分析”为次要兼容方向；预算管理不是第一优先方向。
- 理由：物流财务 BP 与跨境物流成本、预算执行、量价结构拆解和业务纠偏的结合最紧密，能够形成比通用经营分析更明确的专业辨识度，同时保留向经营分析岗位迁移的能力证明。
- 后续约束：项目叙事、核心指标、差异归因、情景模拟、AI 管理分析和简历描述，应优先证明将财务结果连接到物流业务动因并推动改善的能力。
- 范围边界：仍需满足集团预算管理人员和物流业务负责人的基本使用需求，但不得因此削弱物流财务 BP 的主线。

## D-003：MVP 核心演示故事

- 状态：已确认
- 日期：2026-08-12
- 决策：以“2026 年 8 月英国市场物流成本显著超预算”为核心异常。促销带来订单量增长；为维持履约时效，德国仓发往英国的订单提高空运占比并更多使用高价承运商，量因素和结构因素共同推高成本；随后通过降低空运比例、调整承运商结构，展示全年滚动预测和情景纠偏。
- 展示主线：发现英国成本异常 → 下钻德国仓、运输方式与承运商 → 拆解量因素和结构因素 → 调整经营参数 → 查看全年 Forecast 变化 → 生成有证据的管理分析。
- 理由：该故事以“业务增长—履约选择—成本后果—财务纠偏”串联预算执行、差异归因、滚动预测、情景模拟和 AI 分析，能够集中证明物流财务 BP 的业务分析价值。
- 次要异常：燃油附加费和退货率可以提供辅助解释或用于其他测试场景，但不得抢占核心演示主线。
- 表述限制：演示数据可以按上述业务机制设计；产品分析仍须区分数据支持的贡献关系与未经证实的因果判断。

## D-004：底层事实数据粒度

- 状态：已确认
- 日期：2026-08-12
- 决策：MVP 底层事实数据采用“月 × 目的国 × 发货仓 × 承运商 × 运输方式”的聚合粒度，不保留订单级明细。
- 维度含义：“国家”统一指订单目的国；“仓库”指实际发货仓；每条事实记录只对应一个承运商和一种运输方式。
- 适用范围：Budget、Actual 和 Forecast 应使用可对齐的统一分析粒度；汇总层指标由该粒度向上聚合，不另外维护口径不同的汇总事实。
- 理由：该粒度支持公司、目的国、发货仓、承运商和运输方式的逐级分析，能够承载已确认的英国异常故事，同时避免订单级重量、退货和成本分摊带来的非必要复杂度。
- 范围边界：MVP 不提供订单级追踪、单票审计或订单明细下钻；如未来产品化确有需要，再评估增加订单级事实层。

## D-005：MVP 成本范围

- 状态：已确认
- 日期：2026-08-12
- 已确认范围：MVP 覆盖销售履约物流成本，包括运输成本、仓储操作成本、包装成本和退货成本。
- 固定成本：仓租、物流人工及系统费用作为固定成本单独展示，不与销售履约变动成本混合展示。
- 排除范围：采购入库运输暂不纳入 MVP。
- 相对基线的修正：包装成本正式进入成本范围；仓租、物流人工和系统费用不再作为完全排除项，而是进入独立固定成本视图。
- 细化记录：物流总成本分层见 D-006，运输成本分类见 D-007，运输计量与计费重量见 D-008 至 D-009，退货及仓储包装口径见 D-010 至 D-011，固定成本和履约事实归属见 D-012 至 D-013，跨境税费边界见 D-016。

## D-006：物流总成本的分层口径

- 状态：已确认
- 日期：2026-08-12
- 决策：顶层“物流总成本”包含销售履约变动成本和物流运营固定成本，但两者必须分层展示。
- 计算关系：`销售履约变动成本 = 运输成本 + 仓储操作成本 + 包装成本 + 退货成本`；`物流运营固定成本 = 仓租 + 物流人工 + 系统费用`；`物流总成本 = 销售履约变动成本 + 物流运营固定成本`。
- 归因边界：量、价、结构、效率归因只针对销售履约变动成本；物流总成本瀑布图将固定成本差异单独列示。
- 粒度约束：固定成本不强行分摊到承运商或运输方式。固定成本应保留在其真实可归属层级，汇总时再与销售履约变动成本合并。
- 指标约束：驾驶舱同时展示物流总成本、销售履约变动成本和物流运营固定成本，避免使用者把固定成本变化误判为履约效率变化。

## D-007：运输成本分类

- 状态：已确认
- 日期：2026-08-12
- 决策：运输成本拆分为基础运费、燃油附加费和异常配送费三类独立成本，计算关系为 `运输成本 = 基础运费 + 燃油附加费 + 异常配送费`。
- 基础运费：承运商正常报价产生的主体运费；偏远地区、旺季等正常报价附加费并入基础运费，MVP 不继续细分。
- 燃油附加费：独立计算和展示，差异归入价因素。
- 异常配送费：仅包括改址、二次派送、拒收、派送失败等异常履约产生的费用，差异归入效率因素。
- 归因规则：基础运费差异根据具体动因进入价因素或结构因素；燃油附加费进入价因素；异常配送费进入效率因素。
- 数据约束：三类成本在事实数据中分别保存，不以一个综合运输单价替代。

## D-008：订单量、包裹量与运输计量基础

- 状态：已确认
- 日期：2026-08-12
- 决策：订单量与包裹量分别记录，运输成本以包裹量和计费重量为计算基础，不直接以订单量计算。
- 计算关系：`包裹量 = 订单量 × 平均每单包裹数`；`总计费重量 = 包裹量 × 单包裹平均计费重量`；`基础运费 = 总计费重量 × 每公斤基础运价`。
- 数据要求：Budget、Actual 和 Forecast 均保存订单量、包裹量和总计费重量，或者保存能够无歧义推导这些值的对应假设。
- 指标分母：单均物流成本以订单量为分母；每公斤物流成本以总计费重量为分母。
- 业务意义：平均每单包裹数是独立业务动因，避免把拆包率变化错误解释为重量或运价变化。

## D-009：计费重量口径

- 状态：已确认
- 日期：2026-08-12
- 决策：MVP 直接保存承运商账单口径的总计费重量，不在月度聚合层根据平均实重和平均体积重重新计算。
- Actual：记录承运商账单中的总计费重量，同时记录总实重用于业务分析和计费重差异观察。
- Budget 与 Forecast：使用单包裹平均计费重量假设推导总计费重量。
- 范围边界：MVP 不模拟包裹级体积重比较、最低计费重量和重量进位规则。
- 展示要求：页面和数据字典须明确“计费重量”为承运商账单口径，不等同于实际物理重量。
- 理由：单包裹计费规则不可在月度汇总层通过平均值准确复现，直接保存账单计费重量能够避免聚合误差并支持成本核对。

## D-010：退货成本口径

- 状态：已确认
- 日期：2026-08-12
- 决策：MVP 采用简化退货模型：`退货订单量 = 当月履约订单量 × 当月退货率`；`退货包裹量 = 退货订单量`；`退货成本 = 退货包裹量 × 单包裹退货物流成本`。
- 成本范围：单包裹退货物流成本包含逆向运输费和退货处理费，不包含商品退款、商品减值和再次销售损益。
- 避免重复：正向仓储操作费不再包含退货处理费。
- 时间口径：退货在原履约月份确认，不建立跨月退货时滞或订单 cohort 模型。
- 演示假设：一笔退货订单对应一个退货包裹。该假设须在数据字典和已知限制中披露。
- 归因规则：退货率变化归入效率因素；单包裹退货物流成本变化归入价因素。

## D-011：仓储操作与包装成本的计费基础

- 状态：已确认
- 日期：2026-08-12
- 决策：仓储操作成本和包装成本采用不同计费基础，并作为两个独立成本字段保存。
- 计算关系：`仓储操作成本 = 订单量 × 单均仓储操作成本`；`包装成本 = 包裹量 × 单包裹包装成本`。
- 仓储操作成本范围：包含拣货、打包作业和出库处理的人工或服务费，不包含包装材料、仓租和退货处理费。
- 包装成本范围：只包含纸箱、填充物、胶带、标签等包装材料。
- 业务意义：拆包率变化会通过包裹量影响包装成本，但不会被错误混入仓储操作单价。

## D-012：固定成本事实粒度

- 状态：已确认
- 日期：2026-08-12
- 决策：固定成本采用“月 × 发货仓层级 × 固定成本类别”的独立事实粒度，不进入销售履约变动成本事实表。
- 固定成本类别：仓租、物流人工、系统费用。
- 归属规则：仓租和物流人工归属具体发货仓；无法合理归属单仓的总部系统费用记入“共享层”。
- 分摊限制：固定成本不分摊到目的国、承运商或运输方式。
- 展示边界：驾驶舱可以按月份和发货仓查看固定成本；目的国、承运商和运输方式下钻只展示销售履约变动成本。
- 数据理由：独立事实表能够避免固定成本在多维履约记录中被重复汇总或产生缺乏业务依据的虚假分摊。

## D-013：销售履约变动成本的事实归属

- 状态：已确认
- 日期：2026-08-12
- 决策：销售履约变动成本全部归属到“月 × 目的国 × 发货仓 × 承运商 × 运输方式”的统一履约事实行。
- 直接成本：基础运费、燃油附加费和异常配送费直接归属产生费用的事实行。
- 作业与包装：仓储操作费按事实行的订单量计算；包装费按事实行的包裹量计算。
- 退货成本：按原履约订单所在事实行计算和归属。
- 拆单规则：同一订单若拆分到不同承运商或运输方式，订单量按对应包裹占比分配到各事实行，形成可为小数的“等效订单量”。
- 汇总约束：各事实行的等效订单量汇总后必须等于公司层实际订单量，从而避免订单量、仓储操作费和退货成本重复计算。
- 展示要求：公司层展示实际订单总量；多维下钻中的订单量应标注为分摊后的等效订单量，避免被误解为订单明细计数。

## D-014：币种模型与汇率扩展边界

- 状态：已确认
- 日期：2026-08-12
- 决策：MVP 前端只展示单一管理报告币种，暂不实现原币切换、完整多币种交互或完整汇率差异分析。
- 报告币种：唯一管理报告币种确定为人民币 CNY；EUR、GBP 等仅作为原币保留，MVP 页面不提供币种切换。
- 数据保留：成本与 GMV 数据从第一版起保留原币金额、原币币种、适用汇率和报告币金额字段，使报告金额能够追溯到原币及折算依据。
- 归因扩展：在归因类别和结果结构中为“汇率因素”预留独立类别，不把未来的汇率差异混入价格因素。
- 当前边界：MVP 不实现完整多币种交互或原币页面切换，但根据 D-023 实际计算并展示汇率因素。
- 架构目的：未来增加原币展示、汇率版本和汇率差异归因时，可以扩展现有模型，而不必推翻成本事实结构。
- 汇率定义：统一表示为“1 单位原币 = 若干 CNY”。
- Budget 汇率：采用年度预算汇率，同一原币全年固定。
- Actual 汇率：采用对应月份的月平均会计汇率。
- Forecast 汇率：采用预测版本发布时确定的月度规划汇率，版本发布后不追溯修改。
- 数据实现约束：汇率保存在独立汇率表中；成本事实记录引用对应汇率记录并保存折算结果，不在事实数据中手工维护无来源的重复汇率。
- 版本要求：任一 Budget、Actual 或 Forecast 报告金额都必须能够追溯并按其原币金额和适用汇率复算。
- 汇率归因：完整规则见 D-023。

## D-015：数值精度与舍入规则

- 状态：已确认
- 日期：2026-08-12
- 决策：统一采用高精度计算、先汇总后舍入的规则，不在事实行计算过程中提前舍入。
- 内部精度：原币金额和 CNY 报告金额保留 4 位小数；汇率保留 6 位小数；单价保留 4 位小数；订单量、包裹量和重量保留 4 位小数；比例和费率保留 6 位小数。
- 计算精度解释：上述精度适用于业务输入、权威金额和标准报告字段；公式中间值、原子成本及归因结果必须使用足以容纳连乘结果的高精度十进制或精确有理数，不能在原子行截断为 4 位。
- 展示精度：明细金额显示 2 位小数；驾驶舱可以万元缩写显示，但悬浮值保留 2 位小数；差异率显示 2 位小数。
- 汇总规则：使用未舍入值完成跨事实行汇总和归因计算，最后才按展示精度舍入。
- 归因尾差：归因结果允许在展示层增加“舍入调整”项吸收不可避免的小额显示尾差；调整前的高精度归因结果必须严格等于总成本差异。
- 一致性要求：国家、仓库、承运商、运输方式与公司层汇总必须从同一组高精度事实值聚合，禁止各层单独生成已舍入金额。

## D-016：关税、进口增值税与清关费用边界

- 状态：已确认
- 日期：2026-08-12
- 决策：关税和进口增值税不计入物流总成本；MVP 不建立税率、税基、抵扣或税务归因模型。
- 正常清关费用：正常清关、报关和经纪服务费计入基础运费。
- 异常清关费用：因资料错误、申报异常或滞留产生的额外清关费用计入异常配送费。
- 业务理由：跨境履约服务成本与税款性质不同，分开处理能够避免运输单价、异常配送费和物流成本率失真。

## D-017：差异归因的总体方法

- 状态：已确认
- 日期：2026-08-13
- 决策：销售履约变动成本采用固定顺序连环替代法作为面向用户的正式归因方法，替代顺序固定为“量 → 结构 → 效率 → 价 → 汇率”。
- 正式结果：页面、管理分析和归因瀑布图均使用连环替代法结果；所有层级和成本类别必须使用同一替代顺序。
- 后台参考：同时计算 Shapley 分解作为顺序敏感性校验，但默认不作为面向用户的主结果。
- 偏差提示：任一因素同时满足以下两个条件时，提示归因对替代顺序敏感：`|连环替代归因 - Shapley 归因| ÷ Σ|Shapley 各因素归因| ≥ 10%`，且差异金额不低于 Budget 销售履约变动成本的 `0.5%`。
- 提示语义：提示只说明因素之间的差异分配受替代顺序影响；Budget 与 Actual 的总成本差异不受归因方法影响。
- 可加总要求：连环替代各因素金额加总必须严格等于销售履约变动成本总差异；汇率功能未启用时，汇率因素为“不适用”，而不是用虚构零值参与替代。
- 方法限制：固定顺序会把因素交互影响分配给替代顺序中相应的后续因素，因此结果是统一管理口径，不是唯一可能的经济归因。
- 披露要求：方法说明页必须列明固定替代顺序、因素定义、恒等式和顺序敏感性限制。

## D-018：量因素定义

- 状态：已确认
- 日期：2026-08-13
- 决策：量因素只表示履约订单总量变化，不把目的国、仓库、承运商、运输方式、包裹或重量的变化重复定义为量因素。
- 替代步骤：从 Budget 场景出发，仅将履约订单总量替换为 Actual 总量；其余输入保持 Budget 值。
- 保持不变：各维度分布占比、平均每单包裹数、单包裹平均计费重量、退货率、计费异常事件率、所有单价和汇率均保持 Budget 口径。
- 成本效果：订单总量变化按 Budget 业务结构和单位动因同比例影响运输、仓储操作、包装及退货成本。
- 业务问题：量因素只回答“如果只有履约订单总量变化，销售履约变动成本会变化多少”。
- 互斥边界：实际订单在国家、仓库、承运商和运输方式间的重新分布归入结构因素；拆包、重量、退货和异常履约等变化按后续已确认规则归类。

## D-019：结构因素定义

- 状态：已确认
- 日期：2026-08-13
- 决策：结构因素同时包含业务组合变化和重量结构变化。
- 替代步骤：完成量因素替代后，将等效订单量在“目的国 × 发货仓 × 承运商 × 运输方式”组合之间的占比，以及各组合的单包裹平均计费重量替换为 Actual。
- 保持不变：平均每单包裹数、退货率、计费异常事件率、所有单价和汇率仍保持 Budget 口径。
- 业务组合：国家、仓库、承运商和运输方式之间的订单流向变化均属于结构因素，包括空运比例和高价承运商使用占比变化。
- 重量结构：在订单总量不变的条件下，单包裹平均计费重量变化归入结构因素，不归入量因素。
- 业务问题：结构因素回答“在订单总量已确定的情况下，订单流向、运输组合和包裹重量特征变化带来了多少成本差异”。

## D-020：效率因素定义

- 状态：已确认
- 日期：2026-08-13
- 正式名称：页面使用“效率与异常因素”；数据结构和计算接口可以使用稳定代码 `efficiency`。
- 决策：效率因素只包含能够通过确定性公式直接改变成本的平均每单包裹数、退货率和计费异常事件率。
- 替代步骤：完成结构因素替代后，将上述三个参数替换为 Actual；各类单价和汇率仍保持 Budget 口径。
- 成本映射：平均每单包裹数影响运输计费重量和包装成本；退货率影响退货包裹量及退货成本；计费异常事件率影响计费异常事件数及异常配送费。
- 准时履约率：作为诊断和证据指标展示，不直接转换成成本金额，也不直接进入归因公式。
- 归因条件：只有异常履约已产生可核验费用时，才通过计费异常事件率进入效率因素；不得根据相关性虚构成本影响。
- 业务问题：效率与异常因素回答“在量和业务结构已确定后，拆包、退货及异常配送表现变化带来了多少成本差异”。

## D-021：燃油附加费计费方式

- 状态：已确认
- 日期：2026-08-13
- 决策：不假设所有国际承运商采用同一种燃油附加费计费方式；计费方式由对应承运商、运输方式和线路价格条件决定。
- 支持类型：`percentage_of_base_freight` 表示基础运费乘附加费率；`per_chargeable_kg` 表示计费重量乘每公斤附加费；`per_package` 表示包裹量乘单包裹附加费。
- MVP 使用范围：演示数据实际使用“按基础运费比例”和“按计费重量”两种方式；数据模型预留“按包裹”，但不要求演示数据必须覆盖。
- 数据要求：每个适用价格记录必须同时保存计费方式和对应费率或单位附加费，不得只保存脱离计费基础的综合燃油费率。
- 归因规则：承运商、运输方式或线路组合变化带来的燃油成本变化归入结构因素；在计费方式不变的对应组合内，费率或单位附加费变化归入价因素。
- 方法边界：若 Budget 与 Actual 的同一业务组合发生计费方式变更，该变化视为合同价格条件变化，归入价因素。

## D-022：价因素定义

- 状态：已确认
- 日期：2026-08-13
- 决策：价因素包含单位价格、附加费率以及合同计费条件变化。
- 替代步骤：完成效率因素替代后，将每公斤基础运价、燃油附加费计费方式及对应费率或单位附加费、单均仓储操作成本、单包裹包装成本、单包裹退货物流成本、平均每计费异常事件成本由 Budget 替换为 Actual。
- 保持不变：汇率仍保持 Budget 口径，留到连环替代的最后一步。
- 结构边界：承运商、运输方式和线路的业务占比变化归入结构因素；同一业务组合中的单价、费率或计费方式变化归入价因素。
- 计费方式变化：同一业务组合若改变燃油附加费计费方式，应把完整合同条件变化归入价因素，不直接比较计量基础不同的费率数值。
- 业务问题：价因素回答“在实际量、结构和效率已经确定后，单位价格及合同计费条件变化带来了多少成本差异”。

## D-023：汇率因素定义

- 状态：已确认
- 日期：2026-08-13
- 决策：汇率因素在 MVP 中实际参与后台确定性归因计算并显示结果，不只保留空字段。
- 替代步骤：价因素先使用 Budget 汇率计算原币价格和合同条件变化的影响；连环替代最后一步仅将汇率替换为对比场景适用汇率，由此产生的 CNY 成本变化归入汇率因素。
- Actual 对比：Budget 对 Actual 时，使用 Actual 对应月份的月平均会计汇率作为最后替代值。
- Forecast 对比：Budget 对 Forecast 时，使用该 Forecast 版本冻结的月度规划汇率作为最后替代值。
- CNY 项目：原币为 CNY 的成本项目不发生折算，其汇率因素为零。
- 展示边界：页面仍以 CNY 为唯一报告币种，不提供原币切换；归因结果可以独立显示汇率影响金额。
- 恒等式要求：量、结构、效率、价、汇率五项高精度归因金额之和必须严格等于销售履约变动成本的 CNY 总差异。

## D-024：固定成本差异与扩展接口

- 状态：已确认当前 MVP 口径，保留后续扩展
- 日期：2026-08-13
- 当前决策：固定成本差异不进入销售履约变动成本的“量、结构、效率、价、汇率”五因素模型；MVP 按发货仓或共享层、固定成本类别展示 Budget、Actual 或 Forecast、差异金额和差异率。
- 总差异关系：`物流总成本差异 = 销售履约变动成本五因素差异 + 固定成本差异`。
- 固定成本类别：仓租、物流人工、系统费用分别展示，不把固定成本变化命名为量、结构或效率因素。
- 币种处理：非 CNY 固定成本在后台保留原币金额和汇率影响；页面默认显示固定成本净差异，必要时可以展开汇率影响。
- 扩展接口：固定成本事实和计算接口须支持以后按成本类别挂接独立驱动模型，例如仓租对应面积与单位租金、物流人工对应人数或工时与单位人工成本、系统费用对应账号量或用量与单位费率。
- 兼容要求：后续增加固定成本驱动分析时，应扩展固定成本子模型，不修改销售履约变动成本五因素定义，也不破坏物流总成本差异恒等式。
- 当前限制：MVP 尚无可靠的面积、人数、工时、账号量或系统用量数据，因此不进行缺乏依据的固定成本驱动分摊。

## D-025：归因计算粒度与下钻一致性

- 状态：已确认
- 日期：2026-08-13
- 决策：五因素归因在原子履约事实组合上计算一次，再向目的国、发货仓、承运商和运输方式逐级汇总；筛选下钻时不重新定义或重新计算因素。
- 计算底表：使用“月份 × 目的国 × 发货仓 × 承运商 × 运输方式”的 Budget 与 Actual 或 Forecast 业务键并集，任一场景缺失的组合补零数量。
- 结果保存：保存每个原子组合的量、结构、效率、价和汇率贡献金额，以及比较场景和归因版本标识。
- 汇总约束：任一维度的子项归因金额之和必须严格等于上级归因金额；所有层级使用同一组高精度原子结果。
- 量因素解释：下钻层的量因素表示公司总订单量变化按 Budget 结构分配到该组合的影响，不重新解释为该国家、仓库或承运商自身总量变化。
- 审计目标：同一归因版本在驾驶舱、瀑布图、下钻表和 AI 证据中必须返回一致金额。

## D-026：新增或退出组合的价格完整性

- 状态：已确认
- 日期：2026-08-13
- 决策：所有可能产生 Budget、Actual 或 Forecast 业务量的原子组合，都必须能从价格主数据取得对应期间的有效价格条件；缺少价格时归因校验失败并停止计算。
- 反事实价格：新增组合虽然在 Budget 中业务量为零，仍须存在预算期间有效的合同价格条件；退出组合虽然在 Actual 中业务量为零，仍须存在实际期间有效的合同价格条件。
- 数据来源：基础运价、燃油附加费计费方式及费率、仓储操作单价、包装单价、退货单价和异常配送单价均从版本化价格主数据读取。
- 禁止处理：不得把缺失价格填零，不使用市场均价或层级平均价补全，也不把缺价造成的差异归入结构、价格或“未分配差异”。
- 前置校验：数据生成、导入或版本发布时检查业务量组合与有效价格的覆盖关系；任何有业务量但无有效价格的记录必须返回明确的数据质量错误。
- 审计要求：每个归因事实组合能够追溯到 Budget 与对比场景所引用的价格版本和有效期。

## D-027：基准效率参数的业务归属粒度

- 状态：已确认
- 日期：2026-08-13
- 决策：基准效率参数按其真实业务驱动维度分别配置，不在每个原子履约组合中机械复制一整套参数。
- 平均每单包裹数：按“月份 × 目的国 × 发货仓”配置，反映拆包和仓内操作策略。
- 退货率：按“月份 × 目的国”配置，反映市场退货特征；MVP 暂无商品品类维度，因此不继续细分。
- 计费异常事件率：按“月份 × 目的国 × 发货仓 × 承运商 × 运输方式”配置，反映具体履约线路表现。
- 版本规则：Budget 使用版本化的预算运营标准；Actual 使用实际观测参数；Forecast 使用预测版本冻结的运营假设。
- 零预算组合：即使某原子组合的 Budget 业务量为零，也应通过上述维度关系取得 Budget 基准效率参数，使新增组合能够区分结构影响与效率影响。
- 数据校验：归因所需参数无法按规定维度关联时，必须阻止归因计算，不得临时使用公司平均值或当期 Actual 倒推补全。
- 模型意义：参数归属维度用于表达业务驱动关系，不等同于履约事实表粒度；计算时再将适用参数关联到原子事实组合。

## D-028：虚构 Budget 运营标准的来源治理

- 状态：已确认
- 日期：2026-08-13
- 决策：MVP 的 Budget 基准效率参数采用明确披露的版本化规划假设，不伪装为真实企业历史统计值。
- 来源标识：参数记录使用 `source_type = demo_planning_assumption`，并保存参数版本、有效月份、制定依据和备注。
- 设计规则：数值在合理业务区间内人工设计，常规月份保持可解释的平滑变化；异常月份通过 Actual 偏离 Budget 标准形成，不反向修改预算基准以配合结果。
- 异常留痕：对核心演示异常涉及的参数，记录设计理由、预期业务机制和预期归因方向，但不以目标差异金额反推并掩盖参数来源。
- 披露要求：README、数据字典和已知限制必须明确说明企业、交易和运营参数均为虚构演示数据。
- 扩展兼容：未来真实数据可以增加 `historical_average`、`contract_target`、`management_target` 等来源类型，而无需修改参数主结构。
- 审计要求：每个用于归因或预测的 Budget 效率参数均能追溯到对应假设版本及制定说明。

## D-029：跨月归因的计算边界

- 状态：已确认
- 日期：2026-08-13
- 决策：月份是不可跨越的归因计算边界；累计、日期范围和全年归因均由月度原子归因结果求和，不先汇总成年度平均参数再计算。
- 单月分析：每个月独立执行 Budget 对 Actual 或 Budget 对 Forecast 的五因素连环替代。
- 年初至今：汇总所有已结账月份的原子归因结果。
- 全年最新预测：已结账月份使用 Actual，未来月份使用指定 Forecast 版本，分别按月相对 Budget 归因后汇总。
- 任意日期范围：只对范围内的月度原子结果求和，保持与单月瀑布图一致。
- 禁止方法：不得使用年度平均运价、平均汇率、平均效率或平均业务结构重新计算年度归因。
- 一致性要求：任一累计层级的五因素金额必须等于组成月份同一因素金额之和，并严格加总到对应期间销售履约变动成本总差异。

## D-030：Actual 与 Forecast 的切换月份

- 状态：已确认
- 日期：2026-08-13
- 决策：全年最新预测由数据版本中显式配置的“最近结账月份”决定 Actual 与 Forecast 的切换，不根据事实记录是否存在自动推断。
- 组合规则：月份不晚于最近结账月份时使用 Actual；月份晚于最近结账月份时使用指定 Forecast 版本。
- 演示时点：MVP 核心演示设为 2026 年 8 月结账后，2026 年 1—8 月使用 Actual，9—12 月使用 Forecast。
- 完整性校验：所有已结账月份必须存在完整 Actual；指定 Forecast 版本必须覆盖所有未结账月份。任一缺失时，全年最新预测发布失败。
- 试算数据：未结账月份即使存在临时或试算 Actual，也不得进入正式全年最新预测。
- 版本要求：最近结账月份、Actual 数据版本和 Forecast 版本共同标识一个可复现的全年最新预测快照。

## D-031：Forecast 生成方法

- 状态：已确认
- 日期：2026-08-13
- 决策：未来月份 Forecast 采用规则驱动的版本化参数覆盖模型，不使用统计模型或机器学习自动外推。
- 基线：以所有未结账月份的 Budget 业务假设作为完整起点。
- 覆盖内容：Forecast 版本记录相对 Budget 的参数覆盖，包括订单量或调整率、业务组合占比、平均每单包裹数、平均计费重量、退货率、计费异常事件率、价格条件和规划汇率。
- 继承规则：未被覆盖的参数继承 Budget，不复制为来源不明的独立数值。
- 计算方式：应用覆盖后，使用同一确定性成本模型重新计算每个未来月份的销售履约变动成本；固定成本使用其独立 Forecast 子模型或假设。
- 版本治理：Forecast 草稿可以修改；发布后冻结，后续调整必须生成新版本，从而保证全年最新预测和归因结果可复现。
- 范围边界：MVP 不提供统计趋势、机器学习预测或自动选择预测模型。

## D-032：Forecast 参数覆盖粒度

- 状态：已确认
- 日期：2026-08-13
- 决策：Forecast 参数只允许在其规范业务归属粒度修改和落库，不支持同一参数在任意层级同时覆盖及自动继承。
- 订单总量：按“月份 × 目的国”维护。
- 业务组合占比：在目的国订单总量下，维护发货仓、承运商和运输方式的分配关系。
- 平均计费重量：按“月份 × 目的国 × 发货仓 × 承运商 × 运输方式”维护。
- 效率参数：平均每单包裹数、退货率和计费异常事件率沿用 D-027 确定的归属粒度。
- 价格与汇率：不作为事实行自由输入，分别引用版本化价格主数据和汇率主数据。
- 上层操作：公司级、国家级或批量情景控件只负责生成规范粒度的覆盖项，不形成另一套运行时优先级规则。
- 保存校验：各目的国下的业务组合占比必须合计为 100%；所有引用组合必须通过价格、效率参数和汇率完整性校验。
- 一致性目标：任何 Forecast 版本都能从 Budget 加一组无歧义覆盖项确定性重建。

## D-033：预置情景模板

- 状态：已确认
- 日期：2026-08-13
- 决策：Base Case、Growth Case 和 Cost Saving Case 定义为可编辑的预置情景模板，不是三个写死的预测算法。
- Base Case：以当前正式 Forecast 版本为起点，不额外增加情景覆盖。
- Growth Case：预置提高未来月份订单量，并默认保持当前业务组合和效率假设；用户可以继续修改。
- Cost Saving Case：承接英国异常纠偏故事，预置降低英国空运占比、减少高价承运商份额，并改善拆包率或计费异常事件率。
- 编辑规则：用户应用模板后可以调整允许的情景参数，系统按规范粒度生成覆盖项并运行确定性模型。
- 快照边界：每次运行生成独立情景快照，不覆盖正式 Forecast 版本。
- 发布边界：情景结果仅用于模拟，不自动发布为新 Forecast；若需转为正式预测，必须通过独立的版本发布流程。
- 演示目的：模板提供可快速理解的起始方案，同时保留参数透明度和进一步试算能力。

## D-034：情景服务目标与极端假设

- 状态：已确认
- 日期：2026-08-13
- 决策：情景分析中的准时履约率和计费异常事件率目标采用可覆盖的软约束，不作为所有情景都不得违反的系统硬规则。
- 默认目标：Cost Saving 等预置模板可以默认带入准时履约率下限和计费异常事件率上限，用于寻找成本与服务的平衡方案。
- 用户自主权：自定义情景允许修改或关闭服务目标，也允许输入极端假设进行压力测试。
- 违反处理：违反服务目标时仍运行确定性模型并展示结果，标记为“服务风险”或“压力测试”，不作为计算错误阻止运行。
- AI 边界：AI 可以分析违反目标的情景，但必须明确披露服务代价；不得把该结果无条件表述为可直接执行的行动方案。
- 推荐标签：“可推荐”或“不可推荐”属于基于当前目标的评估标签，不是限制用户模拟权限的规则。
- 硬校验边界：系统硬校验只处理数学与数据有效性，例如占比合计 100%、数值非负、价格与参数主数据完整。

## D-035：情景预计准时履约率

- 状态：已确认
- 日期：2026-08-13
- 决策：情景预计准时履约率使用版本化线路服务参数按情景包裹占比加权计算，不从成本、运价或运输方式名称直接推断。
- 计算关系：`情景预计准时履约率 = Σ（情景包裹占比 × 线路预计准时履约率）`。
- 线路粒度：目的国 × 发货仓 × 承运商 × 运输方式。
- 参数来源：Budget 使用预算服务目标；Actual 使用实际观测值；Forecast 与情景使用对应版本的规划服务参数。
- 联动规则：用户调整发货仓、承运商或运输方式结构后，系统以新的包裹占比重新加权预计准时履约率。
- 范围边界：MVP 不建立运输时效统计或机器学习预测模型，也不根据价格高低假设服务水平。
- 追溯要求：情景服务结果必须能够下钻到各线路服务参数及其包裹权重。

## D-036：计费异常事件率与准时履约率的关系

- 状态：已确认
- 日期：2026-08-13
- 决策：计费异常事件率与准时履约率作为两个独立参数维护和校验，二者不互为补数，也不得通过 `1 - 准时履约率` 相互推导。
- 准时履约率：衡量包裹是否在承诺时间内完成配送，用于服务诊断、情景软约束和风险提示，不直接转换为成本。
- 计费异常事件率：衡量每单位包裹发生的计费异常事件频率，进入异常配送成本计算和效率因素归因。
- 业务边界：包裹可能迟到但不产生异常费用，也可能发生额外配送后仍按时完成，因此两个指标必须分别观测和规划。
- Forecast 与情景：分别保存线路预计准时履约率和计费异常事件率，用户调整业务结构后按各自规则重新计算或加权。
- AI 表述：不得把准时履约率下降直接写成已发生异常成本，除非存在计费异常事件率和费用证据。

## D-037：AI 交互范围

- 状态：已确认
- 日期：2026-08-13
- 决策：AI 交互采用“固定问题模板为主、受控自由问答为辅”的组合。
- 固定入口：首屏提供“发生了什么、为什么发生、未来会怎样、调整参数后会怎样”四类入口，并生成明确的期间、版本、维度筛选和情景参数。
- 自由问答边界：用户可以自由输入问题，但系统只回答已定义指标、维度、归因结果、Forecast 版本和情景快照能够支持的问题。
- 计算职责：数字、排名、差异金额、Forecast 和情景结果全部由确定性查询及计算服务返回；模型只组织语言、解释证据和提出受证据约束的建议。
- 数据访问：模型不直接读取原始事实表，不生成或执行自由 SQL。
- 数据不足：超出数据范围时明确返回缺少的字段、维度或计算能力，不自行补充数字或外部事实。
- 产品权衡：固定入口保证核心演示可复现，自由问答提供自然交互，但不得突破业务模型和证据边界。

## D-038：自由问题的结构化查询意图

- 状态：已确认
- 日期：2026-08-13
- 决策：自由问题先由模型解析为受限的结构化查询意图，经过程序校验后，再由确定性查询和计算服务执行。
- 核心字段：`question_type`、`comparison`、`period`、`forecast_or_scenario_version`、`metrics`、`group_by`、`filters`、`top_n`。
- 模型职责：识别用户意图和候选参数，不生成 SQL、不直接计算结果、不绕过字段及枚举限制。
- 程序校验：验证指标、维度、筛选值、期间、比较场景、版本和 Top N 范围；无效请求不得进入查询服务。
- 歧义处理：存在多个合法解释且无法从明确上下文唯一确定时，要求用户澄清，不静默选择会改变结论的解释。
- 查询输出：确定性服务返回结构化结果、口径信息和证据 ID。
- 生成约束：模型只能基于查询输出生成“结论、证据、影响、建议、限制”五部分，不得补写未返回的数字或事实。
- 评估要求：固定评估集同时覆盖意图字段准确性、查询结果准确性和最终回答证据一致性。

## D-039：AI 查询的页面上下文

- 状态：已确认
- 日期：2026-08-13
- 决策：自由问答可以使用当前页面可见且已明确选择的上下文补全缺省参数，但必须在回答中显式披露实际采用范围。
- 可用上下文：当前期间、比较口径、Forecast 或情景版本，以及页面当前筛选的国家、发货仓、承运商和运输方式。
- 披露方式：AI 回答开头显示实际查询范围，例如“范围：2026 年 8 月｜英国｜Actual vs Budget”。
- 优先级：用户在当前问题中明确给出的条件优先于页面上下文。
- 必须澄清：缺失参数会显著改变结论，且当前页面没有唯一明确值时，必须要求用户澄清。
- 禁止继承：不得沿用当前界面不可见的早期会话筛选或已失效版本状态。
- 审计要求：实际执行的结构化意图及其上下文来源与回答一并保存，便于复现。

## D-040：AI 行动建议的权限边界

- 状态：已确认
- 日期：2026-08-13
- 决策：AI 只能提出可验证的候选行动，不得直接宣称措施必然可执行或一定产生未经模型验证的收益。
- 证据关联：每项建议必须关联具体负向驱动因素、查询结果和证据 ID。
- 情景流程：涉及可调参数的建议先形成情景参数草案，由用户确认后运行确定性模型；成本节省和服务影响只引用实际情景计算结果。
- 可行性限制：缺少合同容量、人员约束、仓库产能或其他实施条件数据时，明确标记“执行可行性未验证”。
- 权限限制：AI 不自动修改或发布 Forecast 版本，不自动应用情景，也不代表管理层批准行动。
- 输出结构：建议动作、量化影响、服务风险、实施前提、数据限制。
- 因果边界：AI 可以根据归因结果描述已计算的贡献关系，但不得把相关性或模拟结果写成已经验证的经营因果或实施承诺。

## D-041：AI 数字证据引用

- 状态：已确认
- 日期：2026-08-13
- 决策：AI 回答中的每个关键数字必须绑定可点击的结构化证据对象，不以回答级笼统来源替代数字级追溯。
- 证据字段：至少包括 `evidence_id`、`metric`、`value`、`unit`、`period`、`comparison`、`filters`、`group_by`、`version`、`calculation_method`、`source_result_id`。
- 点击内容：显示实际查询范围和指标口径、Budget/Actual/Forecast 原值、差异金额或差异率、归因因素、对应下钻结果、数据版本及计算方法。
- 引用规则：关键数字逐项引用对应证据；纯文字建议可以引用支撑该判断的一组证据，不要求每句话机械添加独立标记。
- 生成限制：模型只能引用查询服务实际返回的 `evidence_id`，不得生成不存在或无法解析的证据标识。
- 评估要求：固定评估集检查回答数字、证据对象数值、筛选范围和版本是否一致。

## D-042：AI 回答与证据快照

- 状态：已确认
- 日期：2026-08-13
- 决策：AI 回答使用的结构化查询和证据随回答保存为不可变快照，不随后续 Actual、Forecast、价格或参数版本更新而变化。
- 快照内容：结构化查询意图、确定性查询结果、证据对象、Actual/Forecast/情景版本、价格与汇率版本、计算版本和生成时间。
- 历史提示：查看旧回答时，如当前正式数据或计算版本已变化，显示“基于历史版本”的提示。
- 重新运行：允许用户按当前版本重新执行同一问题，生成新的回答和证据快照；不覆盖原回答。
- 引用一致性：旧回答中的数字始终解析到生成时的证据快照，禁止点击后动态切换为最新数据。
- MVP 保存范围：第一版只要求在当前浏览会话内保存回答和证据快照，不要求跨用户长期历史、账户同步或审计归档。

## D-043：总体技术路线

- 状态：已确认
- 日期：2026-08-13
- 决策：MVP 采用“纯代码确定性后端 + 模型 API”的技术路线，Dify 不承担核心计算、查询或 AI 编排。
- 组件边界：业务 API 统一调用指标与归因计算模块、Forecast 与情景模块、受限查询服务和 AI 编排模块；AI 编排模块再调用模型 API。
- 代码职责：预算、Actual、Forecast、情景、五因素归因、证据生成、意图校验和版本复现全部由可测试的程序代码控制。
- 模型职责：模型只负责受限意图解析、基于结构化结果生成解释，以及形成需用户确认的情景参数草案。
- Dify 边界：Dify 不进入 MVP 的生产运行链路；如后续使用，仅作为提示词或工作流实验工具，不成为数字结果的权威来源。
- 选择理由：该路线更适合实现类型约束、自动测试、恒等式校验、不可变证据快照和版本审计，并直接证明确定性计算与生成式 AI 的职责分层。

## D-044：应用形态与代码仓库

- 状态：已确认
- 日期：2026-08-13
- 决策：MVP 采用 TypeScript 单仓库全栈 Web 应用，不分别维护独立前端和后端项目，不拆分微服务或独立 Python 服务。
- 前端职责：驾驶舱、差异归因下钻、Forecast、情景模拟和 AI 交互界面。
- 服务端职责：业务 API、受限查询执行、模型调用、API 密钥保护、访问限流和证据快照生成。
- 共享契约：前后端共用类型定义、枚举和运行时校验模式，避免查询意图、业务指标及证据结构发生口径漂移。
- 计算模块：确定性成本、Forecast、情景和归因计算保持无 Web 框架依赖，可以独立运行单元测试与恒等式测试。
- 部署边界：第一版作为一个应用部署；只有后续出现独立扩缩容、运行时或团队边界需求时才评估拆分服务。
- 后续选择：具体全栈框架见 D-045；数据存储和公开托管平台由后续技术方案确定。

## D-045：Web 框架与技术选择授权

- 状态：已确认
- 日期：2026-08-13
- 决策：Web 框架采用 Next.js App Router、React 和 TypeScript，服务端运行时采用 Node.js。
- 服务端接口：业务 API、受限查询和模型代理使用 Route Handlers；模型 API 密钥只保存在服务端环境变量中。
- 前端边界：交互式图表、筛选器、Forecast 编辑器和情景模拟器使用客户端组件；其余页面优先使用服务端能力。
- 解耦要求：成本、Forecast、情景和归因计算保存在无 Next.js 依赖的独立领域模块中，不写入页面组件或 Route Handler。
- 技术授权：后续纯技术实现选择默认采用记录时的推荐方案，不再逐项要求业务确认。
- 授权边界：产品行为、公开访问策略、费用上限、安全暴露、数据口径，以及具有显著不可逆平台锁定的选择，仍须单独确认。

## D-046：公开演示的匿名 AI 访问

- 状态：已确认
- 日期：2026-08-13
- 决策：公开演示允许匿名访客直接调用真实模型，无需注册；同时设置调用限额和只读示例回退。
- 无限制功能：驾驶舱、归因下钻、Forecast 查看和本地确定性情景计算不消耗 AI 调用额度。
- 受限功能：自由问答、管理分析生成和自然语言意图解析等真实模型调用受匿名限流保护。
- 超限回退：匿名额度用尽后仍可查看固定示例问题、历史生成的示例回答及其证据快照，不影响核心业务流程展示。
- 安全要求：模型 API Key 仅存放于服务端环境变量，不发送到浏览器；服务端同时执行请求校验、调用限流和输出证据校验。
- 用户提示：页面在调用前显示剩余 AI 次数和额度状态，不在提交后才隐藏限制。
- 演示模式：本地或受保护的项目展示模式可以使用独立额度策略，但不得通过公开前端参数绕过限制。
- 额度与熔断：具体规则见 D-047。

## D-047：匿名 AI 额度、模型路由与费用熔断

- 状态：已确认
- 日期：2026-08-13
- 匿名额度：同一 IP 与浏览器组合在滚动 24 小时内最多完成 3 次完整 AI 请求。
- 全站限流：全站每小时最多完成 20 次真实模型请求；超过后使用固定示例回退。
- 月度预算：模型 API 月度费用上限为人民币 100 元，按服务端记录的实际 token 用量和当前价格配置估算。
- 一级熔断：预计月度费用达到预算的 80% 后，匿名请求全部切换到低成本模型并缩短最大输出。
- 二级熔断：预计月度费用达到预算的 100% 后，关闭匿名真实模型调用，只提供固定示例回答和证据。
- 展示模式：受保护的项目展示模式可以绕过单个匿名访客次数限制，但仍受全局安全限流和月度费用上限约束。
- 模型路由：默认使用 `gpt-5.6-luna` 处理结构化意图解析，使用 `gpt-5.6-terra` 生成管理分析；一级熔断后统一使用低成本模型。模型名称和价格均通过服务端配置管理，不写死在业务代码中。
- 调用限制：每次调用设置输入与输出 token 上限，记录模型、token 用量、估算费用、限流键和熔断状态。
- 配置要求：次数、时间窗口、模型、token 上限、人民币预算及告警比例均为可调整的服务端配置。

## D-048：履约 GMV 与物流成本率

- 状态：已确认
- 日期：2026-08-13
- 决策：公司层物流成本率以履约 GMV 为分母，计算关系为 `公司物流总成本率 = 物流总成本 ÷ 履约 GMV`；目的国层采用 D-068 定义的履约变动成本率。
- 履约 GMV：已进入销售履约流程的订单商品金额，扣除取消或未履约订单商品金额及 VAT，不包含向消费者收取的运费。
- 退货处理：后续发生退货时不冲减原履约 GMV，避免退货异常同时增加成本分子并缩小分母而造成双重放大。
- 币种规则：原币 GMV 按对应 Budget、Actual 或 Forecast 已确认的汇率规则折算为 CNY。
- 指标语义：该指标回答“为履约既定商品价值投入了多少物流成本”，不是会计收入费用率或利润率。
- 展示命名：公司层可简称“物流成本率”，完整名称为“公司物流总成本率”；未来若引入净销售额和盈利分析，应新增独立指标，不修改本口径。

## D-049：单均成本与每公斤成本指标

- 状态：已确认
- 日期：2026-08-13
- 主指标：`单均履约变动成本 = 销售履约变动成本 ÷ 订单量`；`每公斤运输成本 = 运输成本 ÷ 总计费重量`。
- 展开指标：`单均物流总成本 = 物流总成本 ÷ 订单量`，用于观察包含固定成本后的整体单位负担。
- 取消指标：不再使用含义模糊的“每公斤物流总成本”，即不把仓租、人工、系统费用和仓储操作成本统一除以计费重量。
- 展示层级：驾驶舱主卡展示单均履约变动成本和每公斤运输成本；单均物流总成本放在成本分层或展开区域。
- 分母规则：订单量使用公司层实际订单总量或相应比较场景订单总量；总计费重量使用承运商账单口径或 Budget/Forecast 对应假设口径。
- 解释边界：每公斤运输成本用于观察运输计费效率与价格综合结果，不代表全部物流资源的重量效率。

## D-050：成本差异的符号与展示语义

- 状态：已确认
- 日期：2026-08-13
- 计算关系：`成本差异金额 = Actual 或 Forecast - Budget`；`成本差异率 = 成本差异金额 ÷ Budget`。
- 成本语义：正值表示超预算和不利，负值表示低于预算和有利。
- 展示规则：同时显示带正负号的金额或比例，以及“不利”或“有利”标签。
- 零分母：Budget 为零时，差异率显示“不适用”，只显示差异金额。
- 非成本指标：订单量、履约 GMV、准时履约率等只显示增减方向，不机械套用成本指标的有利或不利语义。
- 颜色规则：颜色由指标业务语义决定，不按数字正负号全局统一映射；例如成本正差为不利色，GMV 正差不应使用同一不利色。
- 一致性要求：驾驶舱、归因瀑布图、下钻表和 AI 文本使用相同符号定义。

## D-051：Top 成本异常的排序规则

- 状态：已确认
- 日期：2026-08-13
- 默认排序：Top 目的国、发货仓、承运商和运输方式只对正向成本差异按不利差异金额降序排列。
- 辅助字段：同时展示差异金额、差异率和占公司总不利差异的贡献占比。
- 低基数处理：Budget 金额较小的项目不因差异率极高自动成为首要异常；差异率排序作为用户可选视图，并设置最低 Budget 金额门槛。
- 有利差异：负向成本差异单独列为“节省贡献”，不与不利异常混合排序。
- 管理语义：默认排序用于优先识别金额影响最大的风险，差异率只补充说明偏离程度。
- 一致性要求：驾驶舱 Top 榜、下钻列表和 AI 的“最大异常”结论使用相同默认排序口径。

## D-052：差异贡献占比的分母

- 状态：已确认
- 日期：2026-08-13
- 决策：不利差异与节省贡献分别计算贡献占比，不以公司净差异作为单项贡献分母。
- 不利贡献：`某正向差异 ÷ 所有正向差异之和`，不利项目占比合计 100%。
- 节省贡献：`某负向差异绝对值 ÷ 所有负向差异绝对值之和`，节省项目占比合计 100%。
- 净差异：`公司净差异 = 不利差异总额 - 节省总额`，只用于展示最终预算偏差，不作为贡献率分母。
- 零分母：某一分池没有项目时，该池贡献占比显示“不适用”。
- 展示要求：页面明确区分“不利贡献占比”和“节省贡献占比”，不得统一称为未限定方向的“贡献率”。

## D-053：预算执行驾驶舱的默认时间视图

- 状态：已确认
- 日期：2026-08-13
- 默认主视图：全年最新预测对年度 Budget，优先回答全年物流成本风险。
- 截止说明：明确显示“2026 年 1—8 月 Actual + 9—12 月 Forecast”，并标注最近结账月份和 Forecast 版本。
- 核心异常入口：突出显示 2026 年 8 月英国为最大单月不利异常，并提供一键下钻。
- 次级切换：支持“2026 年 8 月 Actual vs Budget”“2026 年 1—8 月累计 Actual vs Budget”“全年最新预测 vs Budget”三种明确比较范围。
- 口径披露：所有图表和 AI 上下文始终显示当前期间、对比类型和版本，不把 Actual 与 Forecast 混称为实际数据。
- 产品目的：首屏同时呈现全年管理风险和核心异常线索，使预算管理视角与物流财务 BP 分析主线能够衔接。

## D-054：驾驶舱核心 KPI 层级

- 状态：已确认
- 日期：2026-08-13
- 决策：预算执行驾驶舱首屏只保留 6 个核心 KPI，其余经营指标放入展开和异常诊断区域。
- 核心 KPI：物流总成本、销售履约变动成本、净差异金额与差异率、单均履约变动成本、每公斤运输成本、物流成本率。
- 固定成本展示：物流运营固定成本作为物流总成本卡片的分层明细，不单独占用核心 KPI 卡。
- 展开指标：订单量、包裹量、总计费重量、履约 GMV、空运比例、退货率、每百包裹计费异常事件数和准时履约率。
- 信息层级：首屏先回答成本规模、预算偏差和单位效率，再通过异常诊断区域解释业务动因。
- 口径一致：核心 KPI 使用 D-006、D-048 至 D-050 已确认的成本、分母和差异定义。

## D-055：履约线路白名单

- 状态：已确认
- 日期：2026-08-13
- 决策：有效业务组合通过版本化履约线路白名单管理，不默认允许目的国、发货仓、承运商和运输方式的全部笛卡尔组合。
- 线路定义：一条履约线路由“目的国 × 发货仓 × 承运商 × 运输方式”唯一标识，并记录启用状态、有效开始日期和有效结束日期。
- 业务约束：Budget、Actual 和 Forecast 只能对对应期间有效的履约线路产生业务量。
- 参数关联：价格条件、计费异常事件率和准时履约率均关联有效履约线路及适用版本。
- 新增线路：必须先完成价格、服务和所需效率参数配置并通过完整性校验，之后才能进入 Forecast、Budget 或 Actual。
- 停用线路：保留历史事实和版本记录，但有效期结束后不得新增业务量。
- 校验要求：发现无效线路业务量时阻止数据版本发布及归因计算，不自动改配到其他线路。

## D-056：运输方式的业务定义

- 状态：已确认
- 日期：2026-08-13
- 正式名称：页面完整名称使用“跨境运输方案”，可简称“运输方式”。
- 决策：运输方式表示从发货仓到目的国消费者的端到端履约线路中占主导的跨境主干方案，不表示单独某一运输段或末端派送方式。
- 类型：`AIR` 表示跨境主干以空运完成；`ROAD` 表示跨境主干以公路运输完成。
- 末端配送：末端派送由该履约线路的承运商负责，不另设末端运输方式维度。
- 成本范围：基础运价覆盖线路合同约定的端到端运输服务及正常清关服务；异常清关和异常配送费用仍按 D-007 与 D-016 单独归类。
- 模型边界：MVP 不把干线、清关和末端派送拆成多个运输事实层，也不建立多段成本分摊模型。
- 一致性要求：线路白名单、价格主数据、服务参数、Forecast 结构和归因下钻均使用同一运输方式定义。

## D-057：仓网与英国履约路径

- 状态：已确认
- 日期：2026-08-13
- 决策：英国不设置本地仓，英国订单主要由德国仓跨境直发履约。
- 仓网角色：德国仓服务德国和英国；法国仓主要服务法国，也可以作为德国或英国的少量备援发货仓。
- 核心异常：2026 年 8 月英国促销订单集中由德国仓履约；为满足时效，部分原计划公路运输改为空运，并提高高价承运商使用占比。
- 线路性质：“德国仓 → 英国”属于跨境直发履约线路，使用 D-056 定义的端到端跨境运输方案。
- 维度命名：页面、数据字典和 AI 回答统一把仓库称为“发货仓”，把国家称为“目的国”。
- 分析意义：英国无本地仓使其成本与服务更容易受到发货仓、运输方式和承运商结构变化影响，支撑核心演示故事。

## D-058：虚构承运商角色

- 状态：已确认
- 日期：2026-08-13
- Carrier A：主力低成本承运商，以公路运输为主，价格较低，英国线路时效表现一般。
- Carrier B：均衡型承运商，同时支持公路与空运，成本和服务水平居中。
- Carrier C：高服务高价格承运商，以空运和紧急履约为主，准时履约率较高。
- 核心异常：2026 年 8 月英国业务中 Carrier C 占比明显上升，与空运比例增加共同形成不利结构影响。
- 情景方向：Cost Saving Case 将部分 Carrier C 空运量转回 Carrier B 或 Carrier A，并根据线路服务参数显示准时履约风险。
- 数据约束：各承运商角色必须通过价格、有效线路和服务参数体现，不只作为文字标签。
- 披露要求：Carrier A、Carrier B 和 Carrier C 均为虚构承运商，角色与数据只用于产品演示。

## D-059：英国促销的模型边界

- 状态：已确认
- 日期：2026-08-13
- 决策：核心故事中的英国促销作为月份级显式业务背景注释，不新增促销活动、SKU 或商品品类维度，也不进行促销增量归因。
- 数据记录：可以保存类似 `event_note = UK promotion` 的业务事件说明，并关联 2026 年 8 月英国范围。
- 可证事实：订单量增长由 Budget 与 Actual 订单数据证明；促销是业务输入背景，不由物流成本模型反向推断。
- 表述边界：产品可以表述“促销期间订单量增长”，但不得仅凭当前数据断言全部增长由促销造成。
- 下钻边界：MVP 不支持按营销活动、SKU 或商品品类分析，不建立自然增长基线或营销增量评估模型。
- AI 证据：回答须区分业务背景注释和确定性计算结果，不把注释当作量化因果证据。

## D-060：管理决策动机的证据边界

- 状态：已确认
- 日期：2026-08-13
- 决策：“为维持履约时效而提高空运及 Carrier C 占比”作为 2026 年 8 月英国范围的显式管理决策注释保存，不由成本和服务数据自动推导。
- 数据记录：保存可版本化的 `management_action_note`，包含适用期间、目的国、决策内容和演示数据来源标识。
- 归因职责：结构归因只证明空运和 Carrier C 占比变化贡献了多少成本差异。
- 服务证据：线路服务参数和准时履约率只证明相关线路具备不同服务特征，以及业务组合变化后的服务结果。
- 因果表述：只有存在管理决策注释时，AI 才能把“维持时效”表述为已知决策动机；没有注释时，只能说明结构变化与较高服务水平同时出现。
- 披露要求：该管理记录与其他演示数据一样属于虚构业务设定，不冒充真实企业决策资料。

## D-061：第一阶段业务模型冻结的验收门槛

- 状态：已确认
- 日期：2026-08-13
- 决策：第一阶段只有在决策记录、数据字典、归因方法文档和小样本测试夹具全部完成并通过验证后，才视为业务模型冻结。
- 决策记录：`docs/decisions.md` 完整保存已确认的产品、业务口径、归因、Forecast、AI 和发布边界。
- 数据字典：`docs/data-dictionary.md` 定义事实表、维度表、主数据、字段含义、业务归属粒度、版本和完整性规则。
- 归因方法：`docs/variance-methodology.md` 定义五因素连环替代、Shapley 后台参考、顺序敏感性提示、恒等式和边界场景。
- 验证夹具：使用只覆盖少量月份和线路的数据，验证成本公式、币种折算、归因严格可加总、跨月汇总和下钻一致性。
- 阶段闸门：上述四项未完成并确认前，不生成 2026 年全年演示数据，不开发正式页面。
- 范围说明：小样本测试夹具属于业务模型验证材料，不属于全年演示数据生成。
- 验收结果：2026-08-13 已完成 `docs/data-dictionary.md`、`docs/variance-methodology.md`、小样本夹具及参考验证器；自动检查结果记录于 `docs/business-model-validation.md`，第一阶段业务模型 V1.0 冻结通过。

## D-062：公开演示的数据写入边界

- 状态：已确认
- 日期：2026-08-13
- 决策：公开演示采用业务数据只读、访客试算不持久化的模式。
- 只读范围：Budget、Actual、正式 Forecast、价格、汇率、线路、服务参数及其他主数据均为项目发布的只读版本。
- 会话试算：匿名访客可以修改情景参数并运行确定性模型，但输入、结果和 AI 回答只保存在当前浏览会话。
- 生命周期：刷新、清除会话或会话结束后，匿名访客的情景改动可以丢弃，不形成共享业务记录。
- 权限边界：匿名访客不能发布 Forecast、修改基线版本、修改主数据或影响其他访客的演示状态。
- 固定内容：预置情景、固定示例问题和示例 AI 证据快照随项目版本发布。
- 后续扩展：如需持久化 Forecast 编辑或作品展示专用管理能力，应放入受保护模式，不改变匿名公开演示的只读边界。

## D-063：Budget、Actual、Forecast 的分层数据模型

- 状态：已确认
- 日期：2026-08-13
- 决策：采用“来源层分开、分析层统一”的分层模型，不把 Budget、Actual 和 Forecast 直接混合作为同一业务录入表。
- Budget 来源层：保存版本化预算业务假设、运营标准及其价格和汇率引用。
- Actual 来源层：保存实际运营量、账单计费重量、实际服务指标、实际成本及其来源记录。
- Forecast 来源层：保存 Forecast 版本和相对 Budget 的规范粒度参数覆盖。
- 统一分析层：由各来源层经过校验和确定性计算，生成相同原子粒度、相同指标契约的 `fulfillment_scenario_fact`，供驾驶舱、归因和 AI 查询使用。
- 场景区分：统一分析事实通过 `scenario_type`、场景版本、来源版本和计算版本明确区分 Budget、Actual 与 Forecast。
- 可重建要求：统一分析事实是派生结果，不作为三类业务数据的原始录入来源；必须能够从对应来源版本确定性重建。
- 独立事实：固定成本和归因结果继续使用独立事实结构，不反写为履约场景原始字段。
- 模型意义：统一的是分析契约和可比粒度，不是 Budget、Actual、Forecast 的业务生成流程。

## D-064：Actual 权威成本与模型重算核对

- 状态：已确认
- 日期：2026-08-13
- 决策：Actual 成本以已结算账单或会计确认金额为权威值，同时保留确定性公式重算值用于核对。
- 计算关系：`模型重算成本 = 实际业务量 × 实际价格条件`；`结算差异 = Actual 权威成本 - 模型重算成本`。
- 数据要求：已结账月份按各成本类别保存权威金额、模型重算金额、结算差异和核对状态。
- MVP 要求：虚构演示数据的结算差异为零，避免用无法解释的调整项制造核心异常。
- 真实扩展：舍入、补扣款或账单调整必须映射到明确成本类别和来源记录，不得静默混入五因素归因。
- 发布校验：存在超过精度容差且无法解释的结算差异时，阻止正式 Actual 版本和归因结果发布。
- 模型意义：Actual 保持财务权威性，同时能够验证数量、价格、费率、汇率和成本公式是否一致。

## D-065：Actual 预提与追补版本

- 状态：已确认
- 日期：2026-08-13
- 决策：已结账月份尚未收到完整账单的物流成本允许以已审批会计预提金额进入 Actual。
- 金额来源：每项 Actual 成本标记 `amount_source = invoice` 或 `amount_source = accrual`，并保留对应来源记录。
- 预提依据：预提金额应通过实际业务量和有效合同价格生成或核验，不得作为无依据的平衡数。
- 后续追补：收到正式发票后生成新的 Actual 修订版本，以发票替换相应预提；旧版本不覆盖，继续支持历史 Forecast 和 AI 证据复现。
- 展示要求：驾驶舱显示当前范围内预提金额及其占 Actual 成本的比例。
- AI 提示：预提占比较高时，回答须说明 Actual 金额仍可能随发票结算调整。
- 演示边界：核心演示可以包含少量预提以体现现实月结，但 2026 年 8 月英国主异常不得依赖预提误差形成。

## D-066：年度 Budget 的冻结与更正

- 状态：已确认
- 日期：2026-08-13
- 决策：年度 Budget 在批准发布后冻结，Forecast 和 Actual 均不得回写或静默覆盖正式 Budget。
- 正式基线：2026 年原始批准 Budget 是默认预算执行和绩效分析基线。
- Forecast 边界：滚动 Forecast 只生成新的 Forecast 版本，不改变 Budget 假设或预算金额。
- Actual 边界：实际结果和差异分析不得用于事后修改预算，以消除已发生偏差。
- 错误更正：发现预算录入或计算错误时，只能发布带更正原因、审批说明和版本关联的预算更正版本。
- 默认比较：即使存在更正版本，页面默认仍使用原始批准 Budget；只有用户明确切换时才使用预算更正版本。
- 披露要求：驾驶舱、归因结果、Forecast、情景和 AI 回答均显示所引用的 Budget 版本。

## D-067：履约 GMV 粒度与订单级扩展路径

- 状态：已确认
- 日期：2026-08-13
- 当前决策：履约 GMV 保存在独立的“月份 × 目的国 × 场景版本”事实结构，不复制或按等效订单量分摊到发货仓、承运商和运输方式线路。
- 汇总规则：公司层 GMV由目的国 GMV 汇总；线路事实与 GMV 事实通过月份、目的国和场景版本等一致维度进行允许层级的组合分析。
- 禁止语义：不得把分摊 GMV 表述为某发货仓、承运商或运输方式拥有的真实收入。
- 稳定维度：月份、目的国、币种和场景版本使用稳定标识，使当前聚合事实与未来订单级模型能够共用维度。
- 后续扩展：预留 `sales_order`、`package` 和 `order_package_bridge` 三类结构；未来通过真实订单—包裹关系连接订单收入与包裹履约成本。
- 聚合兼容：引入订单级数据后，当前月度 GMV 和履约事实可以转为订单及包裹事实的聚合视图或发布快照，无需修改已确认指标定义。
- MVP 边界：当前虚构月度数据不伪造订单 ID 或订单—包裹明细。
- 成本率协调：公司层与目的国层的具体指标口径见 D-068。

## D-068：公司与目的国的分层成本率

- 状态：已确认
- 日期：2026-08-13
- 公司层：`公司物流总成本率 = 物流总成本 ÷ 公司履约 GMV`，分子包含销售履约变动成本和物流运营固定成本。
- 目的国层：`目的国履约变动成本率 = 目的国销售履约变动成本 ÷ 目的国履约 GMV`，不包含固定成本。
- 更细下钻：发货仓、承运商和运输方式层不展示成本率，只展示成本、业务量、重量、效率和服务指标。
- 分摊限制：不得为了生成目的国物流总成本率而把仓租、物流人工或共享系统费用分摊到目的国。
- 命名要求：页面、数据字典和 AI 回答使用完整指标名称；不得把公司总成本率与目的国履约变动成本率混称为同一口径。
- 业务理由：固定成本没有目的国归属依据，分层指标能够保持真实业务归属和可审计性。

## D-069：Actual 的月份归属原则

- 状态：已确认
- 日期：2026-08-13
- 决策：Actual 的业务量、履约 GMV 和销售履约变动成本采用履约月份权责口径，不按发票收到日期或会计录入日期归属。
- 订单、GMV、包裹、重量和运输成本：统一按 D-070 定义的承运商接收事件月份归属。
- 仓储与包装：按对应出库操作月份归属。
- 退货成本：沿用 D-010 的简化口径，归入原履约月份。
- 发票晚到：通过预提计入对应履约月份，正式发票到达后修订该月份 Actual 版本，不把成本推迟到发票月份。
- 固定成本：按仓租、人工或系统服务实际覆盖月份归属。
- 配比目标：同一分析月份中的业务量、GMV 和对应履约成本保持一致，避免单均成本和预算差异因入账时点产生人为错配。

## D-070：月末截止事件与时区

- 状态：已确认
- 日期：2026-08-13
- 决策：月度履约归属统一以发货仓当地时间的“承运商接收包裹”事件为截止点。
- 时区：德国仓使用 `Europe/Berlin`，法国仓使用 `Europe/Paris`，并遵循相应夏令时规则。
- 月份归属：包裹交接承运商的当地日期决定月份；等效订单量、履约 GMV、包裹量、重量及对应销售履约变动成本按同一月份归属。
- MVP 简化：不生成跨月拆包订单，同一订单的所有包裹必须在同一月份完成承运商接收。
- 未来扩展：进入订单级模型后，通过订单—包裹关联处理跨月拆包，不修改包裹事件的原始时间。
- 存储要求：来源记录保留事件时间和时区；月度分析事实保存标准月份键。
- 展示边界：页面以管理月份展示，不要求普通用户处理时区换算细节，但数据字典必须披露截止规则。

## D-071：Actual 准时履约率与成熟度

- 状态：已确认
- 日期：2026-08-13
- 计算关系：`准时履约率 = 承诺送达日期前完成妥投的包裹数 ÷ 已到承诺截止日期的包裹数`。
- 承诺日期：由包裹交运日期和对应履约线路的有效服务标准确定。
- 准时判断：提前或按期妥投计为准时；超期妥投以及到期仍未妥投计为不准时。
- 未到期包裹：尚未到承诺截止日期的在途包裹不进入准时率分母。
- Cohort 口径：按承运商接收月份形成发运 cohort，使服务表现与同月份履约成本保持关联。
- 成熟度：`服务指标成熟度 = 已到承诺截止日期包裹数 ÷ 该发运 cohort 全部包裹数`。
- 展示与 AI：准时履约率同时显示成熟度；成熟度不足时，AI 必须说明结果尚未完全成熟，不得表述为最终服务表现。
- 范围边界：准时履约率继续作为诊断和情景软约束，不直接转换为成本金额。

## D-072：计费异常事件与异常配送费的 MVP 模型

- 状态：已确认
- 日期：2026-08-13
- 决策：MVP 不按异常事件类型拆分，使用计费异常事件总数、计费异常事件率和平均每计费异常事件成本的聚合模型。
- 计算关系：`计费异常事件率 = 计费异常事件总数 ÷ 包裹量`；`平均每计费异常事件成本 = 异常配送费总额 ÷ 计费异常事件总数`；`异常配送费 = 包裹量 × 计费异常事件率 × 平均每计费异常事件成本`。
- 事件范围：只统计实际产生额外费用的改址、二次派送、拒收、派送失败和异常清关等事件；一般延迟但未产生费用时不计入。
- 多次事件：同一包裹可以产生多次计费异常事件，因此计费异常事件率理论上可以超过 100%；页面优先显示“每百包裹计费异常事件数”。
- 归因规则：计费异常事件率变化归入效率因素；平均每计费异常事件成本变化归入价因素。
- MVP 展示：展示事件总数、每百包裹事件数、平均每事件成本和异常配送费，不提供事件类型下钻。
- 已知限制：异常类型组合变化会反映在平均每事件成本中，并暂时归入价因素；该限制须在归因方法文档中披露。
- 后续扩展：预留按“月份 × 履约线路 × 异常事件类型”保存事件数和单位费用的明细结构；启用后当前聚合指标由明细汇总生成，不修改上层成本公式。

## D-073：固定成本 Forecast 方法

- 状态：已确认
- 日期：2026-08-13
- 决策：未来月份固定成本 Forecast 默认继承 Budget，只有存在明确最新信息时，才按“月份 × 发货仓或共享层 × 固定成本类别”录入金额覆盖。
- 默认规则：仓租、物流人工和系统费用不随订单量自动同比例变化。
- 覆盖场景：已知合同涨价、招聘或人员计划、系统费用调整等信息可以形成明确金额覆盖。
- 版本治理：每项覆盖保存原因、来源、Forecast 版本和适用月份；Forecast 发布后冻结。
- 全年组合：全年固定成本最新预测由已结账月份 Actual 加未来月份固定成本 Forecast 组成。
- 币种规则：非 CNY 固定成本继续保留原币和规划汇率，并按已确认汇率口径折算。
- 后续扩展：引入面积、人数、工时、账号量或系统用量后，由 D-024 预留的固定成本驱动子模型生成或解释覆盖金额，不修改当前总成本结构。

## D-074：价格主数据的业务归属粒度

- 状态：已确认
- 日期：2026-08-13
- 决策：各类价格按其真实业务驱动粒度分别配置，再在确定性计算时关联到原子履约事实行；不把全部单价机械复制到每条线路。
- 基础运价与燃油附加费：按“月份 × 履约线路 × 价格版本”配置。
- 仓储操作单价：按“月份 × 发货仓 × 价格版本”配置。
- 包装单价：按“月份 × 发货仓 × 价格版本”配置。
- 单包裹退货物流成本：按“月份 × 目的国 × 发货仓 × 价格版本”配置。
- 平均每计费异常事件成本：按“月份 × 履约线路 × 价格版本”配置。
- 引用规则：多个事实组合适用同一价格时引用同一价格记录，不重复录入；所有价格记录保存原币、币种、有效期和版本。
- 完整性要求：计算所需价格无法按规定维度唯一关联时，阻止场景版本发布和归因计算。

## D-075：基础运费的有效单价模型

- 状态：已确认
- 日期：2026-08-13
- 决策：MVP 不模拟承运商的重量阶梯价、最低收费和重量进位，使用线路月度有效每公斤基础运价计算基础运费。
- 计算关系：`基础运费 = 总计费重量 × 有效每公斤基础运价`。
- Budget 与 Forecast：保存对应版本的规划有效基础运价。
- Actual：保存权威基础运费，并通过 `权威基础运费 ÷ 总计费重量` 计算实际有效基础运价，用于模型重算、核对和价格归因。
- 指标命名：页面和数据字典使用“有效基础运价”，不表述为承运商合同牌价。
- 业务含义：有效运价可以综合反映阶梯、最低收费及重量进位后的月度结果，但 MVP 不声称复现包裹级计费过程。
- 后续扩展：引入包裹级数据后可以增加合同阶梯与最低收费引擎；月度有效基础运价继续作为汇总核对指标。

## D-076：退货物流成本的有效单价模型

- 状态：已确认
- 日期：2026-08-13
- 决策：MVP 使用聚合的有效单包裹退货物流成本，不进一步拆分逆向运输费和仓内退货处理费。
- 成本范围：有效单价仍包含逆向运输和退货处理，但不包含商品退款、商品减值和再次销售损益。
- Budget 与 Forecast：按“月份 × 目的国 × 发货仓 × 价格版本”配置有效单包裹退货物流成本。
- Actual：通过 `权威退货成本 ÷ 退货包裹量` 计算实际有效单价，并用于模型核对和价因素归因。
- 归因含义：价因素反映组合有效单价变化，MVP 不进一步判断变化来自逆向承运商还是仓库退货处理费。
- 展示披露：页面和方法文档说明该指标是组合有效单价，不将其表述为单一供应商报价。
- 后续扩展：未来可以拆为逆向运输成本和仓内退货处理成本，当前聚合总额继续作为汇总核对值。

## D-077：平均计费重量的月度季节性

- 状态：已确认
- 日期：2026-08-13
- 决策：Budget 与 Forecast 的单包裹平均计费重量允许按月配置可解释的季节性，不要求全年固定不变。
- 配置粒度：沿用“月份 × 目的国 × 发货仓 × 承运商 × 运输方式”的原子履约线路粒度。
- 设计规则：常规月份保持平滑；明显变化应反映商品组合或包装体积等已知业务背景，并保存 `assumption_note`。
- 范围边界：MVP 不为解释重量季节性新增商品品类或 SKU 维度。
- Actual 口径：`实际单包裹平均计费重量 = 账单总计费重量 ÷ 实际包裹量`。
- 归因规则：平均计费重量变化继续归入结构因素。
- 核心故事约束：2026 年 8 月英国主异常不以重量突增为主要驱动，重量影响保持次要，避免稀释订单增长和运输组合变化主线。

## D-078：情景业务组合占比的归一化

- 状态：已确认
- 日期：2026-08-13
- 默认交互：用户修改一条或多条履约线路占比时，锁定这些新占比，剩余未锁定线路按原占比同比缩放，使目的国下的总占比保持 100%。
- 多项锁定：允许锁定多条线路；锁定占比合计超过 100% 时阻止保存并明确提示。
- 手动模式：用户可以切换为“手动分配”，逐条输入全部线路占比，并自行满足合计 100%。
- 透明度：运行确定性模型前展示调整后的完整线路分配表、变化前后占比及受影响参数。
- 禁止规则：不得把剩余差额静默分配给某一默认承运商、仓库或运输方式。
- 数据校验：只允许在有效履约线路白名单内分配业务量，并通过价格、效率和服务参数完整性校验。

## D-079：情景订单量调整的基准

- 状态：已确认
- 日期：2026-08-13
- 正式名称：页面使用“订单量调整”，不使用含义不明确的“订单增速”。
- 计算关系：`情景订单量 = 当前正式 Forecast 订单量 × (1 + 情景调整率)`。
- 适用范围：用户选择目的国和未来月份范围后应用调整，也可以直接输入目标订单量。
- 基准版本：情景快照保存所引用的正式 Forecast 版本和调整方式，基准 Forecast 更新后旧情景不自动漂移。
- Base Case：订单量调整率为 0，沿用当前正式 Forecast。
- 口径边界：`+10%` 表示相对当前 Forecast 增加 10%，不表示同比或环比增长。
- 数据限制：MVP 没有 2025 年历史数据，因此不提供同比订单增速分析。

## D-080：多月订单量调整方式

- 状态：已确认
- 日期：2026-08-13
- 决策：对多个月份应用统一订单量调整率时，每个月都相对自身正式 Forecast 基准独立计算，不逐月复利。
- 计算示例：`9 月情景订单量 = 9 月 Forecast × (1 + 调整率)`；10 月及后续月份分别使用各自 Forecast 作为基数。
- 默认模式：“统一调整”对所选月份使用同一比例。
- 逐月覆盖：用户可以展开并为不同月份输入不同调整率或目标订单量。
- Growth Case：默认对 2026 年 9—12 月统一增加一个比例，但不以上一月情景量作为下一月基数。
- 展示要求：运行前后展示各月 Forecast 基准订单量、调整率和情景订单量。

## D-081：情景结果的比较基准

- 状态：已确认
- 日期：2026-08-13
- 主要比较：`情景增量影响 = 情景结果 - 当前正式 Forecast`，用于衡量用户调整本身增加或减少的成本与服务影响。
- 次级比较：`情景对预算差异 = 情景全年结果 - 年度 Budget`，用于判断调整后是否仍存在全年预算风险。
- 展示顺序：主卡优先显示相对正式 Forecast 的成本增减和服务变化；次级区域显示相对 Budget 的全年差异。
- 版本固定：情景快照保存所引用的正式 Forecast 版本和 Budget 版本，基准更新后旧情景不自动重算。
- 指标范围：成本、订单量、单位成本和服务指标均显示 Forecast 基准值、情景值及变化。
- Actual 边界：情景不把已结账月份单独改写或与 Actual 作为可调整基准比较；全年情景仍包含不可变的已结账 Actual。

## D-082：未来情景与历史复盘的边界

- 状态：已确认
- 日期：2026-08-13
- 决策：正式情景只允许调整未结账月份，不允许修改已结账 Actual。
- 演示范围：在 2026 年 8 月结账时点，正式情景默认只能调整 2026 年 9—12 月。
- 已结账月份：1—8 月 Actual 只读，并作为全年情景结果的固定组成部分。
- 历史复盘：探索“8 月当时如果采用另一方案”的问题属于独立反事实模式，可以重算模拟结果，但不得写入 Actual 或正式 Forecast。
- MVP 范围：数据与接口可预留历史复盘模式，第一版不要求实现其交互页面。
- 标签要求：历史复盘结果必须明确标记为反事实模拟，不得与已发生 Actual 混合。
- AI 边界：不得把历史反事实节省表述为实际已经实现的节省。

## D-083：Cost Saving Case 的实施节奏

- 状态：已确认
- 日期：2026-08-13
- 默认节奏：2026 年 9 月维持当前正式 Forecast，作为方案准备期；10 月部分调整空运比例和 Carrier C 份额；11—12 月达到目标业务结构。
- 展示方式：情景显示逐月实施路径及各月参数变化，不只使用一个全年平均比例。
- 用户自主权：默认节奏属于预置模板，用户可以修改生效月份、调整幅度，或设置 9 月立即生效等极端假设。
- 计算规则：每月参数按 D-032 的规范粒度生成覆盖项，并独立相对对应月份 Forecast 计算。
- AI 表述：默认结果称为“分阶段方案”；缺少合同容量、切换周期或仓库产能证据时，不声称实施条件已经确认。
- 业务理由：8 月结账后 9 月通常已经进入执行期，承运商和运输结构调整分阶段生效更符合经营纠偏现实。

## D-084：Cost Saving Case 的供应商范围与实施成本

- 状态：已确认
- 日期：2026-08-13
- 决策：Cost Saving Case 只在 Carrier A、Carrier B、Carrier C 现有承运商及已启用履约线路之间重新分配业务份额，不引入新供应商。
- 合同与流程假设：预置情景不新增合同，不产生违约金、系统接入、培训或重大流程改造。
- 一次性成本：在该限定情景下标记为“不适用”，不保存为数值零，也不进入情景成本计算。
- 结果命名：情景结果使用“物流运行成本影响”或“预计运行成本节省”，不称为包含全部实施投入的项目净收益。
- 可行性提示：现有承运商的合同容量、最低承诺量和实际操作可行性仍可能未验证，AI 与页面必须披露。
- 后续扩展：未来允许新增承运商或重大线路改造时，再启用一次性实施成本、实施周期和投资回收期模型。

## D-085：承运商容量约束的阶段边界

- 状态：已确认
- 日期：2026-08-13
- 决策：MVP 不配置或校验承运商线路容量上限；在有效履约线路白名单内，情景计算暂时假设现有承运商具备足够承载能力。
- 情景行为：Cost Saving Case 和自定义情景不会因业务量超过规划容量而警告或阻止计算。
- 表述限制：容量无限是 MVP 简化假设，不代表供应商已经确认承接；页面和 AI 必须标记“承运商承接能力未验证”。
- 推荐边界：AI 不得仅根据数学节省结果断言份额调整可以直接执行。
- 二期范围：规划容量、容量利用率、最低承诺量、阶梯返利和合同配额统一留待第二阶段或后续产品化扩展。
- 扩展兼容：后续可按“月份 × 履约线路”增加容量参数及软约束，不修改当前履约线路或情景覆盖主结构。

## D-086：关系型数据存储方案

- 状态：已按技术授权确定
- 日期：2026-08-13
- 决策：MVP 的持久化业务数据采用 PostgreSQL 关系型模型，保持可移植性，不依赖某一托管厂商的专有数据结构。
- 分层实现：Budget、Actual、Forecast 来源数据分别建模；统一分析事实、固定成本事实、归因结果、价格、汇率、线路及服务参数使用独立关系结构。
- 数值类型：金额、单价、汇率、数量和费率使用精确十进制类型，不使用二进制浮点数保存财务结果。
- 发布数据：正式演示数据以版本化只读发布快照进入运行环境，并能够从来源层和计算版本重建。
- 会话数据：匿名情景输入、情景结果、AI 回答和证据快照默认只保存在浏览会话，不写入共享业务数据库。
- 运行数据：匿名限流、用量和费用熔断状态使用服务端持久化计数，但与业务事实分离。
- 范围边界：MVP 不建设企业级数据仓库、流式处理、主从集群或多租户数据库架构。

## D-087：MVP 语言策略

- 状态：已确认
- 日期：2026-08-13
- 决策：MVP 采用中文产品界面和中文 AI 回答，不实现完整的中英文界面切换。
- 业务术语：Budget、Actual、Forecast、GMV 等跨境财务与预算常用英文术语可以在中文界面中保留，并由词汇表统一含义。
- 工程语言：数据字段、API 契约、代码标识和测试代码使用英文，面向用户的标签使用中文规范名称。
- 项目说明：README 提供中英文项目摘要；完整产品说明、数据字典和演示流程第一版以中文为主。
- 评估边界：MVP 只维护中文 AI 固定评估集，不重复建设英文问答评估集。
- 后续扩展：目标岗位明确需要英文展示时，再增加英文语言包、英文管理分析模板和独立英文评估集。
- 范围理由：完整双语会显著增加文案、提示词、证据格式和测试维护量，对当前国内物流财务 BP 求职主线的增益有限。

## D-088：固定 AI 评估集与通过门槛

- 状态：已确认
- 日期：2026-08-13
- 评估规模：建立 20 个固定中文评估问题，并绑定可复现的数据、Forecast、情景和计算版本。
- 分类配额：“发生了什么”4 题；“为什么发生”6 题；“未来会怎样”4 题；“情景调整后会怎样”4 题；“数据不足与越界保护”2 题。
- 总体门槛：至少 18/20 题整体通过。
- 数字要求：所有程序计算数字必须与标准答案精确一致，不允许以语义接近替代数字正确性。
- 证据要求：关键数字的证据范围、版本、指标口径和数值必须完全一致。
- 硬性失败：出现任何编造数字、虚假证据，或把相关性写成确定因果时，整体评估失败，不受总分影响。
- 越界门槛：两道数据不足与越界保护题必须全部通过，否则整体评估失败。
- 分层记录：分别记录结构化意图解析、确定性查询结果和最终回答质量，避免只按文字流畅度判断通过。
- 版本治理：模型、提示词、查询契约或业务数据版本变化后重新运行评估，并保留历史结果。

## D-089：业务决策访谈结束

- 状态：已确认
- 日期：2026-08-13
- 决策：原始“尚未解决的决策树”及其依赖分支已经达到共享理解，结束逐项访谈，进入第一阶段冻结产物编制。
- 后续顺序：数据字典 → 归因方法文档 → 小样本测试夹具与恒等式验证 → 跨文档一致性检查。
- 阶段限制：冻结产物和验证完成前，继续禁止生成 2026 年全年演示数据或开发正式页面。

## D-090：单订单单发货仓假设

- 状态：已确认
- 日期：2026-08-13
- 决策：MVP 假设每个订单只由一个发货仓履约，不允许同一订单跨德国仓和法国仓拆分。
- Actual 来源：按“月份 × 目的国 × 发货仓”保存真实订单量；同一目的国各发货仓订单量之和必须等于目的国订单总量。
- 仓内拆包：同一订单可以在所属发货仓拆成多个包裹，并使用不同承运商或跨境运输方案。
- 等效订单：在同一“月份 × 目的国 × 发货仓”范围内，按包裹占比把真实订单量分配到承运商和运输方式，形成线路层等效订单量。
- 拆包率：平均每单包裹数使用该目的国和发货仓的包裹总量除以真实订单量计算。
- 成本意义：仓储操作成本以真实仓级订单量为基础，线路层等效订单成本汇总后必须回到该仓权威成本。
- 后续扩展：进入订单级模型后，通过订单—包裹关联支持跨仓拆单，并解除该 MVP 限制。

## D-091：计算字段的高精度实现

- 状态：已按技术授权确定
- 日期：2026-08-13
- 决策：业务输入、权威金额和标准报告金额沿用 D-015 的 4 位小数口径；确定性公式中间值、原子成本与归因事实使用高精度十进制或精确有理数。
- 数据库建议：持久化计算金额使用不低于 `numeric(50,24)` 的精度；汇总完成后另生成 4 位业务报告金额和 2 位界面显示值。
- 算法要求：禁止使用 JavaScript `number` 执行财务计算；计算核心使用十进制定点或任意精度库。
- 恒等式意义：严格可加总校验针对未舍入高精度结果；4 位报告字段和 2 位显示字段的尾差按 D-015 的舍入调整规则处理。
- 产品影响：该技术细化不改变任何公式、指标口径或用户看到的显示精度，只防止原子行提前舍入破坏下钻一致性。

## D-092：自营仓人工成本分类

- 状态：已确认
- 日期：2026-08-14
- 适用前提：MVP 的德国仓和法国仓均按自营仓处理，不使用第三方仓按单收取的综合仓储服务费。
- 决策：原“仓储操作费”和“物流人工”改为三个互斥口径：一线作业弹性人工、一线作业基础人工、仓库管理人工。
- 一线作业弹性人工：包括计件工资、加班费、临时用工及其他随作业量变化的人工，属于销售履约变动成本；计算为 `仓级真实订单量 × 有效单均一线作业弹性人工成本`，线路层按等效订单量承接。
- 一线作业基础人工：包括一线作业人员底薪、固定津贴及雇主承担的固定人工成本，属于物流运营固定成本，归属具体发货仓。
- 仓库管理人工：包括仓库经理、主管、计划及其他非直接计件管理岗位的人工成本，属于物流运营固定成本，归属具体发货仓。
- 底薪处理：不得把一线人员底薪摊入当期单均变动人工成本；实际工资按薪资项目、劳动合同或经批准的薪酬结构拆分。只有计件、加班、临时用工及其他量相关部分进入变动成本。
- 归因边界：一线作业弹性人工的有效单均成本变化归入价因素；一线作业基础人工和仓库管理人工不进入五因素，统一在固定成本桥中展示。
- 数据兼容：事实粒度、固定成本粒度、五因素顺序和物流总成本恒等式不变；仅替换成本类别与价格字段。后续固定成本驱动模型可分别挂接一线基础人工人数或工时、管理岗位编制及单位人工成本。
- 替代关系：本决策细化并取代 D-005、D-006、D-011、D-012、D-024、D-073、D-074 和 D-090 中使用“仓储操作费”或“物流人工”作为当前口径的相关表述；历史决策原文保留用于追溯。

## D-093：第三阶段采用核心纵向切片推进

- 状态：已确认
- 日期：2026-08-14
- 决策：第三阶段不同时铺开全部页面，先实现并验收一条端到端核心纵向流程，再扩展 Forecast 和情景模拟功能。
- 核心流程：全年 Latest Outlook 异常识别 → 2026 年 8 月英国异常下钻 → 五因素归因 → 数字级结构化证据 → AI 管理分析。
- 契约范围：首批确定性查询、证据对象、页面上下文和 AI 输出契约优先覆盖该流程，并复用同一指标、版本、筛选及归因口径。
- 验收要求：流程中的页面数字、查询结果、归因金额、证据对象和 AI 标准答案必须相互一致，并由现有固定评估基线覆盖。
- 扩展顺序：核心流程验收通过后，再依次接入 Forecast 查看、Growth Case、Cost Saving Case 和自定义情景。
- 范围边界：该决策只确定交付顺序，不删除或缩减已经冻结的 MVP 功能范围。

## D-094：核心异常进入独立归因分析页

- 状态：已确认
- 日期：2026-08-14
- 决策：用户从驾驶舱点击“2026 年 8 月英国异常”后，进入独立的归因分析页，不在驾驶舱内直接展开完整归因内容。
- 页面职责：驾驶舱负责识别和排序异常；归因分析页负责展示差异范围、五因素瀑布图、多维下钻、数字级证据和 AI 管理分析。
- 可复现地址：期间、比较口径、目的国、Budget/Actual/Forecast 或情景版本等决定分析结果的状态写入页面地址；同一有效地址必须恢复相同的查询范围。
- 上下文传递：进入归因页后，页面可见筛选状态同时作为 AI 查询上下文；AI 回答仍须明确披露最终采用的范围，不得仅依赖隐式页面状态。
- 证据链接：数字级证据可以链接回该归因页及对应筛选范围；证据快照仍按 D-042 保持不可变，不因页面默认值变化而改写。
- 导航要求：归因页保留返回驾驶舱的明确入口，并保持用户离开前的驾驶舱比较范围。

## D-095：归因页默认下钻路径与维度切换

- 状态：已确认
- 日期：2026-08-14
- 决策：归因分析页采用“发货仓 → 运输方式 → 承运商 → 成本类别”的默认下钻路径，同时允许用户切换为按单个合法维度独立查看。
- 默认路径：核心纵向切片按上述顺序展示英国异常从德国仓、空运、高价承运商到具体成本类别的贡献变化。
- 建模边界：发货仓、运输方式、承运商和成本类别是可组合分析维度，不定义为不可变的主数据父子层级；默认路径只是产品引导顺序。
- 查询要求：确定性查询契约须支持 `group_by` 选择合法维度，并保证固定路径下钻、单维切换、AI 结论和证据对象使用同一原子归因事实聚合。
- 状态行为：切换查看维度不改变期间、比较口径、目的国和版本；返回默认路径时恢复此前的下钻位置。
- 首期范围：核心纵向切片优先实现默认路径，其他单维视角沿用同一接口逐步接入，不重新计算归因。

## D-096：五因素瀑布图同步筛选下钻表

- 状态：已确认
- 日期：2026-08-14
- 决策：用户选择五因素瀑布图中的某个因素时，下方多维下钻表同步筛选为该因素的贡献明细。
- 默认状态：未选择单一因素时，下钻表展示量、结构、效率、价、汇率五因素的合计差异；各行和总计必须与当前范围的履约变动成本差异勾稽。
- 选中状态：选择某个因素后，下钻表只展示该因素在当前分组维度上的贡献；表格总计必须等于瀑布图中该因素的未舍入高精度金额。
- 清除行为：再次选择当前因素或执行“清除筛选”后恢复五因素合计差异，不改变期间、比较口径、目的国、版本或下钻位置。
- 证据要求：因素选择进入确定性查询参数和证据对象；页面数值、表格汇总、AI 引用及证据快照必须使用同一因素范围。
- 固定成本边界：固定成本差异继续在独立区域展示，不响应五因素选择，也不并入因素下钻表。

## D-097：因素筛选不自动触发 AI 管理分析

- 状态：已确认
- 日期：2026-08-14
- 决策：用户选择或清除五因素、切换下钻维度及改变下钻位置时，只更新确定性图表、表格和待查询上下文，不自动调用模型或改写已有 AI 回答。
- 主动触发：页面根据当前可见范围提供明确操作，例如“分析结构因素”；只有用户主动执行该操作后，才提交结构化意图并生成新的 AI 管理分析。
- 回答稳定性：已经生成的回答及其证据快照保持不变；页面筛选变化后，应提示该回答对应的历史范围，不把旧回答伪装成当前筛选结果。
- 新回答：每次主动触发均基于当时的确定性查询结果生成独立回答和不可变证据快照，不覆盖此前回答。
- 费用边界：纯确定性筛选和下钻不消耗 AI 调用额度；只有实际模型请求计入 D-046、D-047 定义的限流和费用熔断。
- 固定示例：固定示例回答可以随页面发布，但其范围和数据版本必须明确，不随当前页面筛选静默变化。

## D-098：数字证据在归因页侧栏中核对

- 状态：已确认
- 日期：2026-08-14
- 决策：用户点击 AI 回答中的数字证据引用时，在当前归因分析页打开证据侧栏，不跳转到独立证据页面。
- 上下文保持：打开和关闭证据侧栏不改变当前期间、比较口径、目的国、版本、因素选择或下钻位置。
- 侧栏内容：至少展示指标名称与口径、数值与单位、Budget/Actual/Forecast 或情景原值、差异、筛选范围、分组维度、数据版本、计算方法、来源结果 ID 和证据生成时间。
- 表格定位：侧栏提供“在表格中定位”操作；只有用户主动执行后才调整或展开下钻表，并明确显示为证据对应范围，不得静默改写页面分析上下文。
- 快照边界：侧栏读取回答生成时保存的不可变证据快照；当前正式数据或页面默认值变化时，旧证据仍显示原版本，并标记“基于历史版本”。
- 链接复现：每个证据对象使用稳定 `evidence_id`，支持通过链接直接恢复对应证据侧栏；不存在或无权访问的证据 ID 必须返回明确错误，不回退到近似结果。
- 移动端行为：窄屏设备使用覆盖式底部面板或全屏面板承载相同证据内容，不删除关键审计字段。

## D-099：归因页首次展示固定示例分析

- 状态：已确认
- 日期：2026-08-14
- 决策：归因分析页首次打开时，AI 区域默认展示一份已经通过固定评估集校验的示例回答，不以空白状态开始，也不自动调用真实模型。
- 默认范围：首期示例绑定“2026 年 8 月｜英国｜Actual vs Budget｜五因素合计”及对应的 Budget、Actual、归因和计算版本。
- 展示标识：示例回答须明确标记为“示例回答”，显示实际范围、生成所依据的数据版本和不可变证据快照，不得伪装为针对当前任意筛选即时生成。
- 主动生成：页面提供“按当前范围重新生成”操作；用户触发后才执行结构化意图校验、确定性查询和真实模型调用，并生成新的独立回答与证据快照。
- 范围变化：当前页面范围与示例回答不一致时，保留示例回答并显示范围不一致提示，不静默替换数字、证据或文字。
- 回退能力：模型额度耗尽、费用熔断或模型服务不可用时，固定示例及其证据仍可完整查看，确定性图表和下钻功能不受影响。
- 评估来源：示例回答优先复用 `data/generated/ai-evaluation-baseline.json` 中与核心纵向切片对应的已校验标准答案，不维护另一套可能漂移的手工数字。

## D-100：归因页采用图表与 AI 并列、下钻表全宽布局

- 状态：已确认
- 日期：2026-08-14
- 决策：桌面端归因分析页顶部采用两列布局，左侧展示差异摘要和五因素瀑布图，右侧展示 AI 管理分析；多维下钻表位于下一行并使用页面全宽。
- 宽度原则：桌面端左侧分析区约占三分之二，右侧 AI 区约占三分之一；具体像素由响应式布局决定，不把比例写死为不可调整常量。
- 图表职责：左侧优先承载当前范围、核心差异和因素选择，使瀑布图保留足够宽度展示五因素金额及起止值。
- AI 职责：右侧同时显示示例回答或用户主动生成的回答，并保留关键数字证据引用；回答较长时在面板内部滚动或分节展示，不挤压下钻表列宽。
- 表格职责：下钻表独占全宽，支持默认下钻路径、单维切换、因素同步筛选、排序和证据定位。
- 证据侧栏：证据侧栏从页面右侧覆盖打开，可以暂时覆盖 AI 区域，但不得永久改变两列布局或丢失当前回答状态。
- 移动端顺序：窄屏按“范围与差异摘要 → 瀑布图 → AI 管理分析 → 多维下钻表”纵向排列；证据使用覆盖式底部或全屏面板。

## D-101：归因页成本摘要与诊断指标分层

- 状态：已确认
- 日期：2026-08-14
- 决策：目的国归因页顶部只设置三张成本摘要卡：当前场景的履约变动成本、比较基准的履约变动成本，以及差异金额与差异率。
- 核心示例：在“2026 年 8 月｜英国｜Actual vs Budget”范围内，三张卡分别展示 Actual 履约变动成本、Budget 履约变动成本和不利差异金额及差异率。
- 诊断指标带：订单量、空运包裹占比、Carrier C 包裹占比、准时履约率和服务成熟度放在成本摘要卡下方，作为业务变化和数据成熟度背景，不占用成本结果卡。
- 固定成本边界：目的国页面不得展示“目的国物流总成本”或“目的国固定成本”，因为仓租、一线作业基础人工、仓库管理人工和共享系统费用没有目的国分摊依据。
- 指标命名：目的国成本卡必须使用“履约变动成本”完整名称，不得简写为可能被理解为包含固定成本的“物流成本”。
- 查询要求：成本摘要与诊断指标来自同一期间、比较口径、目的国和版本范围；任一范围变化后统一刷新，禁止跨范围拼接。

## D-102：诊断指标的比较表达与成熟度边界

- 状态：已确认
- 日期：2026-08-14
- 决策：诊断指标带对可比较的经营指标统一展示当前值、基准值和变化量；服务成熟度只展示当前值及成熟状态，不与 Budget 成熟度机械比较。
- 订单量：展示当前订单量、基准订单量、绝对变化量和百分比变化；目的国层使用真实目的国订单量，不使用线路等效订单量。
- 结构指标：空运包裹占比和 Carrier C 包裹占比展示当前值、基准值及百分点变化，不把占比变化写成百分比增长率。
- 服务指标：准时履约率展示当前值、基准值及百分点变化，但只描述服务表现变化，不直接转换为成本或异常配送费用。
- 成熟度指标：服务成熟度只展示当前成熟比例；未达到 100% 时显示“结果尚未最终成熟”，并说明未到承诺截止日期的在途包裹不进入准时率分母。
- 语义边界：成熟度表示当前 cohort 的可评价覆盖程度，不是履约质量、准时率目标达成度或数据准确率。
- AI 一致性：AI 回答和数字证据使用相同的绝对量、百分比及百分点定义，不得混用。

## D-103：下钻表同时展示原始成本与归因贡献

- 状态：已确认
- 日期：2026-08-14
- 决策：多维下钻表不得只展示因素贡献金额，必须同时保留当前值、基准值、总差异和归因贡献，支持从归因结果反查成本基础。
- 默认列：维度名称、基准履约变动成本、当前履约变动成本、差异金额、差异率、不利贡献占比或节省贡献占比，以及证据入口。
- 因素状态：未选择单一因素时，归因贡献表示量、结构、效率、价、汇率五因素合计；选择某个因素后，增加或突出“该因素贡献”列，原始成本与总差异列保持可见。
- 汇总要求：表格底部显示当前分组范围的总计；基准成本、当前成本、总差异和因素贡献分别与上级范围及瀑布图勾稽。
- 贡献占比：沿用 D-051、D-052，将不利差异与节省贡献分池计算和展示，不以净差异作为单项贡献分母。
- 固定成本边界：目的国及其发货仓、运输方式、承运商、成本类别下钻只展示履约变动成本；不得为补全表格而把固定成本分摊到这些行。
- 证据要求：证据对象同时保存原始成本、差异和所选因素贡献的查询范围，避免只引用脱离基数的贡献金额。

## D-104：下钻表在当前页面内逐层展开

- 状态：已确认
- 日期：2026-08-14
- 决策：用户点击下钻表中的可展开行时，在当前表格内展开默认路径的下一层，不跳转到新的详情页面。
- 展开路径：发货仓行展开运输方式，运输方式行展开承运商，承运商行展开成本类别；展开后的子行继承当前期间、比较口径、目的国、版本和因素选择。
- 勾稽要求：每组直接子行的基准成本、当前成本、总差异及因素贡献之和必须等于父行；舍入尾差按既定高精度规则处理。
- 状态保持：展开或折叠行不重新计算归因、不调用模型，也不清除其他已展开路径；切换单维视角时，保存默认路径此前的展开位置。
- 操作区分：点击行名称或展开控件执行下钻；独立证据按钮打开证据侧栏。两种操作不得共用一个无明确含义的点击区域。
- 末级行为：成本类别为默认路径末级，不提供不存在的订单级或事件类型下钻；超出当前数据粒度时明确说明范围限制。
- 移动端行为：窄屏继续使用同一层级关系，可通过折叠列、横向滚动或卡片化子行展示，但不得改变父子勾稽逻辑。

## D-105：下钻表初始只展示发货仓层

- 状态：已确认
- 日期：2026-08-14
- 决策：首次进入目的国归因页时，下钻表只展示发货仓第一层，不自动展开系统判断的最大不利路径。
- 分析引导：按既定不利差异排序规则识别第一层最大不利行，并添加“最大不利贡献”标记；该标记用于引导查看，不表示唯一原因或已经确认的因果。
- 用户控制：用户主动展开发货仓后才加载并显示运输方式，再按 D-104 继续展开承运商和成本类别。
- 状态变化：选择因素、切换比较范围或重新加载当前地址时，不因排名变化自动展开新路径；只有用户操作改变展开状态。
- 固定示例：核心英国异常可以通过文案和标记提示“建议从德国仓继续下钻”，但不得预先隐藏其他发货仓或直接跳到 Carrier C。
- 排序一致：最大不利标记、表格排序、驾驶舱异常入口和 AI 的最大异常结论使用同一确定性排序结果。

## D-106：归因页默认展示整合式 AI 管理分析

- 状态：已确认
- 日期：2026-08-14
- 决策：归因页默认示例和用户主动生成的主回答均采用一份连贯的整合式管理分析，不拆成互不关联的固定问答卡。
- 固定结构：主回答固定包含“结论、证据、影响、建议、限制”五部分；各部分使用结构化字段承载，页面可以分节展示，但不得只依赖自由文本标题解析。
- 示例内容：核心英国异常示例组合现有评估集中的履约变动成本差异、五因素归因、结构指标变化、服务成熟度和数据边界标准答案，数字仍引用原评估基线及确定性查询结果。
- 证据要求：关键数字逐项绑定 `evidence_id`；整合多项标准答案时不得把不同范围、版本或计算方法的证据拼接为单一无范围结论。
- 快捷问题：固定问题继续作为“发生了什么、为什么发生、未来会怎样、调整参数后会怎样”的快捷入口；用户选择后生成独立回答，不替代或改写默认主分析。
- 建议边界：建议只能引用已计算的负向驱动和已知管理注释；未运行情景前不得把候选动作写成已经验证的节省结果。
- 评估映射：整合式回答需要记录其引用的固定评估题 ID 和查询结果 ID，支持分别验证意图、数字、证据及最终表述。

## D-107：首个纵向切片不提前接入情景草案操作

- 状态：已确认
- 日期：2026-08-14
- 决策：首个核心纵向切片中的 AI 建议只展示候选动作，不提供可点击的“创建情景草案”或其他尚未实现的情景操作。
- 状态标识：涉及降低空运比例、调整承运商结构或改善效率参数的建议必须标记“尚未进行情景验证”，不得展示未经确定性重算支持的节省金额或服务影响。
- 界面边界：首期不放置禁用、无响应或仅用于装饰的情景按钮；用户能够看到的操作必须具有真实、可验证的结果。
- 数据结构：建议仍按结构化字段保存候选参数方向、关联负向驱动、证据 ID、实施前提和限制，为下一切片映射情景草案保留接口。
- 后续接入：核心归因与 AI 主线验收后，再实现“结构化建议 → 用户确认 → 情景覆盖项 → 确定性重算 → 成本与服务结果”的完整闭环。
- 表述限制：在情景闭环上线前，AI 只能使用“建议评估”“可考虑”“候选动作”等措辞，不得写成已批准方案或已验证收益。

## D-108：核心纵向切片采用 9 题分阶段验收

- 状态：已确认
- 日期：2026-08-14
- 决策：首个核心纵向切片不以全部 20 道固定 AI 评估题作为上线门槛，先使用与驾驶舱异常入口、英国归因、AI 管理分析和越界保护直接相关的 9 题进行分阶段验收。
- 首期题目：`E01`、`E04`、`E05`—`E10`、`E20`。
- 覆盖范围：英国履约变动成本差异、全年 Latest Outlook 异常入口、五因素归因、最大驱动、空运和 Carrier C 结构变化、发货仓下钻、成本类别下钻、服务成熟度及订单级越界保护。
- 通过规则：9 题必须全部通过；程序计算数字与标准答案精确一致，证据对象的范围、版本、指标和数值完全一致，`E20` 不得虚构订单级事实。
- 页面验收：对应页面卡片、瀑布图、诊断指标、下钻表、AI 文本和证据侧栏必须返回同一结果；只通过后台 JSON 而页面不一致不视为通过。
- 总体门槛：D-088 定义的 20 题、至少 18/20、两道越界题全过及硬性失败规则继续作为完整 MVP 最终门槛；Forecast 和情景切片完成后统一执行。
- 回归要求：后续查询契约、数据版本、提示词或模型变更时，首期 9 题和已具备能力对应的其他题目持续回归，不因分阶段验收而停止维护完整评估集。

## D-109：公开根地址直接进入预算执行驾驶舱

- 状态：已确认
- 日期：2026-08-14
- 决策：公开作品集的根地址直接进入预算执行驾驶舱，不在核心应用前增加独立介绍页或营销落地页。
- 默认内容：驾驶舱沿用 D-053，默认展示全年 Latest Outlook vs Budget，并明确“2026 年 1—8 月 Actual + 9—12 月 Forecast”、最近结账月份和 Forecast 版本。
- 数据披露：页面顶部使用简短、持续可见但不遮挡主内容的说明条，标明“完全虚构的求职作品集演示数据”、报告币种和当前数据截止范围。
- 演示入口：驾驶舱提供“查看演示主线”操作，引导用户从全年异常进入 2026 年 8 月英国归因页；该入口不改变核心数据口径。
- 文档分工：完整项目目标、业务模型、数据结构、计算方法、技术架构和限制放在 README 及项目文档中，不复制为独立前置页面。
- 访问边界：匿名访客无需登录即可查看驾驶舱、归因和固定示例；真实模型调用继续受既定限流和费用熔断约束。
- 失败回退：即使模型服务不可用，根地址仍能显示完整驾驶舱、核心异常入口和固定示例，不得因 AI 状态阻塞首屏。

## D-110：演示主线由用户主动启动

- 状态：已确认
- 日期：2026-08-14
- 决策：“查看演示主线”不在首次访问时自动弹出；用户通过驾驶舱上的持续可见入口主动启动引导。
- 引导范围：首期使用四步引导，依次说明全年 Latest Outlook 风险、2026 年 8 月英国最大异常、五因素归因与多维下钻、数字级证据与 AI 管理分析。
- 交互原则：引导只高亮相关区域、显示简短说明并提供明确下一步，不锁定页面其他功能，不强制用户按顺序完成。
- 退出与重启：用户可以随时退出，并通过同一入口重新开始；无需保存复杂的跨会话完成状态。
- 导航行为：涉及从驾驶舱进入归因页时，只有用户点击明确的下一步或异常入口后才导航，不因阅读引导文字自动切换页面。
- 可访问性：引导支持键盘操作、清晰焦点、关闭按钮和减少动态效果偏好；不得依赖颜色或动画单独传达步骤。
- 数据边界：引导文案只引用当前已发布演示数据和固定示例，不触发真实模型调用，也不生成新的分析数字。

## D-111：采用专业财务经营分析视觉方向

- 状态：已确认
- 日期：2026-08-14
- 决策：MVP 采用专业财务经营分析风格，AI 作为受控分析能力呈现，不采用以渐变、发光和强装饰动画为主的科技感视觉。
- 基础色彩：浅色背景、深蓝和中性灰构成主要界面层级；普通信息和可交互状态使用蓝色体系。
- 业务语义色：不利成本差异使用克制的红色或橙红色，有利差异和节省贡献使用绿色；颜色含义遵守 D-050，不按数字正负号机械映射。
- AI 区域：可以使用与主界面协调的蓝色强调和明确的“示例回答”“实时生成”“历史版本”状态标签，但不得以视觉效果掩盖证据和限制。
- 图表原则：优先保证数值标签、起止值、单位、图例、比较范围和勾稽关系清晰；减少无业务含义的阴影、三维效果和过渡动画。
- 信息密度：采用适合经营复盘和财务分析的中高信息密度，通过留白、分组和层级组织内容，不把驾驶舱设计成低信息量宣传页。
- 可访问性：不依赖颜色单独表示有利、不利、选中或风险状态；同时使用文字、图标或形状，并满足基本对比度和键盘焦点要求。

## D-112：首版只实现桌面端

- 状态：已确认
- 日期：2026-08-14
- 决策：MVP 首版只设计、开发和验收桌面端；移动端适配延后到全部桌面功能完成并通过验收之后再评估。
- 目标环境：主要面向办公电脑和招聘展示场景，设计基准约为 1440px 宽桌面视口，并保证常见 1280px 及以上桌面宽度可用。
- 首期边界：不为手机和平板单独设计导航、卡片化下钻表、底部证据面板或移动端引导流程，也不以移动端截图作为首版验收物。
- 窄屏处理：低于支持宽度时可以保持桌面布局并出现横向滚动，或显示“请使用桌面端查看完整分析”的明确提示；不得为通过表面响应式检查而隐藏核心数字、证据或下钻功能。
- 延后范围：D-098、D-100、D-104 中关于移动端证据面板、纵向布局和卡片化下钻的要求统一转为后续扩展，不属于当前实现门槛。
- 仍需满足：桌面端继续满足键盘操作、焦点可见、颜色对比度和减少动态效果等基本可访问性要求；桌面优先不等于取消可访问性。

## D-113：正式开发前先验收可点击桌面原型

- 状态：已确认
- 日期：2026-08-14
- 决策：进入正式应用开发前，先制作并验收一个可点击的桌面端低保真原型，用于验证核心纵向切片的页面流程、信息层级和交互关系。
- 数据要求：原型使用当前已审计的真实演示数字和标准答案，不使用 Lorem Ipsum、随机金额或与数据基线不一致的占位内容。
- 覆盖流程：预算执行驾驶舱 → 2026 年 8 月英国异常入口 → 独立归因页 → 五因素筛选 → 表格逐层展开 → 整合式固定 AI 示例 → 数字证据侧栏。
- 功能边界：原型不连接正式后端、PostgreSQL、真实模型、限流或费用熔断服务；所有可见数字和示例回答来自版本化本地数据快照。
- 契约关系：原型使用查询意图、查询结果、证据对象和 AI 输出契约的草案结构；原型验收后再冻结这些契约，避免正式代码与已验证交互脱节。
- 验收目的：重点检查业务口径是否清楚、数字是否勾稽、操作是否可发现、证据是否可核对，不以生产性能、权限、安全或部署能力作为原型通过条件。
- 实现约束：原型代码与正式应用代码分开管理或明确标记为可丢弃，不因已经可点击就直接视为生产实现。

## D-114：原型只开放固定核心演示范围

- 状态：已确认
- 日期：2026-08-14
- 决策：可点击原型只实现 D-113 定义的核心演示路径，不同时开放所有月份、目的国、比较口径和版本筛选。
- 驾驶舱范围：固定展示 2026 全年 Latest Outlook vs Budget，并以只读标签明确“1—8 月 Actual + 9—12 月 Forecast”、结账月份和版本。
- 归因范围：从核心异常入口固定进入“2026 年 8 月｜英国｜Actual vs Budget”；期间、目的国、比较口径和版本以只读标签展示，不伪装成可切换筛选器。
- 可用操作：演示主线、英国异常跳转、五因素选择、表格逐层展开、AI 固定示例、数字证据侧栏及返回驾驶舱必须具有真实行为。
- 禁止占位：原型不得显示点击无响应的国家、月份、版本或情景控件；尚未实现的能力通过文档说明，不在页面制造假入口。
- 数据来源：所有可见内容直接读取或由构建过程提取自版本化演示数据和评估基线，禁止维护另一套手工数字。
- 正式应用边界：多范围筛选、自由查询、Forecast 和情景功能在原型验收及查询契约冻结后接入正式应用，不因原型范围固定而从 MVP 删除。

## D-115：驾驶舱主图展示全年总成本与结账分界

- 状态：已确认
- 日期：2026-08-14
- 决策：预算执行驾驶舱主图采用 12 个月公司物流总成本趋势图，以月度 Budget 为对比线，并把 Latest Outlook 明确拆分为 1—8 月 Actual 和 9—12 月 Forecast。
- 视觉编码：Budget 使用中性灰对比线；Actual 使用深蓝柱；Forecast 使用浅蓝柱；8 月与 9 月之间标出最近结账月份分界。
- 指标口径：柱形和对比线均使用公司物流总成本，包含履约变动成本和物流运营固定成本；图表标题、图例和悬浮信息明确该口径。
- 异常标记：2026 年 8 月柱显示不利异常标记，并披露该月公司总成本 Actual、Budget 及差异；不得只用颜色暗示异常。
- 归因入口：用户点击 8 月异常标记或对应明确操作后，进入“2026 年 8 月｜英国｜Actual vs Budget”归因页；页面同时说明从公司总成本视角切换为英国履约变动成本视角。
- 版本披露：主图持续显示 Latest Outlook 由 1—8 月 Actual 与 9—12 月指定 Forecast 组成，不把未来月份称为 Actual。
- 原型范围：首个可点击原型只支持上述固定全年视图和 8 月英国入口，不显示尚未接入的月度或版本筛选控件。

## D-116：驾驶舱展示可审计的 Top 5 月度目的国异常

- 状态：已确认
- 日期：2026-08-14
- 决策：驾驶舱异常区域不只展示单一英国故事卡，而是展示按确定性规则计算的 Top 5“月份 × 目的国”不利异常排名。
- 排序指标：使用履约变动成本不利差异金额降序排列；已结账月份比较 Actual vs Budget，未来月份比较指定 Forecast vs Budget。
- 行展示：每行显示月份、目的国、Actual 或 Forecast 属性、当前履约变动成本、基准成本、差异金额、差异率和不利贡献占比。
- 贡献分母：不利贡献占比使用 D-052 的正向差异分池，不使用公司净差异或包含节省项的分母。
- 核心标记：2026 年 8 月英国位列第一时添加“最大单月不利异常”标记，并提供明确归因入口；该位置必须来自当前发布数据的排序结果，不硬编码排名序号。
- 交互边界：原型中只有已实现核心路径的首行提供归因跳转；其他行保留完整数字但不显示无功能详情按钮。
- 一致性：排名结果、8 月主图标记、D-105 最大不利标记和 AI 对最大异常的结论必须使用同一查询和排序规则。

## D-117：物流总成本卡展示变动与固定成本分层

- 状态：已确认
- 日期：2026-08-14
- 决策：驾驶舱“物流总成本”核心卡在主数字下直接展示履约变动成本和物流运营固定成本的拆分，并提供固定成本类别展开。
- 主数字：展示当前全年 Latest Outlook 物流总成本、年度 Budget、差异金额、差异率及有利或不利语义。
- 第一层拆分：履约变动成本与固定成本分别显示当前值、Budget 和差异；两部分之和必须与主数字勾稽。
- 固定成本展开：展示仓租、一线作业基础人工、仓库管理人工和系统费用，沿用 D-092 当前口径，不再显示已被替代的“物流人工”合并类别。
- 归属边界：固定成本只按公司、月份、可归属发货仓或共享层展示，不分摊到目的国、承运商、运输方式或五因素归因。
- 交互要求：原型中的展开控件必须真实显示上述类别及勾稽总计；展开和折叠不触发 AI、不改变驾驶舱范围。
- 口径一致：卡片、展开明细、月度总成本主图、工作簿和 AI 证据使用同一固定成本事实及版本。

## D-118：固定成本明细按类别优先组织

- 状态：已确认
- 日期：2026-08-14
- 决策：驾驶舱固定成本展开明细默认按成本类别组织，发货仓或共享层作为可继续展开的第二层。
- 第一层类别：仓租、一线作业基础人工、仓库管理人工和系统费用；每类展示当前值、Budget、差异金额和差异率。
- 第二层归属：仓租、一线作业基础人工和仓库管理人工可以继续展开德国仓与法国仓；系统费用显示为共享层，不伪造单仓分摊。
- 勾稽要求：各类别之和等于固定成本总额；类别下各发货仓或共享层之和等于对应类别；当前值、Budget 和差异分别勾稽。
- 展示边界：类别优先只用于页面组织，不改变固定成本事实的“月份 × 发货仓或共享范围 × 类别 × 版本”粒度。
- 后续扩展：未来固定成本驱动模型可以在第二层以下增加面积、人数、工时或账号用量，但不得修改当前类别定义和历史事实。

## D-119：结束核心原型访谈并进入产物编制

- 状态：已确认
- 日期：2026-08-14
- 决策：D-093 至 D-118 已覆盖核心桌面原型的业务范围、页面结构、交互、AI、证据、视觉、数据边界和验收门槛，本轮逐项访谈结束，进入产物编制。
- 产物顺序：先生成确定性查询契约草案和原型规格文档，再制作使用真实演示数据的可点击桌面原型。
- 文档产物：`docs/query-contract.md` 定义查询意图、确定性结果、证据对象及 AI 输出结构；`docs/prototype-spec.md` 定义页面、状态、交互、范围和验收清单。
- 原型产物：只覆盖 D-114 固定核心演示范围，使用当前版本化演示数据和固定评估标准答案，不连接正式后端、数据库或真实模型。
- 设计探索：原型阶段可以提供多个结构明显不同的桌面布局方案供比较，但所有方案必须遵守相同业务口径、数据、核心流程和可用操作。
- 验收门槛：原型业务结果按 D-108 的 9 题分阶段门槛检查；交互验收同时覆盖异常跳转、因素筛选、逐层下钻、固定 AI 示例和证据侧栏。
- 后续闸门：原型确认后冻结查询与证据契约，再进入正式 Next.js 应用实现；原型代码不直接视为生产代码。

## D-120：分离公司发货仓差异与英国归因下钻口径

- 状态：已确认
- 日期：2026-08-14
- 决策：保留审计工作簿和 AI 评估基线中 E08 的公司口径，并在驾驶舱增加紧凑的“8 月公司发货仓差异”区域；英国归因页继续使用“2026 年 8 月｜英国｜Actual vs Budget”的目的国口径。
- 公司口径：E08 按 2026 年 8 月公司销售履约变动成本差异、分组维度为发货仓，德国仓为 589,251.0927 CNY，法国仓为 12,299.5128 CNY，合计为 601,550.6055 CNY。
- 英国口径：归因页发货仓根节点仅包含英国，德国仓为 566,770.9315 CNY，法国仓为 7,703.2917 CNY，合计为 574,474.2232 CNY。
- 展示要求：公司发货仓区域必须明确标注“公司口径”，归因页必须持续显示“英国”范围标签；不得在同一表格或同一总计中混合两组数字。
- 查询与证据：公司发货仓差异和英国归因下钻使用不同查询结果 ID、证据 ID、筛选范围和勾稽总计；E08 只映射到公司发货仓差异查询，E09 继续映射到英国成本类别下钻。
- 权威边界：不修改已审计工作簿或评估标准答案。若未来要把 E08 改为目的国口径，应建立新评估版本并重新完成工作簿审计、基线生成和验收，而不是覆盖当前结果。

## D-121：正式实现采用方案 B“分析工作台侧轨”

- 状态：已确认
- 日期：2026-08-17
- 决策：正式实现以方案 B“分析工作台侧轨”为主体结构，不以方案 A“经营驾驶舱分栏”或方案 C“管理简报画布”为主体。
- 保留结构：左侧固定导航与范围摘要；主区域承载指标、月度成本趋势、异常排名、固定成本与公司发货仓差异；归因页使用横向五因素命令带；AI 管理分析在右侧持续可见；多维归因表保持全宽。
- 选择依据：该结构优先支持财务分析岗位的高频异常识别、范围核对、因素切换、逐层下钻与证据复核，减少在驾驶舱、归因和 AI 结果之间反复切换上下文。
- 不变边界：方案选择只冻结信息结构，不改变 D-093 至 D-120 已确认的指标口径、数据范围、排序、五因素算法、证据对象、AI 限制和 9 题验收基线。
- 待确认项：是否吸收方案 A 或 C 的局部元素，以及人工视觉与交互验收是否通过，需在冻结查询与证据契约前分别确认。

## D-122：方案 B 不吸收 A/C 局部元素

- 状态：已确认
- 日期：2026-08-17
- 决策：方案 B 保持纯“分析工作台侧轨”结构，不吸收方案 A 的传统驾驶舱分栏或方案 C 的管理简报画布元素。
- 原型收敛：删除 A/C 页面渲染路径、方案查询参数、底部方案切换器和左右方向键切换行为；原型入口固定展示方案 B。
- 原因：方案 B 已完整覆盖核心指标、异常排名、五因素命令带、多维下钻和持续可见的 AI 分析。混合其他结构会增加重复层级，并削弱范围摘要和分析上下文的稳定性。
- 不变边界：收敛只影响原型信息结构和导航，不改变数据快照、确定性结果、证据对象、评估基线或业务口径。
- 下一闸门：完成人工桌面视觉与核心交互验收后，才能冻结查询与证据契约并进入正式 Next.js 实现。

## D-123：人工桌面验收是契约冻结前的强制闸门

- 状态：已确认
- 日期：2026-08-17
- 决策：方案 B 原型必须先完成人工桌面视觉与核心交互验收；验收通过前，不冻结查询与证据契约，也不进入正式 Next.js 页面实现。
- 目标环境：至少检查 1440px 设计基准和 1280px 最低支持宽度；低于支持宽度不属于本轮验收范围。
- 核心路径：全年工作区 → 8 月英国异常入口 → 英国归因 → 五因素选择 → 多维表逐层展开 → AI 数字证据 → 表格定位 → 返回全年工作区。
- 必查交互：固定成本明细展开、异常入口与返回、因素选择与清除、树形表展开、证据侧栏打开与关闭、证据定位、四步演示主线及浏览器前进后退。
- 视觉要求：范围、比较口径、Actual/Forecast 分界、不利语义、公司与英国口径边界、AI 示例状态和限制信息必须无需猜测即可识别；不得出现遮挡、溢出或关键内容不可操作。
- 通过规则：核心路径或业务口径问题为阻断项，必须修复后复验；纯装饰性轻微问题可以记录，但不得影响可读性、键盘操作或证据核对。
- 验收记录：结果写入独立验收记录，包含环境、步骤、预期结果、实际结果、问题等级、修复状态和复验结论；不得仅以口头“看起来正常”代替。

## D-124：方案 B 人工桌面验收通过

- 状态：已确认
- 日期：2026-08-18
- 决策：方案 B 已完成 D-123 规定的人工桌面验收，1440px 与 1280px 两档均通过 V01—V12。
- 修复结果：P01 排序测试说明缺口、P02 AI 限制披露问题和 P03 静态资源缓存问题均已修复并复验通过；当前无未关闭的阻断、主要或轻微问题。
- 数据结果：重新生成的原型快照包含 65 个证据对象，核心 9 项评估继续全部通过。
- 阶段结论：D-123 验收闸门已满足，查询与证据契约具备冻结条件；在单独确认冻结决策前仍保持草案状态。

## D-125：冻结查询与证据契约 V1.0

- 状态：已确认
- 日期：2026-08-18
- 决策：`docs/query-contract.md` 从 V0.1 草案升级并冻结为正式实现契约 V1.0，作为 Next.js 核心纵向切片的查询、结果、证据、AI 输出和错误处理基线。
- 冻结范围：查询意图与操作、范围和版本语义、金额与比率规则、结构化证据对象、证据快照、AI 管理分析输出、订单粒度保护、页面地址状态及核心固定结果。
- 版本治理：V1.0 不得被静默修改；新增字段或操作优先采用向后兼容扩展，任何破坏性变化必须创建新契约版本，并记录迁移影响、回归范围和发布条件。
- 验收依据：D-124 的 1440px 与 1280px 人工验收、核心 9 项评估、65 个证据对象及 P01—P03 修复复验结果。
- 阶段结果：项目进入第三阶段核心纵向切片的正式实现准备；原型继续作为交互参考，但其代码不直接作为生产代码。

## D-126：正式核心纵向切片采用两个顺序实现闸门

- 状态：已确认
- 日期：2026-08-18
- 决策：正式核心纵向切片拆成两个必须依次通过的实现闸门；第二闸门不得在第一闸门未通过时提前验收。
- 第一闸门：Next.js App Router 正式应用、PostgreSQL 只读发布数据、确定性查询与证据 API、方案 B 桌面页面和已校验固定 AI 示例。
- 第一闸门验收：V1.0 契约、核心 9 题、页面数字、勾稽、证据侧栏、URL 状态和 1440/1280 桌面行为全部通过；不调用真实模型。
- 第二闸门：接入真实模型调用、匿名限流、费用估算与两级熔断、不可变证据快照及固定示例回退。
- 第二闸门验收：模型不可篡改确定性数字，不得生成虚假证据；限流、费用熔断、失败回退、范围不一致提示和历史快照复现全部通过。
- 顺序约束：第二闸门只能增加受控 AI 能力，不得修改第一闸门冻结的业务口径、查询结果或证据语义；必要变更按 D-125 创建新契约版本。

## D-127：第一闸门必须通过 PostgreSQL 执行正式确定性查询

- 状态：已确认
- 日期：2026-08-18
- 决策：第一闸门的正式运行数据必须进入标准化 PostgreSQL 关系表；查询服务必须从数据库执行确定性计算并生成查询结果与结构化证据。
- 原型快照边界：`prototype/logiplan-core/prototype-data.json` 仅作为原型输入、测试夹具和冻结结果参照，不得成为正式应用的运行数据源，也不得由页面直接读取后冒充数据库查询结果。
- 验收要求：PostgreSQL 查询结果、金额精度、汇总勾稽、排序、证据定位和核心 9 题必须与 V1.0 契约及已审计基线逐项一致。
- 可追溯要求：正式结果必须携带数据发布版本和计算版本，能够追溯至版本化只读发布数据；数据库路径不得改变 D-125 冻结的业务语义。

## D-128：正式演示数据采用离线、可重复、原子发布

- 状态：已确认
- 日期：2026-08-18
- 决策：正式演示数据通过独立于应用启动过程的受控发布流程装载，不允许 Web 应用在启动时自动填充、覆盖或迁移业务数据。
- 固定流程：先执行数据库结构迁移，再将带唯一版本号的数据包导入候选发布区，随后校验清单、行数、文件校验和、引用完整性、业务恒等式和核心 9 题，全部通过后原子切换当前活动发布版本。
- 失败处理：任一导入或校验步骤失败都不得激活候选版本；当前活动版本保持不变。回滚通过重新指向已验证的上一发布版本完成，不修改历史发布数据。
- 可重复性：同一数据包、数据库结构版本和计算版本必须产生相同结果；重复执行不得生成重复业务记录或改变已发布版本。
- 运行边界：应用启动只检查数据库结构及活动数据版本是否兼容；不执行正式数据写入。业务查询账户对已发布业务数据保持只读。

## D-129：首批数据库迁移只实现第一闸门所需结构

- 状态：已确认
- 日期：2026-08-18
- 决策：首次正式数据库迁移只创建第一闸门核心纵向切片实际读写或校验的结构，不提前创建尚未进入实现阶段的空表。
- 首批范围：数据发布与计算版本、核心业务维度和履约线路、Budget、Actual、正式 Forecast、价格、汇率，以及驾驶舱、下钻、五因素归因和证据生成所需的统一分析事实与固定成本事实。
- 延后范围：会话情景输入与结果、真实 AI 调用记录、不可变 AI 证据快照、匿名限流、用量和费用熔断结构，分别随 Forecast/情景扩展或第二闸门实施。
- 兼容约束：首批表名、字段、枚举、精度、稳定标识和外键语义必须与 V1.0 数据字典兼容；延后创建不构成删除 MVP 能力或修改既有业务口径。
- 迁移原则：每项新增结构在对应功能进入实施时通过独立、可回滚迁移加入，不通过预建未使用表推测未来设计。

## D-130：发布时固化原子事实，按请求组装页面结果

- 状态：已确认
- 日期：2026-08-18
- 决策：第一闸门采用混合计算边界。统一分析事实、固定成本事实和原子归因事实在数据发布阶段以确定性模型计算、校验并写入 PostgreSQL；页面级汇总、筛选、排序和证据对象由查询服务按请求生成。
- 原子归因粒度：继续遵守 D-063、D-086 和数据字典 8.6，按“比较 × 月份 × 履约线路 × 成本类别 × 归因方法 × 因素”保存高精度结果；页面只汇总这些原子事实，不在筛选后重新运行归因算法。
- 禁止事项：不得把驾驶舱、下钻表、侧栏回答或任意完整页面响应预先保存为权威业务事实，也不得以原型 JSON 代替查询组装。
- 缓存边界：后续如增加页面查询缓存，缓存键必须覆盖完整查询范围、数据发布版本、比较版本和计算版本；缓存可随时删除并从 PostgreSQL 权威事实重建。
- 一致性：发布阶段和请求阶段均使用精确十进制；同一版本及查询范围必须返回稳定一致的金额、排序和证据引用。

## D-131：显式 SQL 迁移是数据库结构的唯一权威来源

- 状态：已确认
- 日期：2026-08-18
- 决策：PostgreSQL 的表、类型、约束、索引、视图和结构版本由纳入版本控制的显式 SQL 迁移定义，SQL 迁移是数据库结构的唯一权威来源。
- 启动边界：Web 应用启动时只检查结构版本兼容性，禁止 ORM、数据库客户端或启动脚本自动建表、自动同步、推送或修改正式数据库结构。
- 工具边界：允许使用类型安全数据库访问层、类型生成器和迁移执行器，但生成类型或 ORM 模型不得反向取代已审查的 SQL 迁移。
- 迁移要求：每个迁移具有稳定编号、单一职责和可审查差异；发布前在空库和上一正式结构版本上分别验证前向迁移，并为可回退变更提供明确回滚路径。
- 财务约束：精确十进制类型、唯一性、引用完整性、检查约束和发布状态约束必须在数据库层显式声明，不仅依赖应用校验。

## D-132：SQL 只允许由独立数据访问层执行

- 状态：已确认
- 日期：2026-08-18
- 决策：正式应用中只有独立数据访问层可以执行 SQL。React 组件、页面加载逻辑、Server Actions、Route Handlers 和查询服务均不得直接拼接或执行 SQL。
- 调用链：界面与路由调用查询服务；查询服务负责权限外的业务范围校验、确定性聚合和 V1.0 契约组装；领域层负责精确计算；数据访问层负责参数化 SQL、事务、连接和 PostgreSQL 类型转换。
- 返回边界：数据访问层返回稳定的领域记录或只读数据传输对象，不向上暴露数据库连接、查询构造器、表对象或驱动专用结果类型。
- 安全要求：所有动态条件通过绑定参数或受控枚举映射进入 SQL；用户输入、URL 参数和模型输出不得成为 SQL 片段、表名、列名或排序表达式。
- 变更隔离：数据库表结构变化首先在数据访问层适配；不得要求页面组件理解物理表、连接关系或精度转换细节。

## D-133：V1.0 查询契约是所有页面查询的唯一入口

- 状态：已确认
- 日期：2026-08-18
- 决策：驾驶舱、下钻、归因、证据侧栏和固定 AI 示例所需的数据请求，最终都必须归一化为 V1.0 定义的 `QuestionType + AnalysisScope`，并由同一个查询服务执行。
- 页面封装：允许为页面提供具名便捷函数，但便捷函数只能构造契约输入和选择返回视图，不得绕过范围校验、显式版本、确定性查询、精度规则或证据生成。
- 禁止分叉：不得为单个页面编写独立口径 SQL、独立指标计算或缺少证据对象的专用数据通道；同一契约输入在不同入口必须得到相同业务结果。
- 扩展规则：新页面优先组合既有 `QuestionType`；只有现有意图无法准确表达业务问题时，才按 D-125 的版本治理扩展契约。
- 测试要求：核心 9 题直接通过统一查询服务执行；页面验收使用同一服务返回值，不维护第二套页面测试答案。

## D-134：浏览器使用单一版本化查询端点，服务端直接复用查询服务

- 状态：已确认
- 日期：2026-08-18
- 决策：所有由浏览器触发的确定性业务查询统一提交至 `POST /api/v1/query`，请求体使用 V1.0 的 `QuestionType + AnalysisScope`；第一闸门不建立页面专用查询端点。
- 服务端路径：Next.js 服务端渲染、Server Component 和服务端页面加载直接调用同一个查询服务函数，不通过 `localhost`、站点域名或其他 HTTP 回环访问自身 Route Handler。
- 逻辑一致性：HTTP 端点和服务端直接调用共享同一输入校验、范围规则、数据访问、精度处理、结果类型、错误映射和证据生成实现，不复制业务逻辑。
- HTTP 边界：Route Handler 只负责解析请求、调用查询服务以及将领域结果或结构化错误序列化为 JSON，不执行 SQL 或页面专用计算。
- 版本治理：路径中的 `v1` 与 D-125 冻结契约对应；破坏性传输或语义变化使用新版本路径，不静默改变现有端点。

## D-135：确定性查询原子返回，超时使用独立基础设施错误

- 状态：已确认
- 日期：2026-08-19
- 决策：`POST /api/v1/query` 采用一次性完整 JSON 响应，查询结果和证据对象必须原子返回，不提供或展示不完整的流式业务结果。
- 超时边界：查询服务设置 10 秒硬超时；超过时限停止本次请求并返回 HTTP 504，不把部分结果写入响应、页面状态或查询缓存。
- 契约隔离：D-125 冻结的 V1.0 `QueryError.code` 联合类型保持不变。超时属于基础设施故障，使用独立响应体，至少包含 `type: "QUERY_TIMEOUT"`、`message_zh` 和 `request_id`。
- 页面行为：收到超时错误后保留用户当前选择的查询范围并明确显示失败；既有成功结果可以保留作为旧结果，但必须标注未被本次请求更新，不得伪装成本次完整结果。
- 性能目标：第一闸门在正式验收数据规模和正常热运行条件下，确定性查询端到端 P95 不超过 1 秒；10 秒是故障保护上限，不是正常性能目标。

## D-136：取消旧查询并禁止过期响应覆盖当前状态

- 状态：已确认
- 日期：2026-08-19
- 决策：用户切换筛选、离开当前页面或在前一次查询完成前发起新查询时，客户端立即取消旧请求，并保证旧响应不能覆盖新的范围、结果、证据或错误状态。
- 客户端机制：每次查询创建独立取消信号和单调递增的本地请求序号；新请求先取消上一请求，响应只有在序号仍为当前值且页面范围一致时才能提交到界面状态。
- 服务端机制：Route Handler 和查询服务接收取消信号，并在数据库驱动支持时尽力取消正在执行的 SQL；客户端断开后不得继续组装或缓存页面结果。
- 防护底线：底层数据库查询未能及时停止时，服务端完成结果也视为过期；不得依赖请求完成顺序判断哪个结果有效。
- 验收要求：快速连续切换范围、浏览器后退/前进和跨页导航时，不得出现旧数字闪回、旧证据定位、新旧加载状态错位或取消错误提示。

## D-137：服务端请求标识与客户端并发序号职责分离

- 状态：已确认
- 日期：2026-08-19
- 决策：每次进入服务端的查询都由服务端生成唯一 `request_id`，用于日志、性能、错误和数据库调用的端到端关联；客户端传入值不得作为权威服务端请求标识。
- 响应规则：成功与失败响应均在 `X-Request-Id` 响应头返回该值；业务错误和基础设施错误同时在既有错误正文的 `request_id` 字段中返回同一个值。
- 契约兼容：成功响应正文不新增 `request_id` 字段，避免修改 D-125 冻结的结果结构；需要排查时由响应头关联服务端记录。
- 客户端边界：D-136 的本地请求序号仅用于取消、判断响应是否过期和保护页面状态，不用于服务端追踪，也不替代 `request_id`。
- 传播要求：查询服务、数据访问层及后续真实模型调用沿用同一服务端 `request_id`，但不得把它用作业务主键、证据标识或缓存键。

## D-138：查询日志只记录最小必要的结构化运行元数据

- 状态：已确认
- 日期：2026-08-19
- 决策：第一闸门的查询日志采用结构化字段，只记录排查正确性、性能和可用性所需的最小运行元数据，不记录完整业务请求或响应内容。
- 允许字段：时间、环境、`request_id`、`QuestionType`、规范化范围代码、数据发布版本、比较版本、计算版本、耗时、读取或返回行数、HTTP 状态、错误类型及是否被取消。
- 禁止字段：完整查询结果、证据正文或值、固定 AI 示例正文、Cookie、Authorization、数据库连接信息、原始 IP、完整请求头，以及后续自由问答的自然语言全文。
- 脱敏要求：不可预期文本、异常对象和第三方错误在写入日志前必须过滤凭据、连接串、会话标识和用户输入；生产环境不得默认打印请求体或响应体。
- 关联边界：运行记录只通过 D-137 的 `request_id` 关联，不建立访客画像，不以日志替代第二闸门的独立限流和费用计数。

## D-139：查询明细日志保留 14 天，匿名聚合指标保留 90 天

- 状态：已确认
- 日期：2026-08-19
- 决策：D-138 定义的请求级结构化查询明细日志自生成起最多保留 14 天，到期自动删除，不复制到长期归档、分析仓库或备份文件中。
- 聚合指标：不含 `request_id`、范围代码、版本标识或其他请求级关联字段的匿名性能汇总可以保留 90 天，用于观察请求量、P50/P95/P99 延迟、错误率、超时率和取消率趋势。
- 禁止用途：明细日志和聚合指标不得用于访客画像、行为广告、业务结果分析或重建用户查询内容。
- 环境一致性：正式环境必须配置并验证自动过期策略；本地开发日志随开发环境清理，不得因调试需要上传完整请求或响应。
- 例外规则：如安全事件或法律要求需要延长特定记录，必须单独形成决策，限定范围、期限、访问权限和删除时间；不得默认扩大全量保留。

## D-140：运行告警区分数据正确性与性能可用性

- 状态：已确认
- 日期：2026-08-19
- 决策：第一闸门建立两级运行告警。数据正确性或运行兼容性失败立即触发严重告警；一般查询错误、超时和延迟仅在达到明确窗口阈值时触发性能可用性告警。
- 严重告警：活动正式数据版本出现任何 `RECONCILIATION_FAILED`、启动或就绪检查发现数据库结构版本不兼容、活动发布版本缺失或发布完整性校验失败时立即触发，不等待累计样本。
- 性能告警：在至少 20 次查询的滚动 5 分钟窗口内，非用户取消的错误与超时合计比例超过 5%时触发；正常热查询 P95 连续 10 分钟超过 1 秒时触发。
- 恢复规则：指标恢复到阈值内后明确标记告警恢复；严重告警必须在重新校验通过后恢复，不因单次成功请求自动关闭。
- 数据最小化：告警内容只包含环境、告警类型、时间窗口、聚合指标、活动版本和示例 `request_id`，不得附带完整请求、响应、证据或身份信息。

## D-141：存活检查与就绪检查分离

- 状态：已确认
- 日期：2026-08-19
- 决策：正式应用提供两个职责独立的健康端点：`GET /api/health/live` 只确认应用进程能够响应；`GET /api/health/ready` 判断实例是否具备安全处理业务查询的条件。
- 就绪条件：数据库可建立受限连接、已执行的 SQL 结构版本与应用兼容、存在且只能解析到一个活动数据发布版本，并且该发布版本的状态为已验证可用。
- 流量行为：存活失败时重启或替换实例；就绪失败时立即停止向该实例分配业务流量，但不以重启掩盖数据库结构或数据发布错误。
- 查询边界：就绪检查使用轻量元数据查询，不执行核心 9 题、完整业务聚合或模型调用，不产生业务查询日志和费用计数。
- 信息披露：公开响应只返回状态、检查时间和通用错误类别，不暴露连接串、数据库地址、SQL、物理表名、结构历史、数据清单或内部异常堆栈。

## D-142：第一闸门先通过本地与 CI，再部署公开测试环境

- 状态：已确认
- 日期：2026-08-19
- 决策：正式核心纵向切片先在可重复的本地环境和 CI 临时 PostgreSQL 中完成实现与自动验收；在这些检查全部通过前，不把公开测试环境部署视为第一闸门的有效验收路径。
- 本地要求：开发者能够从空数据库执行显式 SQL 迁移、发布正式演示数据包、启动应用并重复运行核心查询与页面测试，不依赖手工修改数据库。
- CI 要求：每次相关变更在隔离的临时 PostgreSQL 上从零执行结构迁移和数据发布，并验证发布完整性、核心 9 题、契约、精度、数据访问边界及必要页面行为。
- 部署条件：本地与 CI 全部通过后才部署公开测试环境；公开环境继续执行迁移兼容、活动发布、就绪检查和人工桌面验收，不以部署成功替代业务验收。
- 故障归类：本地与 CI 同样失败时优先判定为代码、结构或数据包问题；仅公开环境失败时再检查托管平台、网络、密钥、资源或部署配置。

## D-143：各环境锁定同一 PostgreSQL 主版本并保持可移植

- 状态：已确认
- 日期：2026-08-19
- 决策：本地、CI 和公开环境使用同一明确锁定的 PostgreSQL 主版本；具体主版本在核对 PostgreSQL 官方支持周期与候选托管平台后另行确认。
- 补丁策略：同一主版本内允许按安全和缺陷修复需要升级补丁版本，但必须先通过迁移、数据发布、核心 9 题和精确数值回归，不因镜像或托管平台默认值静默升级主版本。
- 可移植边界：第一闸门只依赖标准 PostgreSQL 类型、约束、索引、事务和 SQL 能力，不依赖托管厂商专有扩展、专有 API 或不可导出的数据结构。
- 环境校验：应用就绪检查和发布流程验证实际数据库主版本符合项目锁定值；不兼容时拒绝就绪和数据激活。
- 变更治理：未来升级主版本必须在隔离环境完成前向迁移、结果一致性和回滚演练，并形成独立决策，不在常规部署中自动发生。

## D-144：数据库主版本锁定为 PostgreSQL 18

- 状态：已确认
- 日期：2026-08-19
- 决策：本地、CI 和公开环境统一锁定 PostgreSQL 18 主版本；首次实现以 PostgreSQL 18.4 作为补丁基线。
- 官方依据：截至 2026-08-19，PostgreSQL 18.4 是 18 系列当前正式补丁版本，18 系列由 PostgreSQL 社区支持至 2030 年 11 月；PostgreSQL 19 尚处 Beta 阶段，不进入正式基线。
- 补丁更新：后续保持在 18.x，并遵循官方建议及时使用当前补丁版本；每次更新先执行 SQL 迁移、数据发布、核心 9 题、精度、性能和回滚验证。
- 配置要求：本地容器、CI 服务和托管数据库均显式声明 18 主版本；不得使用 `latest` 等可能静默跨主版本的标签或默认配置。
- 参考：PostgreSQL 官方版本政策 <https://www.postgresql.org/support/versioning/>；18.4 发布说明 <https://www.postgresql.org/docs/release/18.4/>。

## D-145：公开测试环境采用 Vercel Hobby 与 Neon Free

- 状态：已确认
- 日期：2026-08-19
- 决策：第一闸门通过本地与 CI 后，公开测试环境使用 Vercel Hobby 托管 Next.js 应用，使用 Neon Free 托管 PostgreSQL 18。
- 使用边界：该组合仅用于个人、非商业的作品演示与验收，不宣称生产 SLA、高可用或持续在线保证；用途变化为商业服务前必须重新核对平台条款并形成升级决策。
- 免费额度：项目必须监测 Vercel 计算、调用和传输额度，以及 Neon 计算、存储和传输额度；免费额度接近上限时优先停止非必要能力，不自动产生付费资源。
- 可用性披露：Neon 闲置缩容后的首次连接、Vercel 冷启动或免费额度暂停可能导致短时延迟或不可用；这些情况不得被包装为生产级可用性。
- 数据与迁移：Neon 只作为标准 PostgreSQL 18 托管环境，继续执行项目显式 SQL 迁移和 D-128 数据发布流程，不使用专有数据 API、Auth 或扩展替代既有架构。
- 运维缺口：Vercel Hobby 的平台日志保留能力不足以单独满足 D-139，14 天查询明细、90 天匿名指标和 D-140 告警必须另行实现并验收。
- 官方参考：Vercel Hobby <https://vercel.com/docs/plans/hobby>；Neon Pricing <https://neon.com/pricing>；Neon PostgreSQL 18 GA <https://neon.com/blog?a=3d8a2e31-3b76-4ca7-8352-a82831584c2b>。

## D-146：Vercel 计算与 Neon 数据库必须同区域部署

- 状态：已确认
- 日期：2026-08-19
- 决策：公开测试环境的 Vercel 服务端函数与 Neon PostgreSQL 必须部署在同一地理区域，并在项目配置中显式声明，不使用可能产生跨区访问的平台默认区域。
- 目标：减少数据库往返延迟、跨区网络故障和潜在网络费用，使 D-135 的热请求 P95 目标具备可验证基础。
- 验收：部署检查记录应用区域和数据库区域；两者不一致时就绪检查失败，不得继续公开验收。
- 迁移：未来变更区域时，应用与数据库作为同一变更批次迁移，并重新执行连接、迁移、数据发布、核心 9 题、性能和故障回退验证。
- 待确认项：具体区域代码在核对 Vercel 与 Neon 当前官方区域列表后单独确认，不依据记忆或平台默认值填写。

## D-147：公开测试环境部署在新加坡区域

- 状态：已确认
- 日期：2026-08-19
- 决策：公开测试环境的 Vercel 服务端函数显式配置为 Singapore `sin1`，Neon PostgreSQL 项目创建在 AWS Asia Pacific Singapore `aws-ap-southeast-1`。
- 选择依据：两个平台当前均正式支持新加坡区域，满足 D-146 的同区域约束；相较欧美区域更接近国内求职展示的主要访问者。
- 排除项：不选择 Vercel Hong Kong `hkg1`，因为 Neon 当前没有对应香港数据库区域；不得以地理名称相近替代实际区域代码核对。
- 固定性：Neon 项目区域创建后不可直接修改；配置错误时创建新项目并按 D-128 迁移和重新发布数据，不在旧项目内伪造区域切换。
- 验收：部署产物与运维记录必须显示 `sin1` 和 `aws-ap-southeast-1`，并实测连接延迟、冷启动、核心 9 题和热请求 P95。
- 官方参考：Vercel Regions <https://vercel.com/docs/regions>；Neon Regions <https://neon.com/docs/introduction/regions>。

## D-148：运行时使用池化只读连接，迁移与发布使用管理直连

- 状态：已确认
- 日期：2026-08-19
- 决策：Vercel 运行时只使用 Neon PgBouncer 池化端点和只读应用凭据；显式 SQL 迁移与 D-128 数据发布使用不进入 Web 应用环境的管理直连端点和独立高权限凭据。
- 运行时权限：应用连接只允许读取已激活的发布元数据和业务事实，不具有建表、改表、授权、写入候选发布、切换活动版本或修改历史数据的权限。
- 管理权限：迁移和发布只在受控本地或 CI 管理任务中运行；管理连接串不得配置到 Vercel 运行时、浏览器变量、预览页面或客户端包。
- 池化约束：运行时 SQL 显式限定 schema，不依赖会话级 `search_path`、`SET`、临时表、会话锁、`LISTEN/NOTIFY` 或 SQL 级 `PREPARE`；事务保持短小并在请求结束前释放。
- 连接校验：部署与就绪检查验证运行时实际连接到带 `-pooler` 的池化端点；迁移和发布命令拒绝池化端点，防止在 PgBouncer 事务模式下执行管理操作。
- 官方参考：Neon Connection Pooling <https://neon.com/docs/connect/connection-pooling>。

## D-149：数据库采用结构迁移、数据发布和运行时只读三个角色

- 状态：已确认
- 日期：2026-08-19
- 决策：第一闸门至少建立 `schema_migrator`、`data_publisher` 和 `app_reader` 三个独立 PostgreSQL 角色，不共用账号、密码或连接串。
- `schema_migrator`：通过直连执行已审查的 SQL 迁移，拥有所需 DDL、约束、索引、视图和授权管理权限；不作为 Web 运行时或日常数据发布账号。
- `data_publisher`：通过直连向候选发布区导入版本化数据、执行发布校验并原子切换活动版本；不能创建、修改、删除数据库结构或改变角色授权。
- `app_reader`：通过 Neon 池化端点只读取活动发布元数据、授权业务事实和必要健康信息；不能写入、激活版本、执行 DDL、读取候选或未发布数据。
- 默认拒绝：三类角色仅授予明确列出的最小权限，撤销 `public` schema 和默认对象上的隐式写权限；后续新增表或序列不自动扩大既有角色权限。
- 凭据隔离：Vercel 运行环境只配置 `app_reader`；迁移与发布凭据分别存放于受控管理环境，轮换任一角色凭据不要求同步更换其他角色。

## D-150：Vercel Preview 不得连接公开测试数据库

- 状态：已确认
- 日期：2026-08-19
- 决策：任何 Vercel Preview 部署不得获得或使用公开测试环境 Neon 项目的连接串，只能连接为该代码变更隔离的临时数据库分支。
- 隔离要求：临时分支从已验证基线创建，在其中独立执行待验收 SQL 迁移和演示数据发布；预览环境只获得该分支的 `app_reader` 池化连接。
- 失败关闭：未成功创建、迁移、发布或绑定隔离数据库时，预览部署必须显示明确的环境不可用状态或直接失败，不得回退到公开测试数据库、原型 JSON 或内存假数据。
- 权限边界：公开测试环境的 `schema_migrator`、`data_publisher` 和 `app_reader` 凭据均不得暴露给 Preview；临时分支凭据仅在对应预览生命周期内有效。
- 验收边界：Preview 用于人工界面检查，不替代 D-142 的本地与 CI 必需验收，也不得激活公开测试数据版本。

## D-151：每个开放 Pull Request 使用独立临时 Neon 分支

- 状态：已确认
- 日期：2026-08-19
- 决策：每个需要数据库支持的开放 GitHub Pull Request 最多对应一个名为 `preview-pr-<编号>` 的 Neon 临时分支；同一 PR 的后续提交复用并更新该分支，不为每次部署新建分支。
- 初始化：临时分支从已验证公开测试基线创建，随后执行该 PR 的全部前向 SQL 迁移、演示数据发布和必要自动校验；任何步骤失败都不向 Preview 注入连接信息。
- 并发上限：同时存在的 PR 预览数据库最多 5 个。达到上限时继续运行本地与 CI 验收，但不创建第 6 个分支，也不复用其他 PR 或公开测试环境的数据库。
- 清理：PR 合并或关闭后立即撤销 Preview 连接信息，并在 24 小时内删除对应 Neon 分支及临时角色；定期清理任务处理事件遗漏产生的孤儿分支。
- 权限与命名：自动化只允许管理 `preview-pr-` 前缀分支，删除前再次核对 PR 状态、分支 ID 和项目 ID；不得通过模糊匹配删除公开测试主分支。
- 验收：并发创建、同 PR 更新、上限拒绝、合并与关闭清理、孤儿回收和误删防护均须自动测试。

## D-152：GitHub Actions 编排 CI 与预览数据库，Vercel 只负责构建部署

- 状态：已确认
- 日期：2026-08-19
- 决策：GitHub Actions 是代码检查、临时 Neon 分支生命周期、SQL 迁移、演示数据发布、自动校验和清理的唯一自动化编排入口；Vercel 不持有数据库管理凭据，也不自行执行迁移或数据发布。
- 固定顺序：代码与静态检查 → 临时 PostgreSQL 集成测试 → 创建或更新 `preview-pr-<编号>` → 执行迁移与数据发布 → 核心 9 题及就绪校验 → 向 Vercel 提供该预览的只读池化连接 → 触发构建与 Preview 部署。
- 失败行为：任一前置步骤失败时不触发新的 Preview 部署，不注入连接信息，并保留清晰的 GitHub 检查结果；不得让 Vercel 使用上一次成功部署的数据库连接冒充本次通过。
- 凭据边界：Neon 管理 API、`schema_migrator` 和 `data_publisher` 凭据只存在于 GitHub 受保护环境；Vercel Preview 只接收对应临时分支的 `app_reader` 池化连接。
- 清理职责：PR 合并、关闭及定期孤儿扫描均由 GitHub Actions 执行 D-151 清理；Vercel 部署删除不能替代数据库分支和临时角色删除。
- 生产边界：公开测试环境迁移、数据发布和 Production 部署使用独立受保护工作流，不复用 PR Preview 的临时凭据或未经批准的自动触发条件。

## D-153：外部 Fork Pull Request 只能运行无密钥检查

- 状态：已确认
- 日期：2026-08-19
- 决策：来自外部 Fork 的 Pull Request 只运行不需要仓库或环境密钥的静态检查、单元测试和使用临时本地 PostgreSQL 的隔离集成测试；不得访问 Neon、Vercel、发布系统或任何长期凭据。
- Preview 资格：只有仓库内可信分支，或在代码与工作流变更经过人工审核后明确批准的提交，才能创建 Neon 临时分支并触发 Vercel Preview。
- 工作流禁令：持有密钥的 `pull_request_target`、`workflow_run` 或其他高权限工作流不得直接检出、构建或执行未经信任的外部 PR 代码、脚本、依赖安装钩子和复合 Action。
- 权限最小化：外部 PR 工作流显式设置只读仓库权限，不授予 `id-token`、`deployments`、`packages` 或写权限；第三方 Action 固定到已审核的不可变提交版本。
- 人工批准边界：批准只对已审核的具体提交 SHA 有效；外部 PR 新增提交后自动失效，必须重新审核，不继承上一次的密钥访问资格。
- 失败关闭：无法可靠判断来源、提交 SHA 或权限上下文时，不创建 Preview、不读取密钥，并返回明确的安全跳过状态。

## D-154：公开测试环境发布必须人工批准并绑定提交 SHA

- 状态：已确认
- 日期：2026-08-19
- 决策：代码合并到 `main` 后不自动修改公开测试数据库或触发 Vercel Production 部署；发布必须通过受保护的 GitHub Environment 人工批准，并绑定一个已通过全部必需检查的具体提交 SHA。
- 批准范围：批准内容明确列出提交 SHA、SQL 迁移清单、演示数据包版本与校验和、计算版本和目标环境；任一内容变化都使原批准失效并要求重新检查与批准。
- 执行身份：只有独立 Production 发布工作流可以读取公开测试环境的 `schema_migrator`、`data_publisher`、Neon 管理和 Vercel 部署凭据；PR 与普通 `main` 检查工作流无权读取。
- 并发控制：同一时间最多执行一个公开测试发布；后发任务不得取消或覆盖正在迁移、发布或激活数据的任务，也不得将不同 SHA 的产物混合到同一发布。
- 审计输出：工作流记录批准时间、执行人、提交 SHA、迁移版本、数据发布版本、校验结果、Vercel 部署标识和最终状态，但继续遵守 D-138 与 D-139 的敏感信息和保留限制。
- 失败关闭：缺少批准、SHA 不一致、检查过期、产物校验和不符或目标环境不明确时立即停止，不使用“最新 main”或上一次成功产物作为隐式替代。

## D-155：公开发布采用扩展—部署—激活顺序

- 状态：已确认
- 日期：2026-08-19
- 决策：公开测试发布固定执行“向后兼容的扩展迁移 → 导入并校验候选数据 → 部署可同时读取旧活动数据与新候选结构的应用 → 存活与就绪检查 → 原子激活候选数据 → 核心 9 题与页面冒烟复验”。
- 兼容窗口：新应用在数据激活前必须能完整读取旧活动版本；激活后不得要求页面刷新、重新构建或使用隐式最新版本才能取得正确结果。
- 迁移禁令：同一次发布不得删除或重命名仍被上一应用版本使用的表、列、类型、视图或权限，不得原地改变既有字段的业务语义、精度或单位。
- 回滚路径：应用部署失败时保留旧应用与旧活动数据；激活后复验失败时先将活动指针切回已验证旧数据，并回滚到兼容旧结构的上一应用部署。已执行的扩展结构可以暂时保留，不紧急执行破坏性降级。
- 激活原子性：候选数据只有在清单、校验和、引用完整性、业务恒等式、核心 9 题和结构兼容全部通过后，才允许由 `data_publisher` 在单个事务中切换活动发布指针。
- 验收记录：发布结果记录每一步开始与结束状态、旧新版本、部署标识、激活事务和复验结论；任何中间失败都不得被标记为发布成功。

## D-156：第一闸门完全禁止破坏性数据库迁移

- 状态：已确认
- 日期：2026-08-19
- 决策：第一闸门内的正式 SQL 迁移只允许新增向后兼容对象或约束，不允许删除、重命名、缩窄类型、降低精度、收紧会破坏既有数据的约束，也不允许原地改变表、列、枚举或视图的业务语义。
- 弃用方式：不再使用的对象保留原结构并显式标记弃用，停止新代码写入和引用；不得通过兼容视图之外的隐式别名让新旧语义混用。
- 清理条件：只有第一闸门通过、旧 Vercel 部署回滚窗口明确结束、静态引用扫描和运行校验均证明无依赖后，才可提出独立清理迁移。
- 清理审批：破坏性清理必须单独列出目标对象、数据影响、备份与恢复方法、应用最低兼容版本和不可逆风险，并通过 D-154 的人工批准；不得夹带在功能迁移中。
- 回滚原则：第一闸门回滚以切换应用部署和活动数据版本为主，不依赖执行逆向破坏性 SQL 恢复数据库结构。

## D-157：正式 TypeScript monorepo 按应用、契约、领域和数据库分层

- 状态：已确认
- 日期：2026-08-19
- 决策：正式工程在同一仓库内采用固定目录边界，不拆分独立部署服务。
- `apps/web`：唯一 Next.js App Router 应用，包含页面、组件、服务端渲染、Route Handlers、健康端点和传输适配；不得直接执行 SQL 或复制领域计算。
- `packages/contracts`：保存 D-125 V1.0 查询、结果、证据、业务错误和基础设施错误的 TypeScript 类型、运行时校验器及契约测试夹具；不得依赖 Next.js 或数据库驱动。
- `packages/domain`：保存精确十进制值对象、聚合规则、范围语义、证据组装和其他纯业务逻辑；不得依赖 React、Next.js、HTTP 或 PostgreSQL 客户端。
- `packages/db`：唯一 SQL 执行层，保存参数化查询、事务、数据库行到领域记录的转换和连接适配；可以依赖 `contracts` 与 `domain`，反向依赖禁止。
- `database/migrations`：保存 D-131 的显式 SQL 迁移、结构版本和角色授权；`database/releases` 保存版本化数据发布清单、校验和及装载定义。
- 既有资产：`prototype/` 继续作为可丢弃交互参考，`scripts/` 与 `data/` 继续保存已审计数据生成和基线资产，`docs/` 继续作为规范来源；不得把原型代码直接复制为正式业务实现。
- 依赖方向：`apps/web → contracts/domain/db`，`db → contracts/domain`，`domain` 与 `contracts` 保持框架无关；通过静态检查阻止反向或循环依赖。

## D-158：第一闸门使用原生 workspace，不引入额外 monorepo 编排器

- 状态：已确认
- 日期：2026-08-19
- 决策：第一闸门只使用包管理器原生 workspace、仓库根目录单一锁文件和根级脚本管理 `apps/web` 与三个内部包，不引入 Turborepo、Nx 或其他任务图与远程缓存编排器。
- 脚本边界：根级脚本统一提供安装、格式、静态检查、类型检查、单元测试、集成测试、构建、迁移和数据发布入口；各 workspace 保留自身明确脚本，不通过隐式插件推断任务。
- 可复现性：CI 与本地使用冻结锁文件安装，workspace 内部依赖使用明确的 workspace 引用，禁止从公共注册表意外解析同名内部包。
- 缓存边界：第一闸门只使用包管理器、Next.js 和 CI 的透明本地缓存；缓存不得改变测试覆盖、迁移顺序或发布结果，也不得包含数据库凭据和数据发布管理连接。
- 重评条件：只有包数量、构建耗时或任务依赖复杂度形成可测量瓶颈时，才评估额外编排器，并单独记录收益、缓存正确性和 Vercel 影响。

## D-159：monorepo 使用精确锁定的 pnpm 10

- 状态：已确认
- 日期：2026-08-19
- 决策：正式 TypeScript monorepo 使用 pnpm 10；根 `package.json` 的 `packageManager` 字段锁定经验证的精确 pnpm 10.x 版本，Vercel、本地和 CI 均以该字段选择工具版本。
- 锁文件：仓库只提交根目录一个 `pnpm-lock.yaml`，所有 workspace 共用；禁止并存 `package-lock.json`、`yarn.lock`、`bun.lock` 或子目录锁文件。
- 安装规则：CI、Preview 和 Production 使用冻结锁文件安装，锁文件与 `package.json` 不一致时失败，不在构建期间自动更新依赖解析结果。
- 内部依赖：`apps/web` 与内部包之间统一使用 `workspace:*`，确保同名包只能解析到本仓库 workspace；发布到公共注册表不在第一闸门范围内。
- 依赖图：启用 workspace 循环依赖失败检查；`contracts` 与 `domain` 不得依赖 `db` 或 `apps/web`，继续执行 D-157 的单向依赖约束。
- Vercel 配置：不使用无法保证版本的通用 `pnpm install` 覆盖命令；以 `packageManager`、Corepack 和锁文件为版本权威来源。
- 官方参考：Vercel Package Managers <https://vercel.com/docs/package-managers>；pnpm Workspaces <https://pnpm.io/workspaces>。

## D-160：正式工程统一使用 Node.js 24.x

- 状态：已确认
- 日期：2026-08-19
- 决策：本地开发、GitHub Actions、Vercel 构建和 Vercel Node.js 运行时统一使用 Node.js 24.x，不依赖平台默认版本自动选择主版本。
- 版本声明：根 `package.json` 使用 `engines.node = ">=24.15.0 <25"`，仓库提供本地版本文件，GitHub Actions 显式选择满足该范围的 24.x，Vercel 项目设置和构建日志均验证实际主版本为 24；最低补丁线由 D-169 收紧。
- 补丁策略：同一 24.x 主版本内跟随当前安全与缺陷修复补丁，但更新后必须重新执行冻结安装、类型检查、测试、构建、数据库集成、核心 9 题和 Preview 验收。
- 主版本治理：不得使用 `>=24`、`node`、`latest` 或其他允许静默跨到 Node.js 26 的宽范围；未来主版本升级单独验证 pnpm、Next.js、数据库驱动、精确十进制和 Vercel 兼容性。
- 运行时边界：所有需要 PostgreSQL、精确数值或服务端密钥的代码使用 Node.js Runtime，不切换到 Edge Runtime。
- 官方参考：Vercel Supported Node.js Versions <https://vercel.com/docs/functions/runtimes/node-js/node-js-versions>；Node.js Releases <https://nodejs.org/en/about/previous-releases>。

## D-161：首版 Web 框架与编译器使用精确版本

- 状态：已确认
- 日期：2026-08-19
- 决策：正式 Web 应用首次实现精确锁定 Next.js `16.3.1`、React `19.2.8`、React DOM `19.2.8` 和 TypeScript `7.0.2`，不使用 `^`、`~`、`latest` 或其他会使安装结果自动漂移的版本范围。
- 编译器边界：TypeScript 7.0.2 作为全部 workspace 的权威命令行类型检查器；`apps/web` 直接依赖该版本并通过 Next.js `experimental.useTypeScriptCli` 执行构建期检查。需要 JavaScript 程序化 TypeScript API 的 ESLint 工具链使用 D-184 的 TypeScript 6 兼容包，不以兼容 API 替代权威 TypeScript 7 检查。
- 同版约束：React 与 React DOM 必须保持完全相同的版本；类型包、Next.js 适配代码和服务端构建均以该组合验证。
- 安装权威：根 `package.json`、workspace 清单和唯一 `pnpm-lock.yaml` 共同定义可复现安装；CI、Preview 与 Production 继续按 D-159 使用冻结锁文件，版本声明与锁文件不一致时失败。
- 升级治理：上述框架或编译器的任何升级均使用独立变更提交，核对官方变更记录，并重新通过格式、静态检查、类型检查、单元测试、PostgreSQL 集成、正式构建、核心 9 题与页面验收后才能合并。
- 首次引导：创建工程时不得让脚手架静默改写已确认版本；生成后立即核对实际安装版本、Node.js 24.x 运行时和 Next.js Node Runtime 边界。
- 官方包索引：Next.js <https://www.npmjs.com/package/next>；React <https://www.npmjs.com/package/react>；TypeScript <https://www.npmjs.com/package/typescript>。

## D-162：正式界面使用设计令牌与 CSS Modules

- 状态：已确认
- 日期：2026-08-19
- 决策：第一闸门的正式界面采用全局设计令牌、CSS Modules 和少量全局基础样式，不引入 Tailwind CSS、第三方组件库或运行时 CSS-in-JS。
- 设计令牌：颜色、字体、字号、行高、间距、圆角、阴影、层级、焦点状态和桌面布局阈值集中定义为可审查的 CSS 自定义属性；组件样式不得复制一套含义相同但数值不一致的常量。
- 模块边界：页面壳、导航、指标卡、图表、下钻表、AI 分析卡、证据侧栏和演示引导分别使用局部 CSS Module；仅重置、字体、设计令牌和跨页面基础语义类进入全局样式。
- 视觉基线：D-124 已验收的方案 B 是正式实现的视觉与交互参考，但原型 DOM、字符串模板和全局样式不得直接复制为正式组件；实现后继续按 1440px 与 1280px 桌面宽度逐项复验。
- 可访问性：颜色不是唯一状态信号；交互控件必须保留可见键盘焦点、语义状态和足够对比度，CSS 不得隐藏原生可访问名称或使表格阅读顺序与视觉顺序不一致。
- 变更边界：如后续确有重复组件、无障碍或交付效率瓶颈，需要引入组件库或新的样式工具，必须先证明其不会改变方案 B 的视觉、交互和依赖边界，并单独记录决策。

## D-163：URL 管理可复现业务状态，局部组件管理瞬时状态

- 状态：已确认
- 日期：2026-08-19
- 决策：第一闸门不引入 Redux、Zustand 或其他全局客户端状态库；影响确定性查询、证据范围、页面恢复或可分享结果的业务状态由 URL 表达，纯瞬时交互状态保留在最小范围的客户端组件内。
- URL 状态：页面视图、期间、比较口径、目的国、Budget/Actual/Forecast 或情景版本、归因因素和需要恢复的下钻定位使用稳定参数名，并归一化为 D-125 的 `QuestionType + AnalysisScope`。
- 校验与规范化：服务端首先使用 `packages/contracts` 校验 URL；无效值返回明确业务错误或跳转到唯一规范地址，不静默沿用旧状态、隐式默认版本或客户端缓存值。
- 历史记录：影响分析范围或页面的用户操作写入浏览器历史，使返回、后退和前进恢复同一有效地址；同一操作不得同时由多个状态源重复写入或触发循环导航。
- 瞬时状态：证据侧栏开关、当前表格展开集合、演示引导步骤、局部悬停和加载提示默认使用组件局部状态；只有既有验收明确要求跨页恢复的部分才提升到 URL。
- 并发一致性：继续执行 D-136 的取消、请求序号和范围一致性检查；响应只有在其规范化范围仍与当前 URL 完全一致时才能更新页面。
- 客户端边界：服务端组件负责读取和验证初始 URL、取得首屏确定性数据并输出静态结构；只有导航、展开、定位、证据侧栏和图表交互等必要区域使用客户端组件。

## D-164：首版分析图表使用 React 原生 SVG

- 状态：已确认
- 日期：2026-08-19
- 决策：第一闸门的月度趋势图和五因素瀑布图使用 React 渲染的原生 SVG，不引入 ECharts、Recharts、D3 或其他通用图表库，也不使用 Canvas 作为主要呈现层。
- 计算边界：比例尺、坐标、柱宽、连接线、标签避让和点击区域由无 DOM 依赖的纯函数计算，渲染组件只消费经过验证的图表视图模型；几何函数使用边界、零值、负值、极值和四舍五入样本进行单元测试。
- 数值权威：SVG 只呈现查询服务返回的确定性金额和标签，不在组件中重新聚合、归因或改变精度；图表、表格、证据对象和 AI 示例继续复用同一结果 ID 与范围。
- 交互要求：可点击数据标记使用明确的按钮语义或等价键盘操作，具备可见焦点和中文可访问名称；点击、Enter 与 Space 产生相同导航或因素选择结果。
- 可访问替代：每张图提供完整的中文标题、范围、口径、单位和摘要，并由页面上的语义表格或可读取明细提供相同核心数值；颜色不得作为区分 Actual、Forecast、Budget 或有利、不利的唯一方式。
- 视觉验收：继续按方案 B 的尺寸、标签、图例、顺序和交互区域在 1440px 与 1280px 宽度验收，不增加会改变业务理解的动画、三维效果或自动重排。
- 重评条件：只有后续图表类型、交互复杂度或无障碍维护成本形成可量化瓶颈时，才评估图表库，并单独验证包体、服务端渲染、视觉一致性和许可证影响。

## D-165：Zod 是 V1.0 运行时契约的唯一模式来源

- 状态：已确认
- 日期：2026-08-19
- 决策：`packages/contracts` 精确锁定 Zod `4.4.3`，以 Zod 模式定义 D-125 V1.0 的查询、结果、证据、业务错误和基础设施错误结构，并从模式推导 TypeScript 类型；不得另行维护一套等价的手写接口或枚举。
- 校验入口：URL 参数归一化结果、`POST /api/v1/query` 请求体、Route Handler 输出、客户端接收响应和契约测试夹具均使用同一导出模式；解析失败必须转换为冻结的业务错误或明确的基础设施错误，不向调用方暴露 Zod 内部英文问题列表。
- 联合类型：业务错误联合类型继续按 D-125 冻结，超时等基础设施错误保持独立；Zod 模式只能实现既有判别字段和成员，不得因库的默认行为新增、合并或重命名错误类型。
- 数据库边界：`packages/db` 对物理行执行数据库层校验和显式转换，将 PostgreSQL `numeric`、日期、枚举及可空字段转换为领域记录后，再由契约模式验证对外结果；数据库列名和驱动返回类型不得泄漏到 API 契约。
- 数值要求：金额、比率和高精度归因值继续按冻结契约使用十进制字符串或明确值对象传递，禁止由 Zod 转换成 JavaScript `number` 后再计算。
- 依赖边界：Zod 只作为 `contracts` 的直接运行时依赖，`domain` 不依赖 Zod；`web` 与 `db` 通过 `contracts` 导出使用模式，禁止安装不同版本形成多份模式实例。
- 升级治理：Zod 升级作为独立变更，必须重新通过全部契约快照、错误联合、未知字段、精确数值、核心 9 题和客户端响应校验测试。
- 官方参考：Zod <https://www.npmjs.com/package/zod>。

## D-166：数据库访问使用 node-postgres 与参数化原生 SQL

- 状态：已确认
- 日期：2026-08-19
- 决策：`packages/db` 精确锁定 `pg` `8.23.0` 和开发类型包 `@types/pg` `8.23.1`，作为第一闸门唯一 PostgreSQL 驱动；不引入 ORM、查询构建器或 Neon 专用运行时驱动。
- 连接可移植性：本地与 CI 使用相同驱动连接标准 PostgreSQL 18.4，公开 Vercel Node.js Runtime 使用同一驱动连接 D-148 的 Neon 池化只读端点；业务代码不得依赖 Neon 专有查询 API 或数据库扩展。
- 连接池：每个 Node.js 进程或暖实例复用单例 `pg.Pool`，设置明确的小型连接上限、连接获取超时、空闲回收和查询超时；不得为每次请求创建新连接池，也不得把 `PoolClient` 保存到请求之外。
- SQL 边界：全部 SQL 位于 `packages/db`，使用绑定参数和受控枚举映射；禁止字符串拼接用户输入、URL 参数、排序字段、表名或列名，继续执行 D-132 的独立数据访问层约束。
- 数值解析：保持 node-postgres 对 PostgreSQL `numeric` 的字符串返回行为，不注册将 `numeric`、`int8`、金额、汇率或归因值全局转换为 JavaScript `number` 的类型解析器；显式转换交给精确十进制领域类型。
- 事务规则：查询默认只读；需要一致快照时由 `packages/db` 显式开启只读事务，并确保成功、失败、取消和超时路径都释放客户端。迁移与数据发布继续使用隔离角色和独立工作流，不复用 Web 连接池。
- 类型边界：`@types/pg` 只提供驱动 API 类型，不把查询结果的泛型声明视为运行时保证；每个查询仍需验证行结构并显式映射到领域记录。
- 升级治理：驱动或类型包升级必须重新验证 PostgreSQL 18.4、Neon 池化连接、超时取消、连接释放、`numeric` 字符串行为、只读权限和并发查询测试。
- 官方参考：node-postgres <https://www.npmjs.com/package/pg>。

## D-167：领域十进制运算使用 80 位 decimal.js 上下文

- 状态：已确认
- 日期：2026-08-19
- 决策：`packages/domain` 精确锁定 `decimal.js` `10.6.0`，并创建项目专用的克隆 Decimal 构造器，统一使用 80 位有效数字和 `ROUND_HALF_UP`；其他包不得直接修改 decimal.js 全局配置或创建不同计算上下文。
- 精度依据：数据库高精度字段为 `numeric(50,24)`，80 位不是业务展示精度，而是在 50 位持久化精度之外为连续乘除、汇总进位和未来合法数据增长保留约 30 位计算余量。
- 输入边界：金额、汇率、单价、数量、重量、费率和归因值只能由经过格式校验的十进制字符串或同一项目 Decimal 实例构造；禁止以 JavaScript `number` 作为权威业务输入，也禁止通过隐式数值强制转换进入计算。
- 舍入边界：公式中间值、原子成本、因素贡献和汇总过程不提前量化；完整汇总后才按 D-015 使用 `ROUND_HALF_UP` 生成 4 位报告值和 2 位界面值，显示值不得反向参与计算或证据勾稽。
- 序列化：API、日志允许的数值元数据、测试夹具和数据库参数使用规范十进制字符串，不使用科学计数法，不丢失尾随精度含义，也不把 Decimal 实例直接跨进程序列化。
- 性能判断：第一闸门数据规模下，80 位相对 50 或 64 位的额外 CPU 与内存开销不构成可测量页面瓶颈；数据库与网络仍是主要延迟来源，不以降低精度换取未经证明的性能收益。
- 边界测试：必须覆盖数据库最大声明精度、正负值、零、连续乘除、大量求和、进位、半值舍入、4 位报告尾差和核心 9 题；若未来降低精度，必须以合法输入上界和测试证明仍有明确余量。
- 官方参考：decimal.js <https://www.npmjs.com/package/decimal.js>。

## D-168：TypeScript 单元与数据库集成测试统一使用 Vitest

- 状态：已确认
- 日期：2026-08-19
- 决策：正式 monorepo 精确锁定 Vitest `4.1.11`，作为 `contracts`、`domain`、`db` 和 Web 非浏览器逻辑的统一单元及集成测试运行器；覆盖率使用同版 `@vitest/coverage-v8`，第一闸门不并存 Jest。
- 运行环境：契约、领域计算、图表几何和数据库测试默认使用 Node 环境；只有确实依赖 DOM 的组件测试才启用单独浏览器模拟环境，禁止把数据库或领域测试放入 DOM 环境。
- 数据库真实性：`packages/db` 集成测试连接从零迁移并发布数据的临时 PostgreSQL 18.4，验证真实 SQL、类型转换、只读权限、事务、取消和连接释放；不得用 mock 替代这些数据库行为。
- 测试分层：快速单元测试与临时 PostgreSQL 集成测试使用不同命令和明确的文件命名，但共享根级配置、路径别名、固定时区、确定性随机种子和统一报告格式。
- 覆盖率规则：覆盖率只衡量可执行程序代码，不把迁移 SQL、生成数据和冻结夹具计入分母；重点阈值分别设置在 `contracts` 判别联合、`domain` 精确计算和 `db` 行映射，而不是仅设一个可被大量简单文件稀释的仓库总百分比。
- 快照边界：快照适用于冻结契约结构、错误响应和少量稳定视图模型，不允许用大面积页面快照替代数值、范围、证据引用、排序和可访问行为断言。
- 失败诊断：CI 保留机器可读测试与覆盖率结果，但测试输出不得包含数据库凭据、完整业务响应或 D-138 禁止记录的内容。
- 升级治理：Vitest 与覆盖率插件保持同版升级，并重新验证 Node.js 24、TypeScript 7、ESM、workspace 路径、并发隔离和覆盖率稳定性。
- 官方参考：Vitest <https://www.npmjs.com/package/vitest>。

## D-169：Node.js 24.x 最低版本收紧为 24.15.0

- 状态：已确认
- 日期：2026-08-19
- 决策：在 D-160 保持 Node.js 24 主版本不变的前提下，正式工程最低运行版本收紧为 `24.15.0`，根 `package.json` 使用 `engines.node = ">=24.15.0 <25"`；本地、CI、Vercel 构建与运行均不得使用更低版本。
- 直接原因：第一闸门采用当前 jsdom `30.0.1` 作为 DOM 组件测试环境，该版本要求 Node.js `^24.15.0` 或更高的受支持主版本；不为了兼容旧 Node 24 补丁线而停留在 jsdom 29。
- 版本文件：仓库本地版本文件和 CI 设置使用经验证的具体 Node 24 补丁版本，该补丁不得低于 24.15.0；Vercel 仍选择 Node 24.x，并在构建日志中执行版本断言。
- 补丁升级：继续按 D-160 在 24.x 内跟进安全与缺陷修复补丁，先通过冻结安装、类型检查、测试、构建、数据库集成、核心 9 题和 Preview 验收；最低版本可以随验证结果提高，不得降低到 24.15.0 以下。
- 失败关闭：安装、测试或构建检测到不满足范围的 Node.js 时立即失败，不依赖包管理器警告后继续运行。
- 官方参考：jsdom <https://www.npmjs.com/package/jsdom>。

## D-170：客户端交互组件使用 Testing Library 行为测试

- 状态：已确认
- 日期：2026-08-19
- 决策：`apps/web` 精确锁定 React Testing Library `16.3.2`、`@testing-library/user-event` `14.6.5` 和 jsdom `30.0.1`，在 D-168 的 Vitest 运行器中测试需要 DOM 的客户端交互组件。
- 查询原则：测试优先通过可访问角色、中文名称、标签、文本和状态查询元素，禁止把 CSS 类名、内部组件层级、私有 state、hook 调用次数或实现细节作为主要断言。
- 交互方式：点击、键盘、焦点移动、输入和关闭操作统一通过 user-event 执行；只有浏览器不产生对应用户交互的底层事件才允许直接触发事件。
- 必测组件：覆盖证据侧栏两种关闭方式与焦点恢复、下钻展开和准确定位、因素选择与清除、演示引导的启动/前进/跨页/完成/退出、导航历史触发后的局部状态，以及 SVG 数据标记的 Enter/Space 等价操作。
- 可访问断言：验证语义名称、`aria-expanded`、`aria-pressed`、对话框关系、键盘焦点和不可用状态；颜色、图形位置或截图不能替代这些行为断言。
- 服务端边界：Next.js 服务端组件、Route Handler、数据库结果和跨页路由不强行放入 jsdom 模拟；分别由查询服务集成测试、Route Handler 契约测试和真实浏览器端到端测试覆盖。
- 样式边界：jsdom 不执行真实布局，因此不得用组件测试证明 1440px/1280px 无重叠、滚动、粘性定位或 SVG 实际尺寸；这些继续留给真实浏览器与视觉验收。
- 依赖治理：所需 `@testing-library/dom` 等直接同伴依赖在工程初始化时核对 React Testing Library 的官方兼容范围后精确锁定，并进入唯一锁文件；不得由不同 workspace 安装多份不一致版本。
- 官方参考：React Testing Library <https://www.npmjs.com/package/@testing-library/react>；user-event <https://www.npmjs.com/package/@testing-library/user-event>；jsdom <https://www.npmjs.com/package/jsdom>。

## D-171：真实浏览器端到端测试使用 Playwright

- 状态：已确认
- 日期：2026-08-19
- 决策：正式工程精确锁定 `@playwright/test` `1.62.1`，作为第一闸门唯一真实浏览器端到端测试工具；测试运行已构建的 Next.js 应用并连接从零迁移、发布的临时 PostgreSQL 18.4，不使用开发服务器、原型 JSON 或模拟查询 API 作为验收路径。
- PR 必过矩阵：每个可信 PR 在 Chromium 中分别以 1440×900 和 1280×800 视口执行完整核心流程，覆盖 D-123 的 V01—V12、核心 9 题对应页面结果、返回/后退/前进、跨页状态、因素筛选、下钻排序与展开、证据侧栏及准确定位、演示引导和窄宽度按钮标签无重叠。
- 跨浏览器边界：Firefox 在 `main` 合并检查或公开发布前执行核心导航、查询、下钻、证据和引导冒烟；WebKit 不作为第一闸门必过项。该范围不宣称完整跨浏览器支持，后续扩展必须单独验收。
- 数据真实性：测试通过公开页面和 `POST /api/v1/query` 使用真实确定性服务，禁止在浏览器层拦截并伪造业务成功响应；超时、错误和竞态专项可以使用明确标识的故障注入，但不得计作核心数字验收通过。
- 稳定定位：优先按角色、中文名称、标签和可见文本定位；仅对无法用用户语义稳定表达的图表数据点和范围容器使用审核过的 `data-testid`，不得依赖脆弱 CSS 路径或 DOM 层级。
- 失败产物：失败时保留截图、视频、trace、测试名称、提交 SHA、浏览器版本、视口和数据发布版本；产物不得包含数据库连接串、密钥、Cookie、完整响应正文或 D-138 禁止记录的信息。
- 视觉边界：自动测试断言重叠、可见性、滚动和关键布局尺寸，并保留少量稳定基线截图；像素差异不能替代数值、范围、证据和交互断言，字体或浏览器更新造成的基线变化必须人工审查。
- 并发规则：每个测试使用可复现只读数据，测试间不得共享可变浏览器状态；CI 并发数以临时数据库连接上限为约束，避免通过增加连接导致 Neon 或本地 PostgreSQL 假性超时。
- 官方参考：Playwright Test <https://www.npmjs.com/package/@playwright/test>。

## D-172：代码检查使用 ESLint，格式化使用 Prettier

- 状态：已确认
- 日期：2026-08-19
- 决策：正式 monorepo 精确锁定 ESLint `9.39.5`、`typescript-eslint` `8.67.0`、`eslint-config-next` `16.3.1` 和 Prettier `3.9.6`；ESLint 10.8.1 因 Next.js 传递插件 peer 不兼容被 D-184 否决。不引入 Biome 或 `eslint-plugin-prettier`。
- ESLint 配置：使用根级 flat config 统一检查所有 TypeScript workspace；通用规则覆盖潜在错误、未使用代码、Promise、导入边界和测试文件，仅 `apps/web` 启用 Next.js Core Web Vitals、React Hooks、React 与 JSX 无障碍规则。
- 格式职责：Prettier只负责 TypeScript、TSX、JavaScript、JSON、CSS、Markdown、YAML 等受支持文本的确定性格式化；生成数据、锁文件、冻结大夹具和第三方产物通过明确 ignore 管理，不使用全目录宽泛排除隐藏源代码。
- 命令边界：根级 `lint` 运行 ESLint 且警告视为失败，`format:check` 只检查格式不改写文件，`format` 是本地显式修复命令；CI 不自动修改源文件后继续通过。
- Next.js 边界：使用独立 ESLint CLI，不依赖已移除或平台隐式提供的 `next lint`；`eslint-config-next` 与 Next.js 保持相同版本，升级时一并验证 flat config 和规则变化。
- 规则治理：不得通过大面积 `eslint-disable`、关闭 Core Web Vitals 或跳过目录消除失败；必要例外必须缩小到具体行或文件，说明原因，并通过审查。
- 格式冲突：不安装 ESLint 格式化规则集或 `eslint-plugin-prettier`；ESLint 和 Prettier 分开运行，避免相互覆盖、重复报告和显著增加检查时间。
- 升级治理：相关工具升级作为独立变更，验证 Node.js 24、TypeScript 7 命令行检查、TypeScript 6 API 兼容层、ESM、Next.js 配置、workspace 边界和全仓库零警告后合并。
- 官方参考：ESLint <https://www.npmjs.com/package/eslint>；eslint-config-next <https://www.npmjs.com/package/eslint-config-next>；Prettier <https://www.npmjs.com/package/prettier>。

## D-173：全部 workspace 使用统一 TypeScript 严格基线

- 状态：已确认
- 日期：2026-08-19
- 决策：根级 TypeScript 基础配置为所有正式 workspace 启用 `strict`、`noUncheckedIndexedAccess`、`exactOptionalPropertyTypes`、`noImplicitOverride`、`useUnknownInCatchVariables` 和 `noFallthroughCasesInSwitch`，子项目不得关闭这些选项。
- 契约要求：D-125 的查询、结果、证据及错误判别联合必须使用穷尽性检查；新增成员而未处理全部分支时类型检查失败，不以 `default` 静默吞掉未知业务状态。
- 可空与缺失：数据库 `NULL`、JSON 字段缺失、显式 `null` 和可选属性分别建模，禁止通过宽泛联合或隐式 `undefined` 混为同一含义；数据库映射和 URL 归一化必须显式处理每种情况。
- 不安全类型：正式源码禁止无说明的显式 `any`、双重断言和非空断言；边界输入先视为 `unknown` 并通过 Zod、数据库行校验或类型守卫收窄。确有必要的例外必须局部化并说明不能安全推导的原因。
- 索引访问：数组、记录和查询结果索引始终可能缺失，必须通过长度、键存在性、领域不变量或显式错误处理收窄；不得用非空断言绕过下钻树、证据字典或因素数组边界。
- 测试边界：测试替身可以在单个辅助函数内使用经过说明的最小类型断言，但测试主体仍按严格配置编译；不得为测试目录整体关闭严格模式。
- CI 要求：根级 `typecheck` 覆盖全部 workspace，任何子项目未被根配置或项目引用纳入时失败；Next.js 构建不能替代独立类型检查。

## D-174：真实模型接入采用可插拔供应商架构

- 状态：已确认
- 日期：2026-08-19
- 决策：第二闸门的业务编排层不绑定 GPT、OpenAI SDK 或任何单一模型供应商；通过项目自有的 `ModelGateway` 中立接口调用模型。首个正式适配器使用 OpenAI，但替换模型或新增供应商时不得重写查询、证据、限流、费用熔断和页面业务流程。
- 模块边界：`packages/ai` 负责意图处理、确定性证据注入、输出约束、通用错误和供应商中立编排；具体供应商 SDK、请求字段、响应映射与供应商错误转换位于独立适配器包，例如 `packages/ai-provider-openai`。业务包不得直接导入供应商 SDK。
- 角色路由：模型选择按服务器端角色配置表达为 `provider + model`，至少区分意图解析、管理分析和回退角色；同一供应商内切换模型只修改受控配置，新增供应商需要实现适配器、能力声明和兼容测试。浏览器不得提交任意供应商名、模型名或供应商参数。
- 统一结果：网关向上层统一返回经过公共 Zod 模式验证的结构化输出、标准化用量、供应商与模型标识、供应商请求标识、结束原因、延迟、成本估算和标准化错误；供应商专有响应结构不得进入 D-125 的 V1.0 业务契约。
- 能力差异：结构化输出、推理参数、数据留存开关、安全标识及其他供应商专有能力由适配器内部处理，并通过明确的能力声明失败关闭；不得用静默降级产生未经公共模式校验的自由文本结果。
- 切换条件：供应商或模型只有在其适配器契约测试、固定 AI 评估集、数字与证据越界测试、超时与回退测试、费用估算和中文输出验收全部通过后才能进入正式路由；不得自动切换到未经验证的模型。
- 复现与审计：每份真实回答及其不可变证据快照记录供应商、模型、适配器版本、提示词版本、输出模式版本和价格版本，使同一业务范围下的结果可追溯；这些元数据不改变确定性查询结果和证据语义。
- 不变边界：匿名限流、预算保留与结算、固定示例回退、证据 ID 白名单和模型不得改写确定性数字等控制位于中立层，对所有供应商一致执行。

## D-175：冻结第一闸门工程工具链的剩余约束

- 状态：已确认
- 日期：2026-08-20
- 包管理器：D-159 的 pnpm 10 精确锁定为 `10.34.5`；全仓库使用 ESM、单一锁文件和 `workspace:*` 内部依赖，不混用 CommonJS 包配置。
- 类型依赖：初始工程精确锁定 `@types/node` `24.13.3`、`@types/react` `19.2.18`、`@types/react-dom` `19.2.4`、`@testing-library/dom` `10.4.1` 和 `@testing-library/jest-dom` `7.0.1`；不得因最新标签跨到 Node 26 类型。
- 编译配置：根级基础配置统一严格选项并设置 `skipLibCheck: false`；各 workspace 仅保留用途差异。Web 使用适合 Next.js 的 Bundler 模块解析，通过源码导出和 `transpilePackages` 消费内部包；第一闸门不启用 TypeScript project references。
- 脚本执行：管理脚本使用精确锁定的 `tsx` `4.23.12`，只承担迁移、数据发布和验证入口，不形成第二套应用运行时。
- 兼容性闸门：正式脚手架扩展前以 D-183 的空白最小工程验证精确组合。2026-08-20 已确认 TypeScript 7.0.2、TypeScript 6 API 兼容包 6.0.2、Next.js 16.3.1、React 19.2.8、ESLint 9.39.5、typescript-eslint 8.67.0、Vitest 4.1.11、jsdom 30.0.1 和 pnpm 10.34.5 通过严格安装、lint、独立类型检查、测试与生产构建；失败的 ESLint 10 组合不得恢复。
- 本地流程：第一闸门不引入 Husky、lint-staged 或 commitlint；格式、检查、测试和提交规范由根级命令、CI 与审查执行。

## D-176：冻结数据库执行、迁移与数据发布约定

- 状态：已确认
- 日期：2026-08-20
- 本地环境：Docker Compose 只启动 PostgreSQL 18.4 及必要健康检查，Node.js 与 pnpm 在宿主机运行；本地和 CI 都从空数据库执行同一迁移与发布流程。
- 迁移执行器：使用项目自有的 TypeScript 执行器顺序运行版本化 SQL；在 `_schema_migrations` 记录版本、SHA-256 和执行时间，使用 PostgreSQL advisory lock 防止并发，单项迁移事务化并在校验和变化时失败。第一闸门不提供破坏性自动 down。
- 物理结构：业务对象位于 `logiplan` schema，SQL 全部显式限定 schema；数据库使用 `snake_case`，边界映射到 API 字段。首版不用 PostgreSQL enum，以 `text` 配合受控 lookup 或 `CHECK` 表达冻结代码。
- 时间与标识：时间戳使用 UTC `timestamptz`，业务月份使用该月第一天的 `date`；稳定业务对象使用可复现业务标识，请求级随机标识使用 Node.js `crypto.randomUUID()`。
- 数据发布器：TypeScript 发布器创建候选版本，记录输入文件与规则版本 SHA-256，执行结构、范围、唯一性、勾稽、核心 9 题和完整性验证后原子切换活动指针；相同发布包重复执行必须幂等，失败候选不得可见。
- 运行连接：Web 进程复用连接池，初始上限为 2，连接超时 2 秒、空闲回收 5 秒、数据库 statement timeout 9 秒，外层应用硬超时继续为 10 秒；迁移与发布不复用该连接池。
- 权限与索引：第一闸门不启用 RLS，依靠 D-149 的独立角色、schema/table 只读授权和无写权限运行账户；索引只针对已证明的核心查询建立，并保存核心 SQL 的 `EXPLAIN` 基线，不预先堆叠猜测性索引。

## D-177：冻结第一闸门 Web、接口与安全基线

- 状态：已确认
- 日期：2026-08-20
- 渲染与地址：业务页采用动态服务端渲染和 `no-store`，不使用 ISR、PPR、React Query、SWR 或业务结果缓存；正式核心地址为 `/` 与 `/attribution`，URL 继续作为可复现业务状态权威。
- 查询接口：`POST /api/v1/query` 只接受 `application/json`，请求体不超过 16 KiB，拒绝未知字段；接口仅同源开放，不配置跨域访问。标准 loading、error 和 not-found 状态必须提供中文可操作反馈。
- React 边界：第一闸门关闭 React Compiler，不用 Server Actions 承载业务查询；服务端组件与 Route Handler 继续调用同一查询服务，客户端只承担已冻结的交互边界。
- 前端资产：使用系统字体或仓库内本地资产及受控 SVG，不依赖外部 CDN、字体、图标库；首版仅提供 `zh-CN`，内部代码必须通过穷尽中文映射展示。现有数据规模不引入列表虚拟化。
- 浏览器状态：第一闸门不设置 Cookie，不接入分析埋点、PWA 或 Service Worker；瞬时 UI 与第二闸门会话快照只保存明确允许的数据，不形成跨会话追踪。
- 安全响应：设置 nonce 型 CSP、`frame-ancestors 'none'`、`X-Content-Type-Options: nosniff`、严格 Referrer-Policy、最小 Permissions-Policy 和生产 HSTS；正式源码禁止 `dangerouslySetInnerHTML`。
- 环境变量：服务器端使用 Zod 在启动或首次使用前集中校验，提供无秘密的 `.env.example`；公开变量、只读运行凭据、迁移凭据、发布凭据和模型供应商密钥严格分离，管理密钥不得进入 Web 构建或浏览器包。

## D-178：冻结供应链安全、测试覆盖与 CI 流水线

- 状态：已确认
- 日期：2026-08-20
- 工作流供应链：GitHub Actions 引用固定提交 SHA 并使用最小 `permissions`；启用 Dependabot、依赖审查、CodeQL 和仓库秘密扫描，外部 Fork 继续执行 D-153 的无密钥路径。
- 覆盖率门槛：`contracts` 与 `domain` 的行、语句、函数和分支均不低于 95%；`db` 行与语句不低于 90%、分支不低于 85%；`web` 行与语句不低于 85%、分支不低于 80%；冻结业务不变量和核心 9 题对应逻辑要求 100% 覆盖。覆盖率不能替代真实数据库与浏览器验收。
- 可访问性：Playwright 使用精确锁定的 `@axe-core/playwright` `4.13.0` 扫描核心页面与主要打开状态，不允许 serious 或 critical 级问题；键盘、焦点、可读表格和中文语义仍由行为测试单独断言。
- 性能：使用固定脚本对构建后应用预热，再以并发 5 执行 100 次核心查询，记录 P50/P95/P99，正常热请求 P95 必须不超过 1 秒；冷启动与免费托管抖动单独报告，不混入数据库查询基线。
- 确定性：测试统一固定 UTC、`zh-CN` locale、随机种子、依赖版本、浏览器版本和数据发布版本，避免由机器环境产生数字或排序漂移。
- CI 顺序：冻结安装 → 格式检查 → lint → typecheck → 单元测试与覆盖率 → PostgreSQL 迁移/发布/权限/核心 9 题 → 构建 → Playwright/axe → 产物与安全检查；任何前置失败都阻止后续预览和发布。

## D-179：修订告警落地并冻结观测边界

- 状态：已确认
- 日期：2026-08-20
- 日志实现：服务端直接向 stdout 输出 D-138 允许的结构化 JSON，不引入 pino 或 winston；公开环境的请求级原始日志仅依赖 Vercel 可用保留能力，最长按 D-139 的 14 天边界处理，不另建长期日志仓库。
- 聚合观测：使用 Sentry Application Metrics 的可用免费额度承载 D-140 的匿名错误率、超时率和延迟聚合及告警；只接入服务端聚合指标，不启用 Session Replay，不上传自然语言输入、模型输出、证据正文、原始 IP 或业务明细。
- D-140 窄修订：第三方告警消息不得附带示例 `request_id`，因为第三方只保留不含请求级标识的聚合指标；排障所需 `request_id` 仅在 Vercel 请求级日志可用期内查询。活动数据勾稽、结构不兼容和活动发布缺失仍按 D-140 立即告警。
- 退化原则：Sentry 指标或告警服务不可用不得改变查询结果，但必须在健康与发布验收中显式报告观测能力缺失；不得因此扩大日志内容或延长请求数据留存。

## D-180：冻结首个真实模型适配器的初始配置

- 状态：已确认
- 日期：2026-08-20
- 适配器：D-174 的首个适配器精确锁定 OpenAI SDK `7.5.0` 并使用 Responses API；该依赖只存在于 `packages/ai-provider-openai`，不进入供应商中立业务编排。
- 初始角色配置：意图解析初始配置为 `openai / gpt-5.6-luna`，管理分析为 `openai / gpt-5.6-terra`，受控回退为 `openai / gpt-5.6-luna`。这些是服务器端可替换配置，不是业务契约；切换继续受 D-174 的评估闸门约束。
- 调用约束：意图解析使用无额外或最低推理设置、最大输出 400 tokens；管理分析使用低推理设置、最大输出 1200 tokens。调用非流式，设置 `store: false`，不开放工具调用，适配器将公共 Zod 模式转换为供应商结构化输出约束后再次用公共模式校验。
- 接口与输入：浏览器只调用统一的 `POST /api/v1/ai/respond`，不得直接访问供应商；用户自然语言输入最多 500 个 Unicode 字符，页面范围与确定性证据由服务端组装，浏览器不能提交权威数字或任意证据 ID。
- 内容安全：使用 `omni-moderation-latest` 检查用户输入，命中或服务异常时返回受控中文状态并保留固定示例；模型不承担确定性查询、证据生成或内容权限判断。
- 可靠性：单次供应商调用硬超时 20 秒，只对 429 或 5xx 最多重试一次，使用短随机退避；校验失败、内容拒绝、4xx 参数错误和预算熔断不重试。
- 用量记录：只记录 D-138 允许的请求级元数据及标准化 token、模型、供应商、延迟和费用，不记录自然语言全文、完整回答或证据正文。

## D-181：冻结第二闸门匿名限流、费用与证据快照实现

- 状态：已确认
- 日期：2026-08-20
- 限流存储：第二闸门使用 Upstash Redis Free 保存短期匿名限流和费用计数，不向 D-149 的 `app_reader` PostgreSQL 角色增加写权限，也不在第一闸门引入 Redis。
- 匿名标识：服务端使用每日轮换密钥对规范化 IP 与 User-Agent 摘要做 HMAC，限流系统与供应商 `safety_identifier` 只使用不可逆派生值；不得记录或持久化原始 IP 和完整 User-Agent。
- 限流算法：使用 Redis 侧原子滑动窗口；Upstash 不可用、超时或返回不确定状态时失败关闭真实模型调用，页面继续显示固定示例和完整确定性分析。
- 费用控制：调用前按角色与最大 token 上限原子预留费用，完成后按供应商返回用量结算差额；月度人民币预算由版本化美元价格、保守汇率和额外 10% 安全余量换算，价格或汇率版本变化后重新验证阈值。
- 会话快照：真实回答、采用范围和不可变证据快照默认只保存在 `sessionStorage`，不写入共享业务数据库；页面恢复必须同时校验契约、数据发布、供应商、模型、提示词和模式版本。
- 证据约束：模型只能引用服务端提供的证据 ID 白名单，输出中的数值必须能回指同一确定性结果；任何额外数字、未知证据或范围不一致都使生成结果失败并回退固定示例。
- 回归触发：供应商、模型、适配器、提示词、输出模式或价格版本改变时，重新运行完整 20 题固定 AI 评估集以及数字越界、证据引用、限流、费用和失败回退测试，未通过不得激活新配置。

## D-182：冻结首个纵向切片的技术排除项

- 状态：已确认
- 日期：2026-08-20
- 第一闸门不引入 ORM、查询构建器、GraphQL、OpenAPI 代码生成、Redis、消息队列、后台任务框架、全局状态库、客户端查询缓存库、第三方样式/组件/图表系统、Storybook、完整 i18n、账号体系、移动端专项、PWA、分析埋点、微服务或 Python 服务。
- 第二闸门不引入 Dify、LangChain 或通用 Agents 框架；模型调用沿用 D-174 的小型供应商中立网关和显式适配器。若未来出现多个工具、长流程状态机或跨系统代理需求，必须以可测量复杂度为依据重新决策。
- 排除项不是永久禁令；只有冻结范围出现无法通过现有架构满足的已验证需求，并完成成本、依赖、安全、部署和迁移评估后，才能新增对应技术。

## D-183：依赖版本通过可执行兼容性闸门后才能冻结

- 状态：已确认
- 日期：2026-08-20
- 决策：新增或升级依赖完成版本与官方文档调研后只能标记为“候选”；只有在精确目标运行时上完成最小工程验证并且全部检查通过，才能写为“已确认”或进入正式锁文件。单独可用、版本存在或顶层 peer 范围匹配均不足以证明组合兼容。
- 严格安装：pnpm 启用 `strict-peer-dependencies=true`、`engine-strict=true` 和精确保存；任何直接或传递 peer dependency 不满足都使安装失败，不允许以警告状态冻结版本或进入 CI。
- 必过流程：候选组合必须顺序通过冻结安装、lint、独立 typecheck、单元测试和正式生产构建；涉及数据库、浏览器或原生二进制时，还须完成相应最小集成检查。验证使用与 CI 相同的 Node、pnpm、操作系统族和精确版本。
- 检查范围：除直接依赖外，必须检查传递依赖、运行时 engine、peer dependency、程序化 API、构建插件和生成器兼容性；编译器命令行可运行不代表依赖其 API 的 lint、编辑器或框架工具兼容。
- 变更隔离：Next.js、TypeScript、ESLint、测试运行器、数据库主版本等高耦合核心组件原则上一次只升级一项；确需联合升级时作为一个候选矩阵验证并记录每项必要性。
- 证据记录：每次冻结保存日期、目标平台、精确版本、锁文件和五步命令结果；失败组合保留原因和修订方案，不通过忽略 peer、关闭检查、宽化版本范围或局部绕过形成假通过。
- 既有决策处理：D-161、D-172 和 D-175 中尚未通过组合验证的版本视为待修订候选，不因此前“已确认”状态绕过本闸门；兼容修订须单独批准后才能更新决策与锁文件。

## D-184：TypeScript 7 编译与 TypeScript 6 工具 API 并行

- 状态：已确认
- 日期：2026-08-20
- 决策：保留 TypeScript `7.0.2` 作为权威命令行编译器；根级增加 `@typescript/native` 对 TypeScript 7.0.2 的 npm alias，并将根级 `typescript` 映射到 `@typescript/typescript6` `6.0.2`，只为 typescript-eslint 等仍依赖 JavaScript API 的工具提供兼容层。
- Web 边界：`apps/web` 直接依赖 TypeScript `7.0.2`，Next.js 16.3.1 明确启用 `experimental.useTypeScriptCli: true`，构建期类型检查运行 TypeScript 7 CLI，不调用 TypeScript 6 API；Web 不把兼容包声明为自身编译器。
- ESLint 修订：D-172 的 ESLint 从 `10.8.1` 调整为 `9.39.5`，并把既有传递依赖 `typescript-eslint` `8.67.0` 提升为根级直接依赖；Next.js 规则只作用于 `apps/web`，通用 TypeScript 规则覆盖全部正式 workspace，临时产物不进入 lint 输入。
- 精确解析：`@typescript/typescript6` 包本身精确锁定 6.0.2，其内部 `@typescript/old` 解析由唯一锁文件固定；该兼容 API 的任何变化与 TypeScript 7 升级都必须重新执行 D-183，不使用宽范围重新解析替代锁文件。
- 验证结果：Node.js 24.15.0 与 pnpm 10.34.5 下，严格冻结安装无 peer 冲突，ESLint 零警告通过，4 个 workspace 的 TypeScript 7 独立检查通过，Vitest 3 个文件共 4 项测试通过，Next.js 生产构建通过且 `/`、`/attribution` 均为动态服务端路由。
- 失败组合：TypeScript 7.0.2 直接作为 typescript-eslint API、以及 ESLint 10.8.1 配合当前 Next.js 传递插件的组合均已验证失败并禁止冻结；未来只有上游明确支持且重新通过 D-183 后才能移除兼容层或升级 ESLint 主版本。
