# LogiPlan Agent 协作规则

## 权威资料

- 业务术语以 `CONTEXT.md` 为准，冻结决策以 `docs/decisions.md` 为准，查询与证据边界以 `docs/query-contract.md` 为准，当前阶段以 `docs/development-roadmap.md` 为准。
- 多 Agent 的完整分级、交接、验证和安全规则见 `docs/multi-agent-workflow.md`。处理非 L0 任务前必须读取其中与任务相关的章节。
- 能从仓库、测试或权威文档确定的事项先自行核查；只有命中暂停条件时才向用户提问。需要提问时一次只问一个问题，并给出推荐答案。
- 默认使用现代汉语标准普通话书面语体，保持客观、简洁、直接。

## 自动分级与委派

1. 主 Agent先根据请求和最小只读检查进行初筛。明显属于 L0 的任务直接处理，不创建子 Agent。
2. 不能明确判定为 L0 时，先创建 `context_scout`；主 Agent根据其最小交接包把任务定为 L1 或 L2。
3. 任一 L2 信号成立即按 L2；无法排除 L2 时也按 L2。L1 中途发现 L2 信号时停止原写入流程，提交交接包，并创建新的 L2 `slice_owner`。
4. 创建 `slice_owner` 时必须显式传入模型与推理强度，禁止隐式继承：L1 使用 `gpt-5.6-terra` / `medium`；L2 默认使用 `gpt-5.6-sol` / `medium`，仅在冻结契约、精确计算、数据库、权限安全、公共接口或跨包影响仍有未消除的不确定性，或定向验证失败且需要更深推理时，升级为 `gpt-5.6-sol` / `high`。
5. L2 完成自测后必须创建 `independent_auditor`。其他命中强制审查条件的任务同样必须审查。
6. 最多同时运行三个子 Agent；只读探索可以并行，但任何时刻最多只能有一个源码写入者。

## 工作流所有权

- 按“共享页面目标、共享上下文、可独立验收”的纵向工作流划分 `slice_owner` 会话，不按单个文件或技术目录拆分。
- 同一个 `slice_owner` 负责影响确认、实现、定向测试、必要修复和工作流级自测；不要把编码与首次测试拆给不同 Agent。
- 审查问题返回原 `slice_owner` 修复，再由原 `independent_auditor` 定向复查。两轮修复—复查仍未通过时，立即停止相关 Agent 并向用户提问。

## 工作区与外部操作

- 每个写入或审查任务开始前检查 Git 状态，把已有改动视为用户资产；不得覆盖、清理或顺手整理任务范围外的文件。
- 子 Agent不得创建分支、提交、暂存、变基、合并、推送或改写历史，也不得使用破坏性 Git 命令。默认不创建独立 worktree。
- `slice_owner` 只能修改交接包列明的 `write_scope`；需要扩大范围时先返回主 Agent重新评估。
- 可自动执行隔离的本地构建、测试、冻结依赖安装、Docker Compose 和临时数据库验证。依赖变更、真实密钥、付费 API、远程写入、部署、公开发布及破坏性操作必须先获得用户授权。

## 完成标准

- 按 `docs/multi-agent-workflow.md` 的分级验证矩阵执行；未执行的检查不得表述为通过。
- 交接只传递最小交接包，不复制完整对话、长代码或原始日志。
- 不在仓库中保存逐次 Agent 对话或临时工作日志；只保存正式决策和最终有效结果。
